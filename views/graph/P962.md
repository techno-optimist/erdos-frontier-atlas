# Erdős #962 — attack card

> Erdős problem #962 · https://www.erdosproblems.com/962
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:962:28389538` — A327909(1000) — smallest start of a run of 1000 consecutive integers each having a prime factor > 1000 (first cell …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 22369305365 (= A327909(999) (b-file, David Spencer ext of Thomas Garrison's 369 terms) + monotonicity: the first 999 integers of any valid 1000-run form a valid 999-run) · upper — (—)
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: a start s with each of s, s+1, ..., s+999 having a prime factor > 1000; for the exact cell, the least such s certified by exhaustive scan below it
- witness verifier: for each of the 1000 integers, divide out all primes ≤ 1000 and check the cofactor exceeds 1 — milliseconds, dependency-free
- notes: a(999) ≈ 2.24e10 and the sequence grows slowly (long constant stretches), so a segmented 1000-smooth sieve to ~1e11 (≈ 2e11 division ops in C) settles a(1000) in hours on one box — among the easiest genuine cells in this chunk. Asymptotics for Erdős's k(n): log k(n) ≫ sqrt(log n loglog n) (Erdős 1976, per erdosproblems #962); constant improved to (1/sqrt(2) − o(1)) attributed to Tang in site remarks (2026) — primary arXiv source NOT located, treat as site attribution; upper: k(n) ≤ …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P962` and the surface id in your receipt so the next agent's graph build finds your result.


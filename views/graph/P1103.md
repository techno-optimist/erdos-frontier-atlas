# Erdős #1103 — attack card

> Erdős problem #1103 · https://www.erdosproblems.com/1103
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Growth rate of an infinite sequence A of integers such that every element of A+A is …[truncated; see source])** — Asymptotic-only problem (erdosproblems.com marks it not resolvable by finite computation). Known: every such sequence satisfies a_j > 0.24·j^(4/3) for all j, and a squarefree-termed example exists with a_j < exp(5j/log j) for large j (van Doorn–Tao, arXiv:2512.01087, 2025); Konyagin (Izv. RAN 2004) had earlier a_j >> j^(15/11-o(1)). The finite analogue is problem #1109 (same OEIS A392164), which IS machine-actionable — see that entry. Upper bounds for f(N) in #1109 imply lower bounds here. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:1103:b2f04f11` — Growth rate of an infinite sequence A of integers such that every element of A+A is squarefree (how slowly can a_j …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: No finite witness: any finite initial segment says nothing about the required infinite squarefree-sum property or the asymptotic growth rate, so no cheaply checkable object can move the known bounds.
- witness verifier: None exists — the quantity is an asymptotic growth exponent for an infinite sequence; only proofs move it.
- notes: Asymptotic-only problem (erdosproblems.com marks it not resolvable by finite computation). Known: every such sequence satisfies a_j > 0.24·j^(4/3) for all j, and a squarefree-termed example exists with a_j < exp(5j/log j) for large j (van Doorn–Tao, arXiv:2512.01087, 2025); Konyagin (Izv. RAN 2004) had earlier a_j >> j^(15/11-o(1)). The finite analogue is problem #1109 (same OEIS A392164), which IS machine-actionable — see that entry. Upper bounds for f(N) in #1109 imply lower bounds here.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #1109 shares A392164 → card `views/graph/P1109.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1103` and the surface id in your receipt so the next agent's graph build finds your result.


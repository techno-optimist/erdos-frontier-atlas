# Erdős #1100 — attack card

> Erdős problem #1100 · https://www.erdosproblems.com/1100
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:1100:3028b7c3` — g(k) = max over squarefree n with omega(n)=k of tau_perp(n), where tau_perp counts coprime consecutive-divisor pairs — …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower (sqrt(2)+o(1))^k (Erdős–Simonovits (see Erdős [Er81h], via erdosproblems.com/1100 remarks, paraphrased)) · upper (2-c)^k for some absolute c>0 (Erdős–Simonovits (see Erdős [Er81h], via erdosproblems.com/1100 remarks, paraphrased))
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `unknown`
- witness object: for a concrete k: a set of k primes whose squarefree product n achieves a large tau_perp(n) — builds a finite-k record table (no OEIS ledger for g(k) exists yet; creating one is the actionable lane) and large-k families would press the asymptotic base
- witness verifier: dependency-free: enumerate all 2^k divisors as prime-subset bitmasks with exact integer products, sort by value, count consecutive pairs whose bitmasks are disjoint (AND == 0). Exact and trivial for k <= 25
- notes: The bounds are asymptotic bases with unspecified o(1)/c — recorded as expressions, not numerics; nobody has published a finite-k g(k) table as far as checked. Finite-k values are suprema over infinitely many prime choices but depend only on the realizable order type of the 2^k subset-log-sums, so bounded search per k is plausible for small k (hyperplane-arrangement enumeration); grading exact as unknown. Other parts of hub 1100 are asymptotic and not witnessable: tau_perp(n)/omega(n) -> …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `divisors`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1100` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #304 — attack card

> Erdős problem #304 · https://www.erdosproblems.com/304
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research solved, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/304.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:304:6ce3f51e` — A097849(106) — max over 1<=k<=106 of the minimal number of unit fractions needed to write k/106 (row-106 maximum of …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `both` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: For the claimed row max m at numerator k*: a length-T(106,k) Egyptian representation for every k (upper bounds each cell), plus a bounded-DFS transcript showing k*/106 has no representation with m-1 distinct denominators (minimality)
- witness verifier: Exact-arithmetic check that each listed 1/n_1+...+1/n_t sums to k/106 with distinct denominators; minimality re-checked by standard bounded backtracking (denominator bounds at each depth make the search finite) — pure integer arithmetic, no dependencies
- notes: b-file ends at n=105 (a(105)=4); recent activity by von Brömssen (May 2026) suggests the table is actively extendable. The open problem itself is asymptotic: Erdős 1950 gives N(b) >> loglog b, Vose 1985 gives N(b) << sqrt(log b) — that gap is a wall (erdosproblems.com/304, edited Dec 2025, still lists Vose as best; checked arXiv:2512.22083 (van Doorn–Tang 2025) — it concerns v(k) for decompositions of 1, related via #293 but does not improve N(b)). Extending A097849 feeds the empirical …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `unit fractions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P304` and the surface id in your receipt so the next agent's graph build finds your result.


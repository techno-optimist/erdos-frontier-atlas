# Erdős #243 — attack card

> Erdős problem #243 · https://www.erdosproblems.com/243
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: No bounded quantity — structural rigidity conjecture: any integer sequence with …[truncated; see source])** — erdosproblems.com/243 (edited 2026-01-21) lists: Erdős–Straus 1964 [ErSt64] proved a limsup criterion any non-recurrence rational-sum sequence must satisfy; Duverney 2001 [Du01] proved the variant where sum(a_{n+1}/a_n^2 - 1) converges: rational sum iff eventually Sylvester recurrence. A000058 = Sylvester's sequence 2,3,7,43,1807,... (greedy Egyptian expansion of 1); OEIS b-file has 13 terms (doubly exponential growth). [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/243.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $a_1 < a_2 < \dots$ be a sequence of integers such that $\lim_{n\to\infty} \frac{a_n}{a_{n-1}^2} = 1$ and $\sum \frac{1}{a_n} \in \mathbb{Q}$. Then, for all sufficiently large $n \ge 1$, $a_n = a_{n-1}^2 - a_{n-1} + 1$.

## ATTACK SURFACES — which overlay said so

### `S:gap:243:cc14334a` — No bounded quantity — structural rigidity conjecture: any integer sequence with a_n/a_{n-1}^2 -> 1 and rational sum of …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None — an affirmative resolution is a proof over all sequences; a counterexample would be an infinite object (a full sequence plus the rationality of its reciprocal sum), not finitely checkable
- witness verifier: None exists: rationality of an infinite series cannot be certified by finite computation
- notes: erdosproblems.com/243 (edited 2026-01-21) lists: Erdős–Straus 1964 [ErSt64] proved a limsup criterion any non-recurrence rational-sum sequence must satisfy; Duverney 2001 [Du01] proved the variant where sum(a_{n+1}/a_n^2 - 1) converges: rational sum iff eventually Sylvester recurrence. A000058 = Sylvester's sequence 2,3,7,43,1807,... (greedy Egyptian expansion of 1); OEIS b-file has 13 terms (doubly exponential growth).
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #315 shares A000058 → card `views/graph/P315.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P243` and the surface id in your receipt so the next agent's graph build finds your result.


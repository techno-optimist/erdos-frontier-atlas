# Erdős #820 — attack card

> Erdős problem #820 · https://www.erdosproblems.com/820
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A263647 extension — integers n with gcd(2^n-1, 3^n-1) = 1 (equivalently H(n)=3 in Erdős' …[truncated; see source])** — The actual open problem — is H(n)=3 infinitely often, and does H(n) = exp(n^{(c+o(1))/loglog n}) — cannot be settled by finite computation (wall). Known: Erdős (1974) proved H(n) > exp(n^{c/(loglog n)^2}) for infinitely many n; a sharpening to exp(n^{c/loglog n}) is sketched in an unrefereed site comment (van Doorn). H(n) for n=1..10 is 3,3,3,6,3,18,3,6,3,12 (numeric facts paraphrased from erdosproblems.com/820). A263647 conjectured infinite (Ailon–Rudnick, per OEIS comment). Machine lane = …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:820:31388a95` — A263647 extension — integers n with gcd(2^n-1, 3^n-1) = 1 (equivalently H(n)=3 in Erdős' notation); b-file ends at …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 24898 (OEIS A263647 b-file extent (10000 terms; all n <= 24898 classified)) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: an integer n > 24898 with gcd(2^n - 1, 3^n - 1) = 1 (extends the verified table; each such n certifies H(n)=3)
- witness verifier: one bignum gcd: math.gcd(2**n - 1, 3**n - 1) == 1 in Python/GMP; ~n-bit operands, seconds even at n ~ 10^6; fully dependency-free
- notes: The actual open problem — is H(n)=3 infinitely often, and does H(n) = exp(n^{(c+o(1))/loglog n}) — cannot be settled by finite computation (wall). Known: Erdős (1974) proved H(n) > exp(n^{c/(loglog n)^2}) for infinitely many n; a sharpening to exp(n^{c/loglog n}) is sketched in an unrefereed site comment (van Doorn). H(n) for n=1..10 is 3,3,3,6,3,18,3,6,3,12 (numeric facts paraphrased from erdosproblems.com/820). A263647 conjectured infinite (Ailon–Rudnick, per OEIS comment). Machine lane = …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #770 shares A263647 → card `views/graph/P770.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P820` and the surface id in your receipt so the next agent's graph build finds your result.


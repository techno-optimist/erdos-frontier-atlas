# Erdős #769 — attack card

> Erdős problem #769 · https://www.erdosproblems.com/769
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:769:bc20564d` — c(3) — least k0 such that the cube can be decomposed into k homothetic cubes for every k >= k0 (Hadwiger …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower 15 (Connor–Marmorino, J. Geometry 109:19: c(n) >= 2^(n+1)-1 for n >= 3 (cleanest refereed bound; see notes for the stronger classical claim)) · upper 48 (classical dissection identities: every k >= 48 is achievable (OEIS A014544 comments; Hadwiger-problem literature))
- witness side: `upper` · witness feasibility `hard` · exact feasibility `unknown`
- witness object: an explicit dissection of the unit cube into k smaller cubes for an unresolved count — 47 (which would force c(3) <= 47 and contradict the Gardner/Scott claim), or one of 31/32/42/44 whose impossibility Hickerson (2007) could not source
- witness verifier: exact rational check of a list (corner, side) of axis-aligned subcubes: side lengths positive, volumes sum to 1, pairwise interior-disjoint, all contained in the unit cube — exact fractions, dependency-free
- notes: Literature is murky: Scott (AMM 54, 1947) reportedly showed the largest non-achievable k is 47 or 54, and Rychener & Zbinden (1977) dissected 54, which would give c(3)=48 exactly — but Hickerson's 2007 OEIS notes could not confirm proofs for 31,32,42,44,47 (impossibility of those five settles the whole list), so the community (and erdosproblems.com, citing Meier's conjecture c(3)=48) still treats it as open. LIBRARIAN TARGET: pull Scott 1947 + Gardner 1992 to possibly close c(3)=48. The Erdős …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `geometry`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P769` and the surface id in your receipt so the next agent's graph build finds your result.


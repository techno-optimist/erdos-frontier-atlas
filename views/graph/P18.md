# Erdős #18 — attack card

> Erdős problem #18 · https://www.erdosproblems.com/18
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: h(m) for practical m (fewest distinct divisors of m that always suffice to represent …[truncated; see source])** — Known (paraphrased from erdosproblems.com/18, edited Apr 2026): Erdős showed h(n!) < n; Vose (1985) proved infinitely many practical m have h(m) << (log m)^(1/2); the $250 reward (Erdős 1981) is for h(m) < (log log m)^O(1) infinitely often; strengthenings h(n!) < n^o(1) or (log n)^O(1) also open. A005153 (practical numbers; b-file 10000 terms, Eldar/Noe) is definitionally computable with no uncomputed cell — the sequence itself carries no record. A tabulation of h(n!) for small n would be new …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/18.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:18:8fa91b6c` — h(m) for practical m (fewest distinct divisors of m that always suffice to represent every 1 <= n < m) — are there …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — the $250 question quantifies over infinitely many m (and all large n for the factorial variant); a single (m, h(m)) evaluation moves no bound
- witness verifier: h(m) for one m is computable (worst-case min-cardinality subset-sum over the divisors of m) but expensive for interesting m like n!, and no OEIS table of h-values exists to extend. erdosproblems.com marks the problem as not resolvable by finite computation.
- notes: Known (paraphrased from erdosproblems.com/18, edited Apr 2026): Erdős showed h(n!) < n; Vose (1985) proved infinitely many practical m have h(m) << (log m)^(1/2); the $250 reward (Erdős 1981) is for h(m) < (log log m)^O(1) infinitely often; strengthenings h(n!) < n^o(1) or (log n)^O(1) also open. A005153 (practical numbers; b-file 10000 terms, Eldar/Noe) is definitionally computable with no uncomputed cell — the sequence itself carries no record. A tabulation of h(n!) for small n would be new …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `divisors`, `factorials`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P18` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #396 — attack card

> Erdős problem #396 · https://www.erdosproblems.com/396
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/396.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is it true that for every $k$ there exists $n$ such that $$\prod_{0\leq i\leq k}(n-i) \mid \binom{2n}{n}?$$

## ATTACK SURFACES — which overlay said so

### `S:gap:396:6a421b02` — A375077(16) — least k such that k(k−1)···(k−16) divides C(2k,k) (first uncomputed cell; a(1)–a(15) listed, a(15) = …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower >= 18253129921842 (Monotonicity (the n=15 product divides the n=16 product, so a(n) is nondecreasing) chained to certified-minimal a(13) (Dehorty exhaustive search + Lean 4 Small Prime Barrier sieve, OEIS A375077 comment)) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: An integer k with k(k−1)···(k−16) | C(2k,k); existence guaranteed for every window length (Pomerance 2014: the valid-n set has density 1). Minimal such k is the cell value
- witness verifier: Factor the 17 consecutive integers k−16..k (Pollard rho at ~1e17 scale, trivial); for each prime p dividing the product check v_p(product) <= number of carries when adding k + k in base p (Kummer); explicitly include all primes p < 2·16+3. Seconds, dependency-free beyond big-int + rho.
- notes: Flagship machine-shaped cell in this chunk, but ACTIVELY OWNED: minimality of a(8)–a(13) is certified by exhaustive search plus a small-prime sieve whose completeness ('Small Prime Barrier Theorem') is formally verified in Lean 4 + Mathlib (Justin Dehorty, Mar 2026; repo github.com/jdehorty/erdos396 with validation certificates — linked from OEIS, not independently audited here). Certification status of listed a(14), a(15) is not stated in the OEIS comment; presuming them minimal would lift the …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `binomial coefficients`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P396` and the surface id in your receipt so the next agent's graph build finds your result.


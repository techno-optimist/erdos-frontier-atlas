# Erdős #669 — attack card

> Erdős problem #669 · https://www.erdosproblems.com/669
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A003035(15) — maximum number of lines containing exactly 3 of 15 points in the plane …[truncated; see source])** — Problem 669 as posed asks to estimate f_k(n) (exactly k) and F_k(n) (at least k) and determine lim F_k(n)/n^2, lim f_k(n)/n^2 — asymptotic, not machine-closable; the linked orchard cell is the record lane. Burr–Grünbaum–Sloane 1974: f_3(n) = n^2/6 - O(n); trivially lim F_k(n)/n^2 <= 1/(k(k-1)). Green–Tao 2013 (arXiv:1208.4714): a(n) = floor(n(n-3)/6)+1 for all sufficiently large n, which gives 31 at n=15 — so the 32-line witness likely does NOT exist, and closing the cell probably needs a …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:669:e5a0486b` — A003035(15) — maximum number of lines containing exactly 3 of 15 points in the plane (classical orchard problem; first …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 31 (OEIS A003035: Sloane comment (Feb 11 2013) 'a(15) is 31 or 32'; Pegg construction illustrations on the entry give explicit 31-line 15-point configurations) · upper 32 (OEIS A003035: Sloane comment (Feb 11 2013); cell open since Burr–Grünbaum–Sloane, Geometriae Dedicata 1974)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: 15 points with exact algebraic/projective coordinates determining 32 lines that each contain exactly 3 of the points
- witness verifier: Exact-arithmetic collinearity via 3x3 determinants; count lines with exactly 3 incident points and confirm 32. Dependency-free.
- notes: Problem 669 as posed asks to estimate f_k(n) (exactly k) and F_k(n) (at least k) and determine lim F_k(n)/n^2, lim f_k(n)/n^2 — asymptotic, not machine-closable; the linked orchard cell is the record lane. Burr–Grünbaum–Sloane 1974: f_3(n) = n^2/6 - O(n); trivially lim F_k(n)/n^2 <= 1/(k(k-1)). Green–Tao 2013 (arXiv:1208.4714): a(n) = floor(n(n-3)/6)+1 for all sufficiently large n, which gives 31 at n=15 — so the 32-line witness likely does NOT exist, and closing the cell probably needs a …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #101 shares A006065 → card `views/graph/P101.md`
- #211 shares A003035 → card `views/graph/P211.md`
- #588 shares A006065, A008997 → card `views/graph/P588.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P669` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #251 — attack card

> Erdős problem #251 · https://www.erdosproblems.com/251
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: No bounded quantity — is sum_{n>=1} p_n/2^n irrational, p_n the n-th prime? (constant = …[truncated; see source])** — Closest known result (erdosproblems.com/251, edited 2025-09-28): Erdős 1958 [Er58b] proved sum p_n^k/n! is irrational for every k >= 1 — the factorial-denominator analogue; the 2^n version is open. Erdős [Er88c] further conjectured sum p_n^k/2^n irrational for all k, and irrationality of sum p_n/(g_1...g_n) whenever g_n >= 2, g_n = o(p_n) (g_n = p_n + 1 shows a growth condition is needed). [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/251.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is $\sum_{n=1}^\infty \frac{p_n}{2^n}$ irrational? Here $p_n$ is the $n$-th prime ($p_1=2, p_2=3, \dots$).

## ATTACK SURFACES — which overlay said so

### `S:gap:251:89bbad6b` — No bounded quantity — is sum_{n>=1} p_n/2^n irrational, p_n the n-th prime? (constant = 3.67464396601132877... = …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None — irrationality of a single explicit constant; no finite witness exists in either direction
- witness verifier: None: digits are cheaply computable from the primes but decide nothing
- notes: Closest known result (erdosproblems.com/251, edited 2025-09-28): Erdős 1958 [Er58b] proved sum p_n^k/n! is irrational for every k >= 1 — the factorial-denominator analogue; the 2^n version is open. Erdős [Er88c] further conjectured sum p_n^k/2^n irrational for all k, and irrationality of sum p_n/(g_1...g_n) whenever g_n >= 2, g_n = o(p_n) (g_n = p_n + 1 shows a growth condition is needed).
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #980 shares A098990 → card `views/graph/P980.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P251` and the surface id in your receipt so the next agent's graph build finds your result.


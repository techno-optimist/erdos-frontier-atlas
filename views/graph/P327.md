# Erdős #327 — attack card

> Erdős problem #327 · https://www.erdosproblems.com/327
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:327:c40419c7` — A384927(5001) — max size of S in {1..5001} with a+b never dividing ab for distinct a,b in S; monotonicity forces the …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 3528 (a(5000)=3528 from Kesarwani b-file (terms 1..5000) + monotonicity a(n+1)>=a(n)) · upper 3529 (a(n+1) <= a(n)+1 (adding one element to the ground set adds at most one to the max))
- witness side: `both` · witness feasibility `open-easy` · exact feasibility `drat-candidate`
- witness object: Lower/closing: an explicit 3529-element subset of {1..5001} with a+b never dividing ab. Upper/closing: an UNSAT certificate (DRAT or ILP dual) that no 3529-element such set exists
- witness verifier: Set check is O(k^2) ~ 6.2M integer divisibility tests ((a+b) | ab), dependency-free, sub-second; the UNSAT side is a max-independent-set decision on a 5001-vertex graph whose edges are exactly the pairs with a+b | ab — checkable with drat-trim or an LP-dual audit
- notes: Conjectured a(n)/n -> 1/2 (odd numbers are an independent set of density 1/2 since a+b even, ab odd). The open content is whether S can be 'substantially more' than the odds, and the a+b | 2ab variant; van Doorn showed density >= 25/28+o(1) forces a bad pair (via #301 discussion). Extending the table sharpens the empirical excess a(n)-ceil(n/2). Site notes 1 claimed proof and 11 comments not yet incorporated (accessed 2026-07-18) — scout should re-check before major effort. Asymptotic question …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #321 shares A384927 → no card (registers only: `atlas/stubs.json`)

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P327` and the surface id in your receipt so the next agent's graph build finds your result.


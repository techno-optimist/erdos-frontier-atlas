# Erdős #408 — attack card

> Erdős problem #408 · https://www.erdosproblems.com/408
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Growth/normal-order constant of f(n) = A049108(n), the number of iterations of Euler phi …[truncated; see source])** — Pillai 1929 [Pi29]: log_3 n < f(n) < log_2 n for all large n; Shapiro 1950 [Sh50]: f is essentially multiplicative. Erdős–Granville–Pomerance–Spiro 1990 [EGPS90] answered the problem's first two questions affirmatively CONDITIONAL on a form of the Elliott–Halberstam conjecture — the unconditional statement is the wall. Also expected: for almost all n the largest prime factor of the k-th phi-iterate is n^{o(1)} whenever k → ∞. Discussed in Guy B41. A049108 = A003434 + 1. No bounded machine …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:408:643da25d` — Growth/normal-order constant of f(n) = A049108(n), the number of iterations of Euler phi needed to send n to 1 (does …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none
- witness verifier: No cheap witness: A049108 is trivially computable to any range (log_3 n ≪ f(n) ≪ log_2 n pins every value between ~log n bounds), and the open content is an asymptotic normal-order statement no finite prefix can move.
- notes: Pillai 1929 [Pi29]: log_3 n < f(n) < log_2 n for all large n; Shapiro 1950 [Sh50]: f is essentially multiplicative. Erdős–Granville–Pomerance–Spiro 1990 [EGPS90] answered the problem's first two questions affirmatively CONDITIONAL on a form of the Elliott–Halberstam conjecture — the unconditional statement is the wall. Also expected: for almost all n the largest prime factor of the k-th phi-iterate is n^{o(1)} whenever k → ∞. Discussed in Guy B41. A049108 = A003434 + 1. No bounded machine …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `iterated functions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P408` and the surface id in your receipt so the next agent's graph build finds your result.


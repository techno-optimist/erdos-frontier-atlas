# Erdős #104 — attack card

> Erdős problem #104 · https://www.erdosproblems.com/104
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures
- prize: $100 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:gap:104:5664e3ba` — A003829(9) — maximum number of distinct unit circles each passing through >= 3 of 9 points in the plane (first …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 16 (Monotonicity (add a 9th point far from all unit circles) from A003829(8)=16 (Harborth-Mengersen, Discrete Math 60 (1986) 193-197)) · upper 24 (DERIVED pair-counting (the Harborth-Mengersen correction of Erdos's double count): each pair of points lies on <= 2 unit circles, each counted circle contains >= 3 pairs => <= floor(2*C(9,2)/3) = 24; tight at n=4 (a(4)=4))
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `unknown`
- witness object: 9 points (coordinates as exact algebraic numbers or high-precision floats plus symbolic certificates) admitting >= 17 distinct unit circles each through at least 3 of the points. Note the exactly-3 vs at-least-3 reading: OEIS name says circles containing 3 points; erdosproblems.com/104 counts circles with at least three — record both counts for safety.
- witness verifier: For each of the C(9,3)=84 triples compute the circumcircle exactly and test unit radius (algebraic identity |p-c|^2=1); deduplicate centers; count. Exact arithmetic (rational/quadratic fields) or certified interval arithmetic; no dependencies beyond bignum. Milliseconds.
- notes: $100 headline is asymptotic: is the number of rich unit circles o(n^2)? Known (per erdosproblems.com/104, paraphrased): >> n trivially (Erdos 1981), >> n^{3/2} construction (Elekes 1984, possibly the right order); upper O(n^2), refined to n(n-1)/3 (Harborth-Mengersen 1986); Erdos offered 100 GBP for deciding O(n^{3/2}) in general position. The finite cell a(9) appears untouched since 1986 (OEIS 'more', no post-1986 refs found) — an 18-dimensional continuous search plus exact certification is a …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P104` and the surface id in your receipt so the next agent's graph build finds your result.


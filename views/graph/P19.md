# Erdős #19 — attack card

> Erdős–Faber–Lovász conjecture · https://www.erdosproblems.com/19
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **TRAP** — prize **$500** + upstream says `decidable`, but our status is **wall**: the finite handle does not help at our scale (atlas/walls.md, finite-handle table). The prize is the lure; this line is the hook.
- **WALL (problem)** — catalogued wall — see atlas/walls.md [atlas/stubs.json status=wall]
- **WALL (surface: Erdos-Faber-Lovasz: if G is an edge-disjoint union of n copies of K_n, is chi(G)=n? …[truncated; see source])** — Our existing WALL judgement STANDS, and for a sharper reason than we had. Two independent kills. (1) The top of the finite check is not finite in any usable sense: KKKMO21's proof covers only "sufficiently large n" via absorption/nibble with no explicit threshold, so even a complete sweep of n=13 leaves n=14..N_0 with N_0 unknown — the DECIDABLE tag is nominal. (2) The bottom is already at a hard SAT wall: the state of the art (SMS co-certificate learning, 2023) closed n<=12 and then failed on the middle-m band at n=13 after 3 CPU-days per case. We would be re-entering exactly where a purpose-built symmetry-modulo SAT framework stopped, with no new idea. Worth logging separately: our atlas …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **wall** (atlas/stubs.json)
- upstream: **Open** (raw `decidable`, finite handle `decidable`)
- formal: not in formal-conjectures
- prize: $500 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:triage:19` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: Erdos-Faber-Lovasz: if G is an edge-disjoint union of n copies of K_n, is chi(G)=n? Equivalently, is every linear hypergraph on n vertices n-edge-colourable?
- witness: CONCRETE: a linear hypergraph H on n vertices (equivalently a partial linear space / a set of n cliques K_n pairwise sharing at most one vertex) whose chromatic index exceeds n. Refuting n-colourability is a coNP claim, but at these sizes it is a SAT UNSAT call with a checkable DRAT proof.
- smallest untested case: n = 13 hyperedges-count m = 33 (i.e. linear hypergraphs on 13 vertices with m in [33,54] — the exact band Kirchweger-Peitl-Szeider could not close).
- known frontier: Hindman 1981 [Hi81]: all hypergraphs with at most 10 vertices (in fact whenever the >=3-element edges span <=10 vertices). Romero & Alonso-Pecina 2014, Discrete Math. Algorithms Appl. 6(3):1450039, title literally "The Erdos-Faber-Lovasz conjecture is true for n <= 12", built on Betten & Betten's enumeration of linear spaces on <=12 points. Kirchweger, Peitl, Szeider, SAT 2023 (LIPIcs vol 271, paper 13) re-verified n<=12 with DRAT proofs and extended in bands: Theorem 14 gives n=13, m in …[truncated; see source]
- search-space estimate: n=13, m=33: candidate line-sets are 33-subsets of the 2^13-1-13-C(13,2)=8100 subsets of [13] of size >=3, so <= C(8100,33) ~ 10^92 before linearity and isomorphism pruning; the SMS pipeline that fully cleared n<=12 timed out at >3 CPU-days on exactly this band.
- verification: True
- verdict reason: Our existing WALL judgement STANDS, and for a sharper reason than we had. Two independent kills. (1) The top of the finite check is not finite in any usable sense: KKKMO21's proof covers only "sufficiently large n" via absorption/nibble with no explicit threshold, so even a complete sweep of n=13 leaves n=14..N_0 with N_0 unknown — the DECIDABLE tag is nominal. (2) The bottom is already at a hard SAT wall: the state of the art (SMS co-certificate learning, 2023) closed n<=12 and then failed on the middle-m band at n=13 after 3 CPU-days per case. We would be re-entering exactly where a purpose-built symmetry-modulo SAT framework stopped, with no new idea. Worth logging separately: our atlas …[truncated; see source]
- no move applies mechanically

### `S:deep:19` — deep audit: **WALL**, lane `SAT+DRAT-nonexistence`, board **HEAVY** (problems.json)

- finite object: A linear hypergraph / linear space on n vertices with m hyperedges (equivalently its n×m 0/1 incidence matrix). A would-be counterexample is one whose line graph needs >n colors; the extremal object is the complete set of χ=n linear spaces or the smallest m_k config with χ≥n+k.
- current record: Asymptotic headline PROVED for all sufficiently large n: Kang, Kelly, Kühn, Methuku, Osthus (FOCS 2021; Annals of Math 198(2):537–618, 2023); prior Kahn (1992) (1+o(1))n. Small-case computational frontier: fully verified n≤12 — Hindman n≤10 (1981), Romero–Alonso-Pecina n≤12 (2014, heuristic, no proof), Kirchweger–Peitl–Szeider (SAT 2023, first DRAT-certified) full n≤12 plus only low-m/high-m bands for n=13–18 (middle-m unsolved at 3 CPU-days/instance; hardest solved single instance n=15,m=85 ≈2.6 CPU-days). χ=n extremal linear spaces completely classified for n≤12 (KPS Thm 15: H_{n,k} chain + Fano + sporadics).
- frontier: EFL fully verified n <= 12; the n=13 bucket is a whole-family DRAT-certificate claim (pigeonhole-hard CDCL)
- verifier: Verifying a PROPOSED n-edge-coloring is trivial poly-time: build the intersection (line) graph and check every intersecting pair got distinct colors, O(m²). BUT the load-bearing direction — proving a bucket (n,m) has NO non-colorable member, i.e. non-colorability of the hard instances — is coNP and empirically pigeonhole-hard (CDCL exponential). KPS pipeline emits a DRAT certificate for the whole (n,m) exhaustion, independently checkable.
- attack: Run the open-source KPS SMS+CCL pipeline on ~100 cores to close the gray middle-m band at n=13 (and possibly low bands of n=14) with DRAT certs. Rough compute: hardest solved instance was ~2.6 CPU-days; the unsolved middle band is many buckets each plausibly 10–100× harder → farm-months, and the K₁₃-not-12-colorable-style coloring-UNSAT buckets are pigeonhole-hard and likely infeasible at ANY realistic core count. Net: a possible incremental "EFL verified for n≤13" note, no prize, no theorem, blocked from going meaningfully further by the resolution wall.
- applies: **SAT + DRAT nonexistence** (predicate `lane-move`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `chromatic number`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P19` and the surface id in your receipt so the next agent's graph build finds your result.


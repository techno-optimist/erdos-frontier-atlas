# Erdős #961 — attack card

> Erdős problem #961 · https://www.erdosproblems.com/961
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/961.lean)

## STATEMENT (Apache-licensed, Lean-derived)

It is conjectured that $f(k) \ll (\log k)^O(1)$.

## ATTACK SURFACES — which overlay said so

### `S:gap:961:b748c745` — A213253(269) — first cell beyond Najman's computed range n=1..268; f(n) = minimal L such that every L consecutive …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 16 (= A213253(268) (b-file from Najman, arXiv:1108.3710 / Arch. Math. 97 (2011)) + monotonicity of f) · upper 269 (Sylvester–Schur theorem: f(k) ≤ k (Sylvester 1892; Erdős 1934 proof))
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `unknown`
- witness object: a run of L ≥ 16 consecutive integers, all > 269 and all 269-smooth (gives A213253(269) ≥ L+1)
- witness verifier: trial-divide each of the L integers by the 57 primes ≤ 269 — microseconds, dependency-free
- notes: b-file (268 terms) unchanged since it was entered from Najman's paper; no extension located (2026-07 search). The EXACT value needs a nonexistence proof over all integers — Najman's bounded Lehmer/Pell (Størmer-type) machinery achieved this for n ≤ 268 but its scaling past there is unclear to me, hence exact_feasibility unknown rather than cell. Asymptotics: f(k) < 3k/log k (Erdős 1955); f(k) ≪ (logloglog k/loglog k)·k/log k (Jutila 1974; Ramachandra–Shorey 1973); conjectured (log k)^{O(1)}, …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P961` and the surface id in your receipt so the next agent's graph build finds your result.


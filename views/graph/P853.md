# Erdős #853 — attack card

> Erdős problem #853 · https://www.erdosproblems.com/853
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/853.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:853:05af6bc3` — A390769 extension — r(n) = least even positive integer absent from the first n prime gaps (Erdős' r(x)); b-file holds …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower n = 72 (a(72) = 16) (OEIS A390769 b-file extent (sequence created late 2025, per A-number range and erdosproblems.com/853 link)) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: an extended exact table of r(n) to large n, i.e. for each jump value t the index of the first occurrence of gap t among consecutive primes
- witness verifier: single segmented-sieve pass tracking the set of gaps seen and recomputing the least absent even value at each new gap — dependency-free; independently cross-checkable against published first-occurrence prime-gap tables (Nicely / Oliveira e Silva) which reach ~4·10^18
- notes: Very fresh, very shallow sequence (72 terms) — trivially extendable to n ~ 10^10 with a sieve in hours; the first-occurrence-gap literature effectively determines r up to 2^64 already, so the b-file push is mostly librarian work plus independent verification. The mathematical content is a wall: r(x) -> infinity is equivalent to every even number eventually occurring as a consecutive-prime gap (open, Polignac-adjacent), and r(x)/log x -> infinity is stronger still. Erdős' original statement …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #5 shares A001223 → card `views/graph/P5.md`
- #852 shares A001223 → card `views/graph/P852.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P853` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #334 — attack card

> Erdős problem #334 · https://www.erdosproblems.com/334
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:334:08974111` — A062241(30) — smallest integer >= 2 that is not a sum of two positive 113-smooth numbers (prime(30)=113); first …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower > 2570169839 (Donovan Johnson, OEIS A062241 comment) · upper <= 328878692999 (A062241(n) <= A045535(n-1) (Sungkawichai, OEIS formula + proof PDF at github.com/vzsky/OEIS-compute, Mar 2026); A045535(29) = 328878692999 (Dobbelaere b-file))
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: A specific integer m with 2570169839 < m < 328878692999 that is not a sum of two positive 113-smooth numbers (gives a(30) <= m); candidates likely come from quadratic-residue obstructions a la pseudosquares A045535
- witness verifier: Enumerate all 113-smooth numbers s <= m by exponent recursion over the 30 primes (roughly 10^7-10^8 values at this scale), then confirm m-s is never 113-smooth via sorted-set membership — self-contained, minutes-to-hours on one box; no factoring of m needed
- notes: Exact determination of a(30) needs the full sweep from 2570169840 upward (each m usually certifies fast via a small smooth s with m-s smooth), bounded above by 3.29e11 — heavy but bounded. Parent asymptotic problem: Balog 1989 proved f(n) <<_eps n^(4/(9 sqrt e)+eps) (exponent ~0.2695), best verified bound per erdosproblems.com/334 (edited Apr 2026); conjecturally f(n) <= n^o(1). A search hit (Turku NT seminar) suggested a convolution-estimate improvement toward exponent 103/(256 sqrt e) ~ 0.244 …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P334` and the surface id in your receipt so the next agent's graph build finds your result.


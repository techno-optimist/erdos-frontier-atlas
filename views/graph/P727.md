# Erdős #727 — attack card

> Erdős problem #727 · https://www.erdosproblems.com/727
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/727.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $k ≥ 2$. Does $((n+k)!)^2∣(2n)!$ hold for infinitely many $n$?

## ATTACK SURFACES — which overlay said so

### `S:gap:727:5ffd155d` — A343507(10) = smallest m with (m+10)!^2 | (2m)! — first uncomputed cell (a(9) = 2935782889) (gap_map, kind `next_cell`, confidence C3)

- bounds: lower >= 2935782889 (monotonicity in k ((m+k)!^2 | (2m)! implies (m+k-1)!^2 | (2m)!) from a(9)=2935782889, Mizrahi, OEIS A343507) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: Any m with (m+10)!^2 | (2m)! gives a(10) <= m; a completed scan from 2935782889 up to that m closes the cell exactly. (Same lane one rung down: A389396 lists 1468 k=2 witnesses up to 148003.)
- witness verifier: For each prime p <= 2m check Legendre valuations: sum_j floor(2m/p^j) >= 2·sum_j floor((m+10)/p^j). One candidate at m~10^10 verifies in minutes with a sieve; scanning is fast because most m fail early on a small prime.
- notes: The Erdos-Graham-Ruzsa-Straus [EGRS75] question (infinitude for each fixed k >= 2) is open even for k=2 and is NOT settled by finite witnesses — the record lane is extending A343507 (keywords hard/more, 9 terms). Growth ratios of a(k) (5x-45x per step) suggest a(10) ~ 10^10-10^11: a days-scale filtered scan on one box. k=1 infinitude is Balakran 1929 (A002503: n with (n+1)^2 | C(2n,n); even perfect numbers m give m-1 as terms). I verified a(2)=208 by direct scan, the equivalence for k=2 with …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `factorials`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P727` and the surface id in your receipt so the next agent's graph build finds your result.


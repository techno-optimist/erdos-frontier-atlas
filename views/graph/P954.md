# Erdős #954 — attack card

> Erdős problem #954 · https://www.erdosproblems.com/954
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: error term E(x) = #{(i,j): a_i + a_j ≤ x} − x for Rosen's greedy sequence (A390642); …[truncated; see source])** — By construction the solution count is ≥ x; Erdős and Rosen could not even prove it is ≤ (1+o(1))x (per erdosproblems #954 remarks). OEIS A390642 is brand new (created 2025-12-01, 55 terms, no b-file) with the comment-conjecture a(n) ~ (pi/8) n^2. No literature bounds found. Scout value: numeric exponent-fit at scale is a cheap exploratory (non-record) task; extending the OEIS sequence + b-file is a trivial concrete contribution. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:954:c0672fff` — error term E(x) = #{(i,j): a_i + a_j ≤ x} − x for Rosen's greedy sequence (A390642); Erdős asks if E(x) = …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none that moves a bound — the question is the asymptotic order of E(x); no finite computation certifies an exponent
- witness verifier: n/a; exploratory computation is cheap (the greedy rule is elementary — extend A390642 to ~1e6 terms, x ~ 1e12, plot E(x) and fit the exponent) but proves nothing
- notes: By construction the solution count is ≥ x; Erdős and Rosen could not even prove it is ≤ (1+o(1))x (per erdosproblems #954 remarks). OEIS A390642 is brand new (created 2025-12-01, 55 terms, no b-file) with the comment-conjecture a(n) ~ (pi/8) n^2. No literature bounds found. Scout value: numeric exponent-fit at scale is a cheap exploratory (non-record) task; extending the OEIS sequence + b-file is a trivial concrete contribution.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P954` and the surface id in your receipt so the next agent's graph build finds your result.


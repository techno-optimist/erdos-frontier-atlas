# Erdős #86 — attack card

> C4-free subgraphs of the hypercube (Turán density 1/2?) · https://www.erdosproblems.com/86
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures
- prize: $100 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:deep:86` — deep audit: **UNKNOWN**, lane `SAT+DRAT-nonexistence`, board **READY** (problems.json)

- finite object: A C_4-free subgraph of Q_n with a record number of edges (or a DRAT certificate that no C_4-free subgraph with k+1 edges exists), pinning ex(Q_n,C_4) = A245762.
- current record: ex(Q6,C4)=132 proven exact (Harborth–Nienborg, ILP). ex(Q7,C4)≥304 and ex(Q8,C4)≥680 — new LOWER bounds only, set May 2026 by arXiv:2603.29127 via two-phase simulated annealing (~2×10^8 steps). Upper bound / exact value for Q7 and Q8 is OPEN. Asymptotic density pinned in [1/2, 0.6068] (flag-algebra upper bound).
- frontier: ex(Q7,C4) >= 304, ex(Q8,C4) >= 680 (SA, arXiv:2603.29127, May 2026); exact values open past ex(Q6,C4)=132
- verifier: C_4-freeness check: enumerate the fixed 4-cycles of Q_n (672 in Q7, 1792 in Q8) and verify none is fully contained; O(#cycles) per candidate, microseconds. Trivially exact and cheap.
- attack: SAT feasibility/optimality on Q7: 448 boolean edge vars, totalizer cardinality "sum ≥ 305", 672 length-4 clauses (one per 4-cycle: at least one edge absent), plus lex-leader symmetry breaking over Aut(Q7)=Z2^7⋊S7 (645,120). SAT at 305 ⟹ new record; UNSAT+DRAT ⟹ ex(Q7,C4)=304 proven, extending A245762. In parallel run ILP (maximize Σx_e s.t. per-cycle ≤3) and portfolio local-search/SA to probe 305–310. Rough compute: Q7 instance is small (sub-R(5,5)); a few core-days to weeks of CDCL if it closes, but could be an unbounded wall if the UNSAT is hard. Q8 out of reach.
- applies: **SAT + DRAT nonexistence** (predicate `lane-move`)

### `S:gap:86:02684554` — A245762(7) = ex(Q_7, C4) — maximum edges in a C4-free subgraph of the 7-dimensional hypercube (first uncomputed term; …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 304 (Minamoto, arXiv:2603.29127 (v4 May 2026): explicit constructions certified by exhaustive enumeration of all 672 four-cycles of Q7; 19866 distinct 304-edge solutions found) · upper 328 (DERIVED elementary layer-splitting bound: ex(Q7,C4) <= 2*ex(Q6,C4) + 2^6 = 2*132 + 64, using ex(Q6,C4)=132 (Harborth-Nienborg 1994; ILP re-proof in arXiv:2603.29127). No better published Q7 upper bound found; the paper's ILP did not close Q7)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `drat-candidate`
- witness object: Edge list of a C4-free subgraph of Q7 with >= 305 edges (vertices = 7-bit strings, edges = Hamming-distance-1 pairs).
- witness verifier: Enumerate the 672 fixed 4-cycles of Q7 (C(7,2)*2^5 = 672) and assert none has all 4 edges present; count edges >= 305. O(672) checks, microseconds, no dependencies.
- notes: $100 prize attaches only to the asymptotic Turan-density conjecture (density -> 1/2?), pinned in [1/2, 0.60318] (lower Brass-Harborth-Nienborg 1995; upper Baber flag algebras, per erdosproblems.com/86) — unclaimable by finite computation. Finite frontier: lower record 304 was just set (May 2026) by heavy two-phase simulated annealing with Aut(Q7) diversification, so beating it with the same tools has no edge (hub deep record grades wall-ish). The unattempted novel target is UNSAT+DRAT at 305 …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P86` and the surface id in your receipt so the next agent's graph build finds your result.


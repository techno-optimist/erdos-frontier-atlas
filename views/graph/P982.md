# Erdős #982 — attack card

> Erdős problem #982 · https://www.erdosproblems.com/982
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: f(n)/n where f(n) = largest m such that every convex n-gon has a vertex with >= m …[truncated; see source])** — Lower-bound chain (proof-only, not witnessable): ceil(n/3) Moser 1952 -> floor(n/3+1) Erdős–Fishburn 1994 -> ceil((13n-6)/36) Dumitrescu 2006 -> (13/36 + 1/22701)n - O(1) NPPZ 2013, still current per Sheffer's distinct-distances survey (arXiv:1406.1949) and the problem page (last ed. Oct 2025). The stronger 1946 variant (a vertex with no 3 equidistant co-vertices) is FALSE, so naive attack routes are closed. Witness lane is disproof-shaped: conjecture is widely believed true, so grade hard; but …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]
- **WALL (surface: If n distinct points in R^2 form a convex polygon, some vertex has at least floor(n/2) …[truncated; see source])** — Fails criteria 2 and 3. The witness is concrete (criterion 1 passes) but the search space is a 12-dimensional continuum, not a finite set: the enumerable part is only the distance-equality pattern, and every pattern leaf is a real-quantifier-elimination problem. Verification is exact only if the counterexample happens to have rational coordinates, and every known extremal few-distance configuration (regular polygons, H_8, eigensolver's 21-gon pair with r a root of x^3-x^2-2x+1) is irrational-algebraic — confirming such a witness needs a CAS, which is precisely what we exclude. The theoretical gap 0.361n vs 0.5n has resisted four successive papers over 60 years with the last improvement …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/982.lean)

## STATEMENT (Apache-licensed, Lean-derived)

If $n$ distinct points in $\mathbb{R}^2$ form a convex polygon then some vertex has at least $\lfloor\frac{n}{2}\rfloor$ different distances to other vertices.

## ATTACK SURFACES — which overlay said so

### `S:triage:982` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: If n distinct points in R^2 form a convex polygon, some vertex has at least floor(n/2) different distances to the other vertices. Regular n-gon shows floor(n/2) is best possible. Formalised in FormalConjectures/ErdosProblems/982.lean (research open, AMS 52).
- witness: Concrete: n points in R^2 in convex position such that EVERY vertex sees at most floor(n/2)-1 distinct distances to the other n-1 vertices. Real coordinates — the object is a point configuration, not a combinatorial structure.
- smallest untested case: n = 8: need a convex octagon in which every vertex has at most 3 distinct distances (floor(8/2)=4). n<=7 is forced by the proved lower bounds: Erdos-Fishburn f(n) >= floor(n/3+1) gives 3 at n=7 = floor(7/2), and gives only 3 at n=8 < 4. I verified numerically that Harborth's H_8 (unit square + equilateral triangles erected on each side), the known 8-point set with exactly 3 distinct distances from every point, has a convex hull of only 4 vertices — so it does NOT settle n=8, which is consistent …[truncated; see source]
- known frontier: No exhaustive small-n computation found in the literature. The frontier is analytic: Moser [Mo52] f(n) >= ceil(n/3); Erdos-Fishburn [ErFi94] f(n) >= floor(n/3+1); Dumitrescu [Du06b] f(n) >= ceil((13n-6)/36); best known Nivasch-Pach-Pinchasi-Zerbib, J. Comput. Geom. 4 (2013) 1-12: f(n) >= (13/36 + 1/22701)n - O(1) ~= 0.36112n. Altman proved the weaker total-count statement (n points in convex position determine >= floor(n/2) distances); the per-vertex form is what remains open. Related …[truncated; see source]
- search-space estimate: Continuum: 8 points x 2 coords = 16 reals, minus 4 similarity d.o.f. = a 12-dimensional real semialgebraic feasibility problem; the discrete shell is at most 3^C(8,2) = 3^28 ~= 2.3e13 per-vertex distance-class labelings, and EACH leaf then needs a real-solvability (Groebner/CAD) decision in 12 unknowns — not 1e13 primitive ops but 1e13 CAD calls.
- verification: False
- verdict reason: Fails criteria 2 and 3. The witness is concrete (criterion 1 passes) but the search space is a 12-dimensional continuum, not a finite set: the enumerable part is only the distance-equality pattern, and every pattern leaf is a real-quantifier-elimination problem. Verification is exact only if the counterexample happens to have rational coordinates, and every known extremal few-distance configuration (regular polygons, H_8, eigensolver's 21-gon pair with r a root of x^3-x^2-2x+1) is irrational-algebraic — confirming such a witness needs a CAS, which is precisely what we exclude. The theoretical gap 0.361n vs 0.5n has resisted four successive papers over 60 years with the last improvement …[truncated; see source]
- no move applies mechanically

### `S:gap:982:b54220b2` — f(n)/n where f(n) = largest m such that every convex n-gon has a vertex with >= m distinct distances to the other …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower (13/36 + 1/22701)*n - O(1) (Nivasch–Pach–Pinchasi–Zerbib, J. Comput. Geom. 4(1):1–12 (arXiv:1207.1266)) · upper floor(n/2) (regular n-gon construction (Erdős, conjectured exact))
- witness side: `upper` · witness feasibility `hard` · exact feasibility `wall`
- witness object: a convex n-gon with exact rational (or algebraic) coordinates in which EVERY vertex determines fewer than floor(n/2) distinct distances — this would disprove the Erdős conjecture and lower the upper bound below the regular polygon
- witness verifier: dependency-free: convexity via O(n) exact cross-product signs; per vertex, sort n-1 squared distances in exact arithmetic and count distinct values; total O(n^2 log n)
- notes: Lower-bound chain (proof-only, not witnessable): ceil(n/3) Moser 1952 -> floor(n/3+1) Erdős–Fishburn 1994 -> ceil((13n-6)/36) Dumitrescu 2006 -> (13/36 + 1/22701)n - O(1) NPPZ 2013, still current per Sheffer's distinct-distances survey (arXiv:1406.1949) and the problem page (last ed. Oct 2025). The stronger 1946 variant (a vertex with no 3 equidistant co-vertices) is FALSE, so naive attack routes are closed. Witness lane is disproof-shaped: conjecture is widely believed true, so grade hard; but …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`, `convex`, `distances`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P982` and the surface id in your receipt so the next agent's graph build finds your result.


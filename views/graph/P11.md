# Erdős #11 — attack card

> Erdős problem #11 · https://www.erdosproblems.com/11
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/11.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is every odd $n > 1$ the sum of a squarefree number and a power of 2?

## ATTACK SURFACES — which overlay said so

### `S:gap:11:64d54aa7` — A377587(13) — smallest odd m such that m - 2^k is non-squarefree for every k = 1..13 (gap_map, kind `next_cell`, confidence C3)

- bounds: lower > 1125899906842624 (= 2^50) (OEIS A377587 comment: no further terms below 2^50; terms nest (a condition holding for k<=13 holds for k<=12), so a(13) > 2^50. Consistent with Hercher, arXiv:2411.01964 (2024), who verified the underlying Erdős conjecture (every odd n>1 is squarefree + 2^k) for all odd n up to 2^50.) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: Any odd m > 2^50 together with a certificate list (k, p_k) for k=1..13 where p_k^2 divides m - 2^k (min-type quantity: any qualifying m upper-bounds a(13))
- witness verifier: 13 modular divisibility checks p_k^2 | (m - 2^k) plus one parity check — instant, dependency-free, if the witness carries its square divisors. Without them: squarefree-test 13 numbers < m via trial division + Pollard rho (m ~ 1e15–1e18: seconds).
- notes: Erdős #11 asks if every odd n > 1 is squarefree + a power of 2; verified to 2^50 ~ 1.13e15 (Hercher 2024; earlier Odlyzko to 1e7). A377587 is the natural record ladder: a(1..12) = 11, 29, 533, 849, 434977, ..., 60690478781437; a(13) is the first uncomputed cell. Witnesses are found by covering systems on exponents (p^2 | m - 2^k propagates along k in arithmetic progressions of the order of 2 mod p^2), the same machinery that produced a(12) — hence plausible, not easy. Exact value = bounded …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `additive basis`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P11` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #357 — attack card

> Erdős problem #357 · https://www.erdosproblems.com/357
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/357.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:357:0cca8486` — A364132(23) — least n such that {1..n} contains an increasing 23-term sequence all of whose consecutive-segment sums …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 52 (a(22)=52, OEIS A364132 data (22 terms, keyword hard,more); a(23) >= a(22) by monotonicity (a valid 23-sequence contains a valid 22-sequence)) · upper — (—)
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `drat-candidate`
- witness object: An increasing sequence a_1 < ... < a_23 <= n with all C(24,2)=276 segment sums sum_{u<=i<=v} a_i distinct (gives a(23) <= n); the exact cell additionally needs an exhaustive/UNSAT proof for n-1
- witness verifier: Compute prefix sums and check all 276 pairwise differences distinct — trivial integer check; the nonexistence side is a small SAT/CP instance (choose 23 of ~55 values, alldifferent on segment sums), certifiable by DRAT
- notes: Open content: growth of f(n) — Weisenberg (via #874) gives f(n) >= (2+o(1)) sqrt(n); whether f(n) = o(n) is open; for the non-monotone variant g(n), Hegyvari 1986 proved (1/3+o(1))n <= g(n) and Coppersmith-Phillips (via #867) gives g(n) <= (2/3 - 1/512 + o(1))n — those asymptotics are walls (erdosproblems.com/357, edited Jan 2026). Companion cell: A364153(14) (non-monotone table, 13 terms known, a(13)=18) is an equally cheap SAT target. Erdős also asked whether sum 1/a_i converges for infinite …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P357` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #20 — attack card

> Erdős–Rado sunflower conjecture (f(n,k) < c_k^n) · https://www.erdosproblems.com/20
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A332077 cell Sun(3,4) — minimal number of distinct sets of cardinality <= 3 forcing a …[truncated; see source])** — Table state (OEIS example section): row m=1 and columns n<=2 trivial; row m=2 closed form (Abbott–Hanson–Sauer); Sun(3,3)=21 proven (Erdős–Rado; 20-set extremal family in the OEIS a-file); data stops exactly at Sun(3,4). Sibling open cells with 30–50-year-old construction records: Sun(4,3) >= 55 (Abbott–Hanson–Sauer 1972, 54-set family) and Sun(3,5) >= 89 (Abbott–Hanson 1974, 88 triples), both per hub deep audit atlas/problems.json#20. Witness search is SA-seeding + ILP/MaxSAT over a swept …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **movable** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/20.lean)
- prize: $1000 — prizes are history, not targets (standing rule 2).

## STATEMENT (Apache-licensed, Lean-derived)

Is it true that $f(n,k) < c_k^n$ for some constant $c_k>0$ and for all $n > 0$?

## ATTACK SURFACES — which overlay said so

### `S:deep:20` — deep audit: **MOVABLE**, lane `witness-local-search`, board **HEAVY** (problems.json)

- finite object: A family of N sets, each of size ≤ m, on a fixed ground set [g], containing no s-set that forms a sunflower (equal pairwise intersections). Target: beat a recorded best-known size for a specific (m,s) cell of the A332077 table, or improve the base gadget behind the sqrt(10)^k growth-constant lower bound.
- current record: Sun(3,3)=21 PROVEN (Erdős–Rado; extremal 20-set family). Open lower bounds only for larger cells: Sun(4,3)≥55 (Abbott–Hanson–Sauer 1972, 54-set 4-uniform construction), Sun(3,4)≥39 (Abbott–Exoo 1992, 38 triples), Sun(3,5)≥89 (Abbott–Hanson 1974, 88 triples). Row m=2 fully solved (AHS closed form). Growth-rate lower bound lim f(k)^{1/k} ≳ sqrt(10)≈3.162 via AHS product recursion. No 2023–2026 improvement to any of these lower bounds found; recent activity is all on the asymptotic upper side (ALWZ / Bell–Chueluecha–Warnke (Cs·log k)^k).
- frontier: Sun(3,3)=21 proven; Sun(4,3) >= 55 (1972), Sun(3,4) >= 39 (1992), Sun(3,5) >= 89 (1974) - construction records static 30-50 yr
- verifier: Given N sets: certify no s of them form a sunflower. For s=3, enumerate triples grouped by common core — O(N^3 · m) worst case, far less with core-bucketing. Exact, poly-time for fixed s, cheap (seconds for N in the hundreds).
- attack: Per (m,s) cell, fix ground set [g] and sweep g upward. Build the s-uniform conflict hypergraph on nodes = all C(g,m) m-subsets, hyperedge per sunflower s-tuple (enumerate by fixing a core C0 and picking sets ⊇ C0 with pairwise-disjoint petals). Solve max set-packing-with-ternary-conflicts via (a) simulated annealing / local search seeded from the known record construction to hunt for a +1 extension — hours on a few cores, the natural first strike; and (b) ILP (Gurobi) or MaxSAT with S_g symmetry-breaking for a fixed-g optimum. Sizes: C(24,4)=10626 vars, millions of sunflower-triple constraints — heavy but feasible on ~100 cores over days per cell. Deliverable: an improved witness → new …[truncated; see source]
- applies: **witness local search** (predicate `lane-move`)

### `S:gap:20:993399dc` — A332077 cell Sun(3,4) — minimal number of distinct sets of cardinality <= 3 forcing a 4-sunflower (first uncomputed …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 39 (Abbott–Exoo, 'On Set Systems Not Containing Delta Systems', Graphs and Combinatorics 8 (1992): a 38-triple family with no 4-sunflower, so Sun(3,4) >= 39; as audited in hub deep record atlas/problems.json#20 (2026-07-13; no 2023–2026 improvement found).) · upper 163 (Erdős–Rado sunflower lemma bound as recorded in the OEIS A332077 formula section: Sun(m,n) <= (n-1)^m * m! + 1; (4-1)^3 * 3! + 1 = 163.)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `wall`
- witness object: A family of >= 39 distinct sets, each of cardinality <= 3, containing no 4 sets whose pairwise intersections are all equal (raises Sun(3,4) >= family size + 1)
- witness verifier: For N sets enumerate all C(N,4) 4-subsets (N=39: 82251) and check whether the six pairwise intersections coincide; core-bucketing reduces this further. Exact, dependency-free, sub-second.
- notes: Table state (OEIS example section): row m=1 and columns n<=2 trivial; row m=2 closed form (Abbott–Hanson–Sauer); Sun(3,3)=21 proven (Erdős–Rado; 20-set extremal family in the OEIS a-file); data stops exactly at Sun(3,4). Sibling open cells with 30–50-year-old construction records: Sun(4,3) >= 55 (Abbott–Hanson–Sauer 1972, 54-set family) and Sun(3,5) >= 89 (Abbott–Hanson 1974, 88 triples), both per hub deep audit atlas/problems.json#20. Witness search is SA-seeding + ILP/MaxSAT over a swept …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `combinatorics`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P20` and the surface id in your receipt so the next agent's graph build finds your result.


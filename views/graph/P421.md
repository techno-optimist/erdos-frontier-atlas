# Erdős #421 — attack card

> Erdős problem #421 · https://www.erdosproblems.com/421
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: d*(421) = supremum of asymptotic densities achievable by sequences 1 ≤ d_1 < d_2 < … with …[truncated; see source])** — HOT: erdosproblems.com/421 shows a claimed solution posted in its comments (unreviewed as of 2026-07-18, 43 comments, several users including Bloom and Alexeev mark it tractable) — the scout should re-check upstream status before investing. Greedy sequence A389544 (Sungkawichai, Jan 2026) has 20000 terms with a(20000) = 20163, prefix density ≈ 0.9919 and rising (complement A390848: 1, 6, 12, 16, …), consistent with the conjectured answer yes. Source: ErGr80 p.84. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/421.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is there a sequence $1 \le d_1 < d_2 < \dots$ with density 1 such that all products $\prod_{u \le i \le v} d_i$ are distinct?

## ATTACK SURFACES — which overlay said so

### `S:gap:421:9c8d6114` — d*(421) = supremum of asymptotic densities achievable by sequences 1 ≤ d_1 < d_2 < … with all contiguous products …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower 1/e - ε achievable for every ε > 0 (≈ 0.36788) (Selfridge construction, per erdosproblems.com/421 remarks (cf. problem 786)) · upper 1 (trivial ceiling; the conjecture is that it is attained) (trivial)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none for the density claim — density is asymptotic, so no finite prefix certifies it; distinctness of all contiguous products IS checkable on finite prefixes (hash the O(N^2) products), which validates the greedy data lane but cannot close the gap
- witness verifier: for prefix data only: recompute all contiguous products of a claimed prefix with bignum arithmetic and check for duplicates; O(N^2) products — feasible to N ~ 1e4-1e5
- notes: HOT: erdosproblems.com/421 shows a claimed solution posted in its comments (unreviewed as of 2026-07-18, 43 comments, several users including Bloom and Alexeev mark it tractable) — the scout should re-check upstream status before investing. Greedy sequence A389544 (Sungkawichai, Jan 2026) has 20000 terms with a(20000) = 20163, prefix density ≈ 0.9919 and rising (complement A390848: 1, 6, 12, 16, …), consistent with the conjectured answer yes. Source: ErGr80 p.84.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P421` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #141 — attack card

> Erdős problem #141 · https://www.erdosproblems.com/141
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A006560(7) — smallest starting prime of 7 consecutive primes in arithmetic progression …[truncated; see source])** — Only one CPAP-7 known; any smaller one is a genuine record with trivial verification. The exact minimum needs an infeasible ~1e21 exhaustive sweep. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/141.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $k≥3$. Are there $k$ consecutive primes in arithmetic progression?

## ATTACK SURFACES — which overlay said so

### `S:gap:141:682607f3` — A006560(7) — smallest starting prime of 7 consecutive primes in arithmetic progression (CPAP-7) (gap_map, kind `bounded_above_only`, confidence C3)

- bounds: lower — (—) · upper 71137654873189893604531 (Zimmermann existence witness (OEIS comments; primerecords.dk))
- witness side: `upper` · witness feasibility `hard` · exact feasibility `wall`
- witness object: a CPAP-7 with a smaller start: 7 consecutive primes in arithmetic progression
- witness verifier: primality of the 7 + consecutiveness (no intervening primes) + equal gaps (multiple of 210); milliseconds
- notes: Only one CPAP-7 known; any smaller one is a genuine record with trivial verification. The exact minimum needs an infeasible ~1e21 exhaustive sweep.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `additive combinatorics`, `primes`, `arithmetic progressions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P141` and the surface id in your receipt so the next agent's graph build finds your result.


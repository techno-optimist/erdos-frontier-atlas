# Erdős #506 — attack card

> Erdős problem #506 · https://www.erdosproblems.com/506
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: What is the minimum number of distinct circles determined by n points in R^2, not all on …[truncated; see source])** — This is the clearest instance in the batch of the trap the brief warns about: upstream's DECIDABLE tag means 'only finitely many n remain open' (n <= 393), NOT 'a finite check settles each n'. For each individual n the configuration space is a continuum modulo Mobius transformations, so the MINIMUM -- the thing actually asked for -- is not finitely checkable, and confirming a candidate minimum needs realisability decisions that are exists-R and want a CAS, which fails criterion 3 outright. The one genuinely cheap and exact piece is the upper-bound half (rational coordinates + exact 4x4 determinants), and there does appear to be a real gap there -- nobody has published a small-n table, and …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `decidable`, finite handle `decidable`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:triage:506` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: What is the minimum number of distinct circles determined by n points in R^2, not all on a circle (and not all on a line)? Tagged DECIDABLE ('resolved up to a finite check').
- witness: Two very different objects, and they must not be conflated. UPPER bound: a concrete n-point rational configuration whose triples fall into few concyclic classes -- exactly checkable (concyclicity of 4 points is a 4x4 determinant = 0 in exact rationals; the circle count is the number of equivalence classes of the C(n,3) triples). LOWER bound / the actual MINIMUM asked for: not a concrete finite object at all -- it is a universally quantified statement over a continuum of configurations at each n.
- smallest untested case: n = 8 is the first n the remarks explicitly flag as anomalous and unresolved: Segre observed that projecting a cube to the plane beats the naive binom(n-1,2) = 21 bound at n=8, but no exact value of the minimum is stated for n=8 or for any n in the open range. The open range is the whole of roughly 5 <= n <= 393.
- known frontier: Zero, as far as I can find. Elliott [El67] proved at least binom(n-1,2) circles for n > 393; Purdy and Smith found the error and corrected it to binom(n-1,2)+1-floor((n-1)/2) for n > 393, which is best possible (n-1 points on a circle plus one off it); Balint and Balintova [BaBa94] report the same. Segre's cube projection is the only small-n data point on the page. Searches for computed tables of the minimum at small n returned nothing -- the adjacent literature …[truncated; see source]
- search-space estimate: At n=8, abstracting to which 4-subsets are concyclic: 2^C(8,4) = 2^70 ~ 1.2x10^21 candidate concyclicity hypergraphs before any realisability filter; pruning to families of maximal concyclic sets pairwise meeting in <= 2 points cuts this a lot but leaves each survivor needing a decision of realisability in the plane, which is an existential-theory-of-the-reals problem requiring real quantifier …[truncated; see source]
- verification cheap and exact: no
- verdict reason: This is the clearest instance in the batch of the trap the brief warns about: upstream's DECIDABLE tag means 'only finitely many n remain open' (n <= 393), NOT 'a finite check settles each n'. For each individual n the configuration space is a continuum modulo Mobius transformations, so the MINIMUM -- the thing actually asked for -- is not finitely checkable, and confirming a candidate minimum needs realisability decisions that are exists-R and want a CAS, which fails criterion 3 outright. The one genuinely cheap and exact piece is the upper-bound half (rational coordinates + exact 4x4 determinants), and there does appear to be a real gap there -- nobody has published a small-n table, and …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P506` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #1016 — attack card

> Erdős problem #1016 · https://www.erdosproblems.com/1016
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:1016:e03342cc` — m(38) — minimum number of edges of a pancyclic graph on 38 vertices; first open cell after Griffin's exact table (hub …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 43 (4 chords admit at most 2^5 - 1 = 31 cycles (Shi, Discrete Math. 133, 1994) < 36 required lengths, so >= 5 chords, i.e. m(38) >= 38 + 5; argument as in Griffin, arXiv:1312.0274) · upper 44 (m(37) = 42 exact (Griffin, arXiv:1312.0274, Table 1) plus m(n+1) <= m(n) + 2 (Griffin, Prop. 2))
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: a pancyclic graph on 38 vertices with 43 edges (Hamiltonian cycle + 5 chords), shipped with one explicit cycle per length 3..38
- witness verifier: positive side is solver-free: check each of the 36 cycle certificates is a valid cycle of the stated length against the edge list, O(n) per cycle. Nonexistence side (m(38)=44): exhaustive 5-chord enumeration over chord-endpoint configurations up to dihedral symmetry (~10^8 configs), each checked …[truncated; see source]
- notes: A genuine two-value cell: m(38) is 43 or 44, decided either way by a bounded search (Griffin ran the analogous 5-chord exhaustion to 31 vertices on 2013 hardware; his Figure-1 construction with 5 chords tops out at exactly n=37, which is why the table stops there). Griffin 2013 gives m(n) exactly for all n <= 37: m(n) = n+5 for 25 <= n <= 37, agreeing with George–Marr–Wallis. OEIS A105206 is STALE — it ends at a(22) = 26 while exact values to n=37 have been published since 2013; the b-file …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `cycles`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1016` and the surface id in your receipt so the next agent's graph build finds your result.


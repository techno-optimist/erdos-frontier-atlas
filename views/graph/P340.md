# Erdős #340 — attack card

> Erdős problem #340 · https://www.erdosproblems.com/340
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/340.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $A = \{1, 2, 4, 8, 13, 21, 31, 45, 66, 81, 97, \ldots\}$ be the greedy Sidon sequence: we begin with $1$ and iteratively include the next smallest integer that preserves the Sidon property (i.e. there are no non-trivial solutions to $a + b = c + d$). What is the order of growth of $A$? Is it true that $|A \cap \{1, \ldots, N\}| \gg N^{1/2 - \varepsilon}$ for all $\varepsilon > 0$ and large $N$?

## ATTACK SURFACES — which overlay said so

### `S:gap:340:a2fc97b7` — Verification depth for '33 is never a difference of Mian-Chowla terms' (33 = A080200(1), the smallest integer not known …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 100000 (A. Reichert, OEIS A080200 comment: candidate non-differences (incl. 33) confirmed absent among all pairwise differences of the first 100000 Mian-Chowla terms; a(100000)=7135566931719) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `unknown`
- witness object: Either (a) indices i>j with a_i - a_j = 33, which settles membership positively and deletes A080200(1), or (b) a deeper verified frontier (first N > 100000 terms with 33 still absent)
- witness verifier: The greedy sequence is deterministic: recompute A005282 to index i with a pairwise-sum bitset (2*a(i) bits; ~1.8 TB at i=100000, so segmented/compressed methods needed) and check the claimed pair — no compact certificate exists, verification = independent recomputation with published code
- notes: Erdős-Graham asked about the growth of A (trivially >> N^(1/3), conjectured N^(1/2-eps): wall) and whether A-A has positive density / contains every integer; 22 was confirmed in A-A (a_15 - a_14 = 204-182). If 33 truly appears, a finite computation closes the first cell; nonmembership is not finitely certifiable — hence exact_feasibility unknown. A080200 currently lists 50 conjectured non-differences. Memory, not CPU, is the binding constraint on deeper verification. Second OEIS ref: A005282 …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `additive combinatorics`, `sidon sets`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P340` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #345 — attack card

> Erdős problem #345 · https://www.erdosproblems.com/345
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A001661(9) — largest number not a sum of distinct positive 9th powers)** — Wiener estimates ~200,000 core-months to finish 9th powers. No cheap certificate on either side; excluded from record hunting. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:345:43a00fdf` — A001661(9) — largest number not a sum of distinct positive 9th powers (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower > 1.55e17 (Wiener (OEIS comment)) · upper — (—)
- witness side: `lower` · witness feasibility `none` · exact feasibility `wall`
- witness object: a larger integer verified non-representable as a sum of distinct 9th powers
- witness verifier: none cheap — non-representability requires a full subset-sum reachability DP (petabyte-scale at 9th powers)
- notes: Wiener estimates ~200,000 core-months to finish 9th powers. No cheap certificate on either side; excluded from record hunting.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `complete sequences`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P345` and the surface id in your receipt so the next agent's graph build finds your result.


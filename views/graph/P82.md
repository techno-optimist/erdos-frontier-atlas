# Erdős #82 — attack card

> Erdős problem #82 · https://www.erdosproblems.com/82
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A394563(6) — least N such that every graph on N vertices has an induced regular subgraph …[truncated; see source])** — Witness verification is trivial; beating the bound means out-searching McKay's 2026 computation. Exact value needs a universal upper-bound proof — wall. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/82.lean)

## STATEMENT (Apache-licensed, Lean-derived)

$F(n) / \log n \to \infty as n \to \infty$

## ATTACK SURFACES — which overlay said so

### `S:gap:82:bd982946` — A394563(6) — least N such that every graph on N vertices has an induced regular subgraph of order ≥ 6 (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower 21 (Dyson–McKay (arXiv:2604.08215)) · upper — (—)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: a graph on N ≥ 21 vertices with no induced regular subgraph of order ≥ 6 (⇒ a(6) ≥ N+1)
- witness verifier: enumerate all vertex subsets of size ≥ 6; check none induces a degree-regular graph (~2^N subsets, cheap at N ≈ 21)
- notes: Witness verification is trivial; beating the bound means out-searching McKay's 2026 computation. Exact value needs a universal upper-bound proof — wall.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P82` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #1084 — attack card

> Erdős problem #1084 · https://www.erdosproblems.com/1084
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: contact-number problem f_d(n) = max pairs at distance exactly 1 among n points pairwise …[truncated; see source])** — Open content is d >= 3 asymptotics, a constants gap not a table: Erdős claimed c_1,c_2 > 0 with 6n - c_1 n^{2/3} < f_3(n) < 6n - c_2 n^{2/3}; best proven upper bound f_3(n) < 6n - 0.926 n^{2/3} for n >= 2 (Bezdek–Reid 2013); general d: (d-o(1))n <= f_d(n) <= 2^{O(d)} n (lattice lower bound, kissing-number upper bound); survey: Bezdek–Khan 2018 (contact numbers for sphere packings). A potential future records lane: exact maximum contact numbers c(n) for small n in R^3 (sticky-sphere clusters) — …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research solved, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/1084.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:1084:e26232cd` — contact-number problem f_d(n) = max pairs at distance exactly 1 among n points pairwise >= 1 apart; linked sequence …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none against the linked ledger — A045945 is a closed-form (keyword: easy) sequence with no uncomputed cell; the planar case of the problem is fully closed: Harborth 1974 proved f_2(n) = floor(3n - sqrt(12n-3)) (matching Erdős's triangular-lattice speculation f_2(3n^2+3n+1) = 9n^2+3n)
- witness verifier: n/a for the linked sequence; a d=3 lane witness (n unit-sphere centers with many touching pairs) would verify by exact/interval arithmetic: all pairwise distances >= 1, count of == 1 pairs — but no OEIS ledger for that exists here
- notes: Open content is d >= 3 asymptotics, a constants gap not a table: Erdős claimed c_1,c_2 > 0 with 6n - c_1 n^{2/3} < f_3(n) < 6n - c_2 n^{2/3}; best proven upper bound f_3(n) < 6n - 0.926 n^{2/3} for n >= 2 (Bezdek–Reid 2013); general d: (d-o(1))n <= f_d(n) <= 2^{O(d)} n (lattice lower bound, kissing-number upper bound); survey: Bezdek–Khan 2018 (contact numbers for sphere packings). A potential future records lane: exact maximum contact numbers c(n) for small n in R^3 (sticky-sphere clusters) — …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`, `distances`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1084` and the surface id in your receipt so the next agent's graph build finds your result.


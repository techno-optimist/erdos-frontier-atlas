# Erdős #376 — attack card

> Erdős problem #376 · https://www.erdosproblems.com/376
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/376.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Are there infinitely many $n$ such that ${2n\choose n}$ is coprime to $105$?

## ATTACK SURFACES — which overlay said so

### `S:gap:376:cd31a00f` — A030979(1375) — least k > 10^70 with binomial(2k,k) coprime to 105 (equivalently: all base-3 digits <= 1, base-5 digits …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 1e70 (Christopher E. Thompson's complete enumeration to 10^70 (OEIS A030979 b-file, 1374 terms; extends Alekseyev's first 62 terms)) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `unknown`
- witness object: any k > 10^70 with base-3 digits in {0,1}, base-5 digits in {0,1,2}, base-7 digits in {0,1,2,3} (Kummer/Lucas: no carries in k+k in each base) — first new term since 2015, upper-bounds a(1375)
- witness verifier: three base conversions + digit-range checks with any bignum library — microseconds; no factorial or binomial computation needed
- notes: Graham's $1000 prize question is INFINITUDE of the sequence — that side is a wall (EGRS 1975 settles any two odd primes; three primes open; Bloom-Croot [BlCr25]: almost-all-digits version for large primes). Pomerance heuristic: ~x^0.02595 terms up to x, so new terms beyond 10^70 exist but are extremely sparse; naive digit-DFS explodes (2^147 base-3 strings to 10^70) — needs Thompson-style multi-base constraint propagation, hence plausible-not-easy, and exact-cell status beyond 10^70 is unproven …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `binomial coefficients`, `base representations`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P376` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #945 — attack card

> Erdős problem #945 · https://www.erdosproblems.com/945
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/945.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is it true that $F(x) \leq (\log x)^{O(1)}$?

## ATTACK SURFACES — which overlay said so

### `S:gap:945:80086f25` — A048892(19) — smallest start of 19 consecutive integers with pairwise distinct numbers of divisors (first uncomputed …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 239919791836864 (= A048892(18) (Martin Ehrenstein, OEIS ext Aug 2023) + monotonicity: any 19-run start is an 18-run start, so a(19) ≥ a(18)) · upper — (—)
- witness side: `upper` · witness feasibility `hard` · exact feasibility `cell`
- witness object: an integer n such that tau(n), tau(n+1), ..., tau(n+18) are pairwise distinct (any such n upper-bounds a(19); no 19-run is currently known)
- witness verifier: factor 19 integers (~1e14–1e17 scale, Pollard rho ms each), compute divisor counts, check 19 distinct values — seconds, dependency-free
- notes: a(16)=3.47e11, a(17)=2.62e12, a(18)=2.40e14 — extrapolation suggests a(19) ~ 1e16, i.e. Ehrenstein-class dedicated sieving (weeks), hence graded hard despite the trivially cheap verifier. Underlying asymptotics (Erdős–Mirsky 1952): (log x)^{1/2}/loglog x ≪ F(x) ≪ exp(O((log x)^{1/2}/loglog x)); erdosproblems #945 credits Beker with upper F(x) ≪ exp(O((log x)^{1/3+o(1)})) — primary source NOT located on arXiv (2026-07-18 search), treat as site-remark attribution; Cambie: Cramér ⇒ F(x) ≪ (log …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `divisors`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P945` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #385 — attack card

> Erdős problem #385 · https://www.erdosproblems.com/385
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Largest member of A322293 = {k : A322292(k) <= k}, where A322292(k) = max over composites …[truncated; see source])** — A322292(k) is trivially <= k + sqrt(k); Erdős–Eggleton–Selfridge suggest exceptional k are finite and that the max may be >= k + (1−o(1))·sqrt(k) [Er79d p.73; ErGr80 p.74]. A322293 has exactly 100 known terms, complete below 10^8; largest 267680 (pair 267672/267680 after a long gap from 8742 — thinning consistent with finiteness). Bloom's page: equivalent to problem #430 (Adenwalla observation); Tao has a blog discussion; Tao marks it difficult. Record = any new exceptional k in (10^8, ∞): a …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/385.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:385:6652c624` — Largest member of A322293 = {k : A322292(k) <= k}, where A322292(k) = max over composites c < k of (c + lpf(c)) — the …[truncated; see source] (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower 267680 (OEIS A322293 b-file (Michel Marcus / Robert Israel; 100 terms, exhaustive below 10^8)) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `wall`
- witness object: An integer k > 267680 (necessarily with k−1 prime) such that every composite c < k satisfies c + lpf(c) <= k
- witness verifier: Only c in (k−sqrt(k), k) matter since lpf(c) <= sqrt(c): for each j = 2..ceil(sqrt(k)) with k−j composite, trial-divide k−j by primes <= j and require a factor; confirm k−1 prime (BPSW). O(sqrt(k)·pi(sqrt(k))) work — seconds at k ~ 1e12. Dependency-free.
- notes: A322292(k) is trivially <= k + sqrt(k); Erdős–Eggleton–Selfridge suggest exceptional k are finite and that the max may be >= k + (1−o(1))·sqrt(k) [Er79d p.73; ErGr80 p.74]. A322293 has exactly 100 known terms, complete below 10^8; largest 267680 (pair 267672/267680 after a long gap from 8742 — thinning consistent with finiteness). Bloom's page: equivalent to problem #430 (Adenwalla observation); Tao has a blog discussion; Tao marks it difficult. Record = any new exceptional k in (10^8, ∞): a …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P385` and the surface id in your receipt so the next agent's graph build finds your result.


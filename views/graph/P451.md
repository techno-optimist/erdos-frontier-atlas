# Erdős #451 — attack card

> Erdős problem #451 · https://www.erdosproblems.com/451
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:451:e37b6c64` — A386620(184) = n_184 — smallest k > 368 such that (k-1)(k-2)...(k-184) has no prime factor in (184, 368); first …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: An integer k > 368 such that for each of the 31 primes p in (184,368), k mod p is NOT in {1,...,184} (equivalently k mod p is 0 or in {185 mod p,...,p-1}); any such k upper-bounds the cell, and a sieve exhaustion below k pins the exact value.
- witness verifier: Candidate check is 31 modular reductions (primes 191..367) — trivially dependency-free and instant. Exactness additionally requires certifying no smaller k > 368 works: a residue-sieve scan up to k (heavy: a(183) = 167981058401851841 ~ 1.7e17, so a(184) likely needs a large wheeled sieve, but it is …[truncated; see source]
- notes: The open PROBLEM is the asymptotic estimate of n_k, which is a wall for machines: van Doorn-Tang (arXiv:2606.19863, June 2026, in OEIS formula section) prove n_k > exp(c (log k)^2/loglog k); trivial upper n_k < e^{O(k)} (PNT / product of primes in (k,2k), Adenwalla observation on erdosproblems.com); Erdos conjectured k^d-beating growth but e^{o(k)}. The RECORD lane is table extension: b-file has exactly 183 terms (Kalogeropoulos 1-70, Chai Wah Wu 71-92, Greathouse 93-183). Because each …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).
- external claim (ai_claims, **HOT** — verification target): Erdős Problem #451 — blocks avoiding middle-sized prime factors — our verdict: “not independently checked”. A claim never sets a status (standing rule 1).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P451` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #1137 — attack card

> Erdős problem #1137 · https://www.erdosproblems.com/1137
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Does max_{n<x} d_n·d_{n-1} / (max_{n<x} d_n)^2 -> 0 as x -> infinity, where d_n = p_{n+1} …[truncated; see source])** — From Vaughan's problem collection attribution [Va99, 1.2] per erdosproblems.com/1137 (page carries no partial results — the remarks section is empty, which is itself signal: nothing to be scooped by). Data context: A005250 (record prime gaps) b-file now has 85 maximal gaps, largest listed 1854 (b-file extended by Kehrig, fetched and read this session — exhaustive frontier has moved past the long-standing 2^64 era); A083550 = d_n·d_{n-1} has a 10000-term b-file. Cramér-type heuristics make …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/1137.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $d_n=p_{n+1}-p_n$, where $p_n$ denotes the $n$th prime. Is it true that $$\frac{\max_{n < x}d_{n}d_{n-1}}{(\max_{n < x}d_n)^2}\to 0$$ as $x\to \infty$?

## ATTACK SURFACES — which overlay said so

### `S:gap:1137:113c01cd` — Does max_{n<x} d_n·d_{n-1} / (max_{n<x} d_n)^2 -> 0 as x -> infinity, where d_n = p_{n+1} - p_n (are two consecutive …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: No witness moves the quantity: it is a limit as x -> infinity, and any finite prime-gap configuration (however striking a pair of consecutive large gaps) is consistent with both answers. No nontrivial proven bound on the ratio was found to record.
- witness verifier: None for the limit. (A claimed record product of consecutive gaps d_n·d_{n-1} is itself cheaply verifiable — certify primality of p_{n-1}, p_n, p_{n+1}, p_{n+2} and compositeness of everything between — but it bounds nothing here.)
- notes: From Vaughan's problem collection attribution [Va99, 1.2] per erdosproblems.com/1137 (page carries no partial results — the remarks section is empty, which is itself signal: nothing to be scooped by). Data context: A005250 (record prime gaps) b-file now has 85 maximal gaps, largest listed 1854 (b-file extended by Kehrig, fetched and read this session — exhaustive frontier has moved past the long-standing 2^64 era); A083550 = d_n·d_{n-1} has a 10000-term b-file. Cramér-type heuristics make …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1137` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #393 — attack card

> Erdős problem #393 · https://www.erdosproblems.com/393
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:393:387be1a1` — A388302(80) — least spread b_t − b_1 over factorizations 80! = b_1···b_t into t >= 2 distinct increasing integer …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper 78 (Trivial construction 80! = 2·3···80 (t = n−1, spread n−2), per A388302 comment (D. A. Corneth))
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: A factorization 80! = b_1···b_t into >= 2 distinct increasing integers with spread b_t − b_1 < 78; heuristically factors cluster near (80!)^{1/t} (Liou geometric-mean window)
- witness verifier: Multiply the b_i with big-int arithmetic, compare to 80!, check strict increase. Milliseconds, dependency-free. (Certifying the cell exactly additionally needs an exhaustive sweep over smaller spreads.)
- notes: f(n) = A388302(n). Headline open question: is f(n) = 1 infinitely often — f(n)=1 ⟺ n! = m(m+1) ⟺ 4·n!+1 is a perfect square; known only n = 2, 3 (Brocard-adjacent). Berend–Osgood 1992: #{n<=N : f(n)=m} = o(N) for each fixed m; Bui–Pratt–Zaharescu 2023: ≪_m N^{33/34}; Luca 2002: ABC ⇒ f(n) → ∞. Existing table n = 2..79 by Corneth (PARI program + tuple list linked from OEIS); a(n) <= min(A061057(n), n−2). Record: extend the certified table from a(80) onward — candidate discovery via the …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `factorials`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P393` and the surface id in your receipt so the next agent's graph build finds your result.


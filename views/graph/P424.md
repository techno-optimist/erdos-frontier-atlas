# Erdős #424 — attack card

> Erdős problem #424 · https://www.erdosproblems.com/424
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Lower asymptotic density of the Hofstadter product-minus-one closure {2,3} -> a_i*a_j - 1 …[truncated; see source])** — erdosproblems.com marks this 'cannot be resolved with a finite computation'. Known structure: no integer == 1 (mod 3) ever appears (residues stay in {0,2} mod 3), so upper density <= 2/3; the original 'almost all integers' phrasing in ErGr80/Guy E31 is trivially false and the intended question (Er77c) is positive lower density. OEIS keyword 'nice,easy' refers to term generation, not the problem. A possible scout instrument (non-record): compute counting function c(x)/x to large x from the …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/424.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $a_1 = 2$ and $a_2 = 3$ and continue the sequence by appending to $a_1, \ldots, a_n$ all possible values of $a_i a_j - 1$ with $i \neq j$. Is it true that the set of integers which eventually appear has positive density?

## ATTACK SURFACES — which overlay said so

### `S:gap:424:0a75c773` — Lower asymptotic density of the Hofstadter product-minus-one closure {2,3} -> a_i*a_j - 1 (A005244); conjectured …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None: no finite object certifies positive lower density. Counting-function data (b-file, 10000 terms) gives empirical density estimates only.
- witness verifier: No cheap witness exists: density is an asymptotic property; any finite prefix of the sequence is consistent with both answers. Membership of an individual integer IS cheaply verifiable (BFS over products a_i*a_j-1 below the target), but that moves nothing.
- notes: erdosproblems.com marks this 'cannot be resolved with a finite computation'. Known structure: no integer == 1 (mod 3) ever appears (residues stay in {0,2} mod 3), so upper density <= 2/3; the original 'almost all integers' phrasing in ErGr80/Guy E31 is trivially false and the intended question (Er77c) is positive lower density. OEIS keyword 'nice,easy' refers to term generation, not the problem. A possible scout instrument (non-record): compute counting function c(x)/x to large x from the …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P424` and the surface id in your receipt so the next agent's graph build finds your result.


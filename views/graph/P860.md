# Erdős #860 — attack card

> Erdős problem #860 · https://www.erdosproblems.com/860
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: h(n) — smallest L such that EVERY interval (m, m+L] contains distinct integers …[truncated; see source])** — The bounded quantity is an asymptotic order gap (linear-times-unbounded vs n^{3/2}), so the records lane has nothing to move — wall. The linked OEIS anchors are the NON-distinct analogues (Jacobsthal of primorials A048670, known to k=64; run-length version A058989 to n=64): they lower-bound nothing here directly since h demands distinct representatives. Librarian spawn opportunity: a small exact table of h(p_k) appears to be absent from OEIS entirely — computable for k up to ~9 by scanning m …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:860:e88bfced` — h(n) — smallest L such that EVERY interval (m, m+L] contains distinct integers a_1..a_pi(n) with p_i | a_i …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower (3-o(1))·n, and h(n)/n -> infinity (Erdős–Selfridge (the 3n bound) and Ruzsa (superlinearity), as reported at erdosproblems.com/860 (Er92c/ErPo80 context)) · upper O(n^{3/2}/(log n)^{1/2}) (Erdős–Pomerance 1980 [ErPo80], per erdosproblems.com/860 (Guy B32))
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none for the asymptotic gap (n^1 vs n^{3/2} orders — no finite computation moves either exponent); per fixed n, a Hall-violating residue m mod n# does certify h(n) > L, which would only feed a finite table
- witness verifier: for fixed n, m, L: build the bipartite graph primes-p_i-vs-multiples in (m, m+L], run Hopcroft–Karp; absence of a perfect matching is a poly-time-checkable certificate that h(n) > L — dependency-free
- notes: The bounded quantity is an asymptotic order gap (linear-times-unbounded vs n^{3/2}), so the records lane has nothing to move — wall. The linked OEIS anchors are the NON-distinct analogues (Jacobsthal of primorials A048670, known to k=64; run-length version A058989 to n=64): they lower-bound nothing here directly since h demands distinct representatives. Librarian spawn opportunity: a small exact table of h(p_k) appears to be absent from OEIS entirely — computable for k up to ~9 by scanning m …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #687 shares A048670, A058989 → card `views/graph/P687.md`
- #854 shares A048670 → card `views/graph/P854.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P860` and the surface id in your receipt so the next agent's graph build finds your result.


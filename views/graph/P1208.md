# Erdős #1208 — attack card

> Erdős problem #1208 · https://www.erdosproblems.com/1208
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:1208:2d8863fb` — A193838(15) — smallest k such that 15 points of the k x k lattice grid have pairwise distinct mutual distances (the …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 18 (A271490(17) = 13 < 15 (Cariboni, OEIS A271490 extension, Jul 2022): no 15-point distinct-distance subset fits in a 17x17 grid; equivalently monotonicity from the exact value A193838(14) = 18 (Cariboni 2022)) · upper 20 (Explicit 15-point distinct-distance configuration in a 20x20 grid (Cariboni, OEIS A193838 comment, Jul 2022; improves the Al Zimmermann Point Packing contest-derived bound of 21 reported by Kamenetsky, Apr 2021))
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: 15 lattice points (x_i, y_i), 0 <= x_i, y_i <= k-1 with k in {18, 19}, whose C(15,2) = 105 squared distances (integers) are pairwise distinct
- witness verifier: 5-line dependency-free Python: compute the 105 integer squared distances, assert len(set(...)) == 105 and all coordinates within the grid. Instant. A k=19 witness sets A193838(15) <= 19; a k=18 witness also lifts A271490(18) to >= 15.
- notes: Erdos problem 1208 proper (Er57b; Er80 p.110) asks for the asymptotics of F_d(n) = largest subset with pairwise distinct distances guaranteed in ANY n points in R^d — not finitely resolvable. Known (paraphrased from erdosproblems.com/1208 remarks): n^{1/3}/(log n)^{1/3} << F_2(n) << n^{1/2}/(log n)^{1/4} (lower Charalambides 2013 [Ch13]; upper from the integer grid, which has << n/sqrt(log n) distinct distances); for d >= 3, Thiele 1995 gave F_d(n) >> n^{1/(3d-2)}, improved by …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`, `distances`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1208` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #668 — attack card

> Erdős problem #668 · https://www.erdosproblems.com/668
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:668:90bfb31a` — A385657(22) — number of nonisomorphic maximally dense unit-distance graphs on 22 vertices (first uncomputed cell of the …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `cell`
- witness object: No cheap standalone witness: a lower bound on the count requires exhibiting configurations AND certifying they attain the maximum unit-distance count A186705(22), which is itself unknown (only bounded above by Alexeev–Mixon–Parshall's 2025 revision).
- witness verifier: None dependency-free: checking 'achieves E unit distances' is trivial (exact coordinate arithmetic), but 'E is maximal for n=22' needs an exhaustive certification of A186705(22) in the style of Alexeev–Mixon–Parshall (combinatorial enumeration + realizability), which is expert tooling, not a cheap …[truncated; see source]
- notes: Erdős 668 asks whether the number of incongruent n-point sets maximizing unit distances tends to infinity, and whether it exceeds 1 for all n > 3 (it equals 1 at n=4). Known counts n=1..21: 1,1,1,1,1,4,1,3,1,1,2,1,1,2,1,1,7,16,3,1,5 (A385657, Weisstein 2025) — caveat from the problem page: computations distinguish graph-isomorphism classes, not congruence classes. Companion A186705 (problem 90's max unit distances) is exact to a(21)=57 with AMP upper bounds for a(22)-a(30) (e.g. best upper for …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`, `distances`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P668` and the surface id in your receipt so the next agent's graph build finds your result.


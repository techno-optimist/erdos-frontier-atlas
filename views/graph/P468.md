# Erdős #468 — attack card

> Erdős problem #468 · https://www.erdosproblems.com/468
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Sub-conjecture record for the f(N)=o(N) question: A167485(N) > 0 (every N is an …[truncated; see source])** — The actual Erdos question — is f(N) = o(N), where f(N) = A167485(N+1)-indexed minimal m — is asymptotic (wall). Data points: liminf a(n)/n = 0 is PROVEN (Israel: a(sigma(n)) <= n and sigma(n)/n unbounded, OEIS comment 2024); but the ratio spikes — max a(n)/n = 11.84 at n=17567 within the b-file (computed this session), so o(N) is far from empirically settled. Companion sequences (Kovac, Aug 2025): A387502 = number of debut sums per n (record indices …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:468:b1af6273` — Sub-conjecture record for the f(N)=o(N) question: A167485(N) > 0 (every N is an initial-segment divisor sum of some m) …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 20000 (OEIS A167485 b-file (Robert Israel, Maple, Sep 2024); zeros scanned this session: only N=2,5 in 0..20000. Israel's a(n)/n plot extends to 1e6 but the underlying values are not published as a b-file, so 20000 is the honest verified figure) · upper — (—)
- witness side: `both` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: Lower/record witness: recomputation certificate of a(N) (the minimal m per N) to larger N with no new zeros. Closing witness: a new zero — an N with NO m whose initial divisor sums hit N — which would refute the Goldbach-flavored strengthening noted in OEIS (every even integer > 22 = p + q with 5 < p < q < 5p) and blow up f(N)=o(N).
- witness verifier: Per-N positive check: exhibit m and verify the running sums d_1 < d_1+d_2 < ... of m's sorted divisors hit N (trivial). Zero-certification is a finite case analysis over admissible initial divisor chains summing to N (each chain's realizability as the exact bottom of a divisor lattice is a finite …[truncated; see source]
- notes: The actual Erdos question — is f(N) = o(N), where f(N) = A167485(N+1)-indexed minimal m — is asymptotic (wall). Data points: liminf a(n)/n = 0 is PROVEN (Israel: a(sigma(n)) <= n and sigma(n)/n unbounded, OEIS comment 2024); but the ratio spikes — max a(n)/n = 11.84 at n=17567 within the b-file (computed this session), so o(N) is far from empirically settled. Companion sequences (Kovac, Aug 2025): A387502 = number of debut sums per n (record indices …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #1054 shares A167485 → card `views/graph/P1054.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P468` and the surface id in your receipt so the next agent's graph build finds your result.


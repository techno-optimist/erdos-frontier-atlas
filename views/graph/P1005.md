# Erdős #1005 — attack card

> Erdős problem #1005 · https://www.erdosproblems.com/1005
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:1005:0ebbba9b` — A386893(101) — first OEIS-uncomputed value of f(n), the Mayer–Erdős run-length: largest m such that any two Farey …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 2 (f(n) >= (n/12)(1 - 4/n^(1/3)) — van Doorn, arXiv:2509.00121 (via OEIS formula section)) · upper 30 (f(n) < n/4 + 5 for all n >= 4 — van Doorn, arXiv:2509.00121 (via OEIS formula section))
- witness side: `both` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: for claimed value v: the violating pair of Farey fractions at index distance v+1 (with (a_k - a_l)(b_k - b_l) < 0) plus a full sliding-window scan of F_n confirming no violation at distance <= v
- witness verifier: generate F_n by the mediant/next-term recurrence (integer-only), scan windowed pairs for the similarly-ordered predicate; O(|F_n| * f(n)) with |F_n| ~ 3n^2/pi^2. Verifier validated in this mining pass: reproduces a(4)..a(60) from the b-file exactly and computes a(101) in milliseconds
- notes: MINER RESULT: a(101) = 27, computed and double-checked here, matching van Doorn's conjectured exact formula f(n) = floor(n/4) + d, d = (1,2,2,4) for n = (0,1,2,3) mod 4, n >= 92. The real lane is bulk: extend the table to n ~ 10^4-10^5 hunting a counterexample to the exact formula (any mismatch is publishable and would kill the conjectured c = 1/4 form), plus a free OEIS b-file extension beyond n=100. Headline asymptotic gap (proof-only): (1/12 - o(1))n <= f(n) <= n/4 + O(1) (van Doorn 2025; …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1005` and the surface id in your receipt so the next agent's graph build finds your result.


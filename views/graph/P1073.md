# Erdős #1073 — attack card

> Erdős problem #1073 · https://www.erdosproblems.com/1073
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A(x) = number of composite u <= x with u | n!+1 for some n — conjecture A(x) <= x^{o(1)})** — Erdős question from discussions with Hardy and Subbarao (Guy UPINT A2). Enumeration state (verified from b-file): 719 composites known, largest a(719) = 99795337, i.e. A(~1e8) = 719 — consistent with very slow (x^~0.36 at this range) growth but of course probing nothing asymptotic. Wilson's theorem makes the prime analogue trivial, which is why only composite u count. Extending the enumeration is open-easy compute (sieve over u, scan n < u) but is data-gathering, not a record. Stub statement …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/1073.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is it true that $A(x) \le x^{o(1)}$?

## ATTACK SURFACES — which overlay said so

### `S:gap:1073:cad087d8` — A(x) = number of composite u <= x with u | n!+1 for some n — conjecture A(x) <= x^{o(1)} (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none for the conjecture — x^{o(1)} is asymptotic with no finite threshold; new terms of A256519 (a composite u with witness exponent n such that u | n!+1) are cheaply verifiable but move no bound
- witness verifier: n/a for the conjecture; per-term check is trivial (compute (u-k)! mod u by modular product, confirm == -1)
- notes: Erdős question from discussions with Hardy and Subbarao (Guy UPINT A2). Enumeration state (verified from b-file): 719 composites known, largest a(719) = 99795337, i.e. A(~1e8) = 719 — consistent with very slow (x^~0.36 at this range) growth but of course probing nothing asymptotic. Wilson's theorem makes the prime analogue trivial, which is why only composite u count. Extending the enumeration is open-easy compute (sieve over u, scan n < u) but is data-gathering, not a record. Stub statement …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1073` and the surface id in your receipt so the next agent's graph build finds your result.


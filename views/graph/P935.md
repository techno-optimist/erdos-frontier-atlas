# Erdős #935 — attack card

> Erdős problem #935 · https://www.erdosproblems.com/935
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: E_2 = limsup_n log_n Q_2(n(n+1)(n+2)) — powerful-part exponent over 3 consecutive …[truncated; see source])** — Erdős's main question asserts E_2 = 2 (Q_2 < n^{2+ε} eventually for every ε), which he called very difficult; ABC implies the weaker Q_2 = o(n^{ℓ+1}) (question 3) per [Fe26] (all per erdosproblems.com/935, paraphrased; page last edited 2026-02-08). Question 2 (limsup Q_2/n² = ∞ for ℓ≥2) is RESOLVED affirmatively via the x²−8y²=1 Pell family (van Doorn / [Fe26], essentially #367). For ℓ=1 the exponent question is closed trivially: Q_2(n(n+1)) ≤ n(n+1) and consecutive powerful pairs (A060355) …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:935:dad24fdf` — E_2 = limsup_n log_n Q_2(n(n+1)(n+2)) — powerful-part exponent over 3 consecutive integers (ℓ=2 case of #935; Q_2 = …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower 2 (Pell-family construction (van Doorn; also given in [Fe26]) makes Q_2(n(n+1)(n+2))/n² unbounded, per erdosproblems.com/935; Mahler already gives limsup ratio ≥ 1 for every ℓ) · upper 3 (trivial: Q_2(n(n+1)(n+2)) ≤ n(n+1)(n+2) ~ n³ (derived here))
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: an explicit infinite family (polynomial identity / Pell-type recurrence) certifying Q_2(n(n+1)(n+2)) ≥ n^{2+δ} for fixed δ>0 infinitely often — this would refute Erdős's conjecture E_2 = 2. Single large-exponent n's are cheap evidence but do not move the limsup
- witness verifier: factor n, n+1, n+2 (ECM at moderate sizes; for a parametric family verify the identity symbolically), take the product of prime powers with exponent ≥ 2, compare log Q_2/log n against 2+δ
- notes: Erdős's main question asserts E_2 = 2 (Q_2 < n^{2+ε} eventually for every ε), which he called very difficult; ABC implies the weaker Q_2 = o(n^{ℓ+1}) (question 3) per [Fe26] (all per erdosproblems.com/935, paraphrased; page last edited 2026-02-08). Question 2 (limsup Q_2/n² = ∞ for ℓ≥2) is RESOLVED affirmatively via the x²−8y²=1 Pell family (van Doorn / [Fe26], essentially #367). For ℓ=1 the exponent question is closed trivially: Q_2(n(n+1)) ≤ n(n+1) and consecutive powerful pairs (A060355) …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #367 shares A057521 → card `views/graph/P367.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P935` and the surface id in your receipt so the next agent's graph build finds your result.


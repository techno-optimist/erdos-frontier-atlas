# Erdős #43 — attack card

> Disjoint-difference Sidon set pairs · https://www.erdosproblems.com/43
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (problem)** — problem DISPROVED (both questions resolved negatively); no movable number [atlas/problems.json wall_reason]

## STATUS — three registers, never merged

- ours: **solved-upstream** (atlas/stubs.json)
- upstream: **Solved** (raw `disproved`)
- formal: not in formal-conjectures
- prize: $100 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:deep:43` — deep audit: **WALL**, lane `wall`, board **NONE** (problems.json)

- finite object: Pairs of Sidon sets in {1,...,N} with disjoint nonzero difference sets; the finite quantity is max of C(|A|,2)+C(|B|,2) at fixed small N.
- current record: Problem DISPROVED. Q1: negative, follows from the resolution of Erdős #42. Q2: negative, Barreto construction gives |A|=|B| Sidon pairs with disjoint difference sets and C(|A|,2)+C(|B|,2) ≥ (1-o(1))C(f(N),2); Tao supplied the matching upper bound (no -c). Both parts settled ~2022-2025; $100 effectively claimed. No small-N record table for disjoint pairs exists.
- verifier: Poly-time exact: verify each set is Sidon (all C(n,2) pairwise differences distinct, O(n^2)); verify (A-A)∩(B-B)={0} by hashing difference multisets and intersecting, O(n^2). Cheap. But no target record to certify against.
- attack: N/A — no movable frontier. In principle one could ILP/SAT-search max C(|A|,2)+C(|B|,2) over disjoint Sidon pairs for small N (~minutes/core up to N~few hundred), but there is no published record to beat and the asymptotic question is closed, so any witness is a non-contribution.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).
- external claim (ai_claims, recorded): Erdős Problem #43 — paired Sidon sets — our verdict: “not independently checked”. A claim never sets a status (standing rule 1).

## BRIDGES — structurally adjacent problems

- #14 shares A143824 → card `views/graph/P14.md`
- #30 shares A003022, A143824, A227590 → card `views/graph/P30.md`
- #155 shares A003022, A143824, A227590 → card `views/graph/P155.md`
- #530 shares A143824 → card `views/graph/P530.md`
- #861 shares A003022, A143824, A227590 → card `views/graph/P861.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P43` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #1060 — attack card

> Erdős problem #1060 · https://www.erdosproblems.com/1060
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/1060.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:1060:14634be7` — A212490(6) - least m such that k*sigma(k) = m has exactly 6 solutions (the record ladder for f(n) = A327153, the count …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower > 5*10^14 (Michel Marcus & David A. Corneth, OEIS A212490 comment; independently consistent with a local exhaustive sieve over k <= 10^7 (complete for m <= 10^14) finding max multiplicity 5, and exactly reproducing a(5) = 1584858562560 with k = {624960, 640080, 696384, 708660, 713232}) · upper 7089671638182002688000 (Daniel Suteu, OEIS A212490 comment (constructed from products of perfect numbers times powers of 2))
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: An integer m < 7089671638182002688000 with six listed k_1..k_6 each satisfying k_i*sigma(k_i) = m, plus an exhaustiveness certificate (sieve of all k <= sqrt(m)) showing no seventh solution - 'at least 6' alone still records if paired with the smallest-known-exactly-6 bookkeeping
- witness verifier: At-least-6: factor each k_i, compute sigma, multiply - trivial. Exactly-6: bounded sieve of k*sigma(k) for k <= sqrt(m) (~8.4e10 at the current upper bound; blocked divisor sieve + disk-bucketed collision count, heavy but bounded and replayable).
- notes: Erdos problem 1060 proper (Guy B11) asks whether f(n) = #{k : k*sigma(k) = n} is <= n^(o(1/loglog n)), maybe (log n)^O(1) - analytic wall. The machine lane is A212490: known a(1)..a(5) = 6, 336, 333312, 5418319872, 1584858562560 (each ~ products of perfect numbers times 2-powers); every new cell is also evidence that f is unbounded, itself unproven. Adjacent cells are open too, some with SMALLER upper bounds than a(6): Suteu gives a(7) <= 106345074572730040320 (~1.06e20) and bounds for a(8), …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1060` and the surface id in your receipt so the next agent's graph build finds your result.


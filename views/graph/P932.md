# Erdős #932 — attack card

> Erdős problem #932 · https://www.erdosproblems.com/932
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A387864 verified frontier — largest verified r such that at least two integers strictly …[truncated; see source])** — Erdős [Er76d] believed it true but with density-0 r (he proved density 0 even for one such n); empirically abundant regardless — 10^4 terms below 6.4e6. Tao is among those flagging the problem 'difficult' on the page (fetched 2026-07-18). Lean-formalized (research open). Sequence created Sep 2025 (Radcliffe); frontier extension is a cheap standing lane but cannot close the problem. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/932.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $p_k$ denote the $k$th prime. For infinitely many $r$ there are at least two integers $p_r < n < p_{r+1}$ all of whose prime factors are $< p_{r + 1} - p_r$.

## ATTACK SURFACES — which overlay said so

### `S:gap:932:15f82732` — A387864 verified frontier — largest verified r such that at least two integers strictly between p_r and p_{r+1} are …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 6386830 (OEIS A387864 b-file, exhaustive first 10000 terms (Chai Wah Wu)) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: new terms r > 6386830: exhibit the prime pair (p_r, p_{r+1}) and ≥2 gap integers with complete factorizations into primes < p_{r+1}−p_r (extends the verified range; evidence only — infinitude is the open content)
- witness verifier: compute p_r, p_{r+1} (sieve; p_r ≈ 1.2e8 at the frontier); for each n in the gap, trial-divide by primes below the gap length and confirm full factorization; seconds, dependency-free
- notes: Erdős [Er76d] believed it true but with density-0 r (he proved density 0 even for one such n); empirically abundant regardless — 10^4 terms below 6.4e6. Tao is among those flagging the problem 'difficult' on the page (fetched 2026-07-18). Lean-formalized (research open). Sequence created Sep 2025 (Radcliffe); frontier extension is a cheap standing lane but cannot close the problem.
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P932` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #218 — attack card

> Erdős problem #218 · https://www.erdosproblems.com/218
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Densities/infinitude of consecutive-prime-gap patterns: {n : d_{n+1} >= d_n} and {n : …[truncated; see source])** — The run version (d_n = d_{n+1} = ... = d_{n+k} solvable for all k) is equivalent to k consecutive primes in AP — that record lane lives at hub #141 (already seeded, CPAP records). Banks (2023) gives a heuristic for the density-1/2 statement conditional on a quantitative prime-tuples conjecture (paraphrased from erdosproblems.com/218, page edited Jan 2026). Empirical density tracking is possible but bounds nothing. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/218.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:218:a9658204` — Densities/infinitude of consecutive-prime-gap patterns: {n : d_{n+1} >= d_n} and {n : d_{n+1} <= d_n} each conjectured …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — density and infinitude claims; no finite example moves them
- witness verifier: n/a — the problem page marks this as not resolvable by finite computation; A064113/A333230/A333231 are bookkeeping sequences (b-files 1000/10000 terms, keyword easy) with no record frontier
- notes: The run version (d_n = d_{n+1} = ... = d_{n+k} solvable for all k) is equivalent to k consecutive primes in AP — that record lane lives at hub #141 (already seeded, CPAP records). Banks (2023) gives a heuristic for the density-1/2 statement conditional on a quantitative prime-tuples conjecture (paraphrased from erdosproblems.com/218, page edited Jan 2026). Empirical density tracking is possible but bounds nothing.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P218` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #580 — attack card

> Erdős problem #580 · https://www.erdosproblems.com/580
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Erdős–Füredi–Loebl–Sós (the (n/2 - n/2 - n/2) conjecture): if G on n vertices has at …[truncated; see source])** — This is the cleanest illustration in the batch of why the upstream state label is not a promise. "DECIDABLE / resolved up to a finite check" is literally true, but the finite check is bounded above by a Szemerédi-Regularity-Lemma n0 that Zhao never makes explicit, and bounded below in cost by the number of graphs on n vertices, which passes 1e23 by n = 20. Criterion 3 is the only one that passes — tree containment is exact and cheap. Criteria 2 and 4 fail catastrophically and criterion 1's witness, while concrete, lives in a space nobody can enumerate. Filing as WALL, with a note for the atlas: #580 is worth carrying as a documented example of an upstream "decidable" tag whose finite handle …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `decidable`, finite handle `decidable`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:triage:580` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: Erdős–Füredi–Loebl–Sós (the (n/2 - n/2 - n/2) conjecture): if G on n vertices has at least n/2 vertices of degree at least n/2, must G contain every tree on at most n/2 vertices? Zhao [Zh11] proved it for all sufficiently large n, which is why upstream tags it decidable.
- witness: CONCRETE. A graph G on n vertices with at least ceil(n/2) vertices of degree at least ceil(n/2), together with a tree T on at most floor(n/2) vertices that is not a subgraph of G. Non-containment is an exact integer subgraph-isomorphism check; there are only A000055 trees to test (106 trees on 10 vertices, 551 on 12), so the per-candidate cost is trivial.
- smallest untested case: Formally n = 12 (need >= 6 vertices of degree >= 6, test all 6 trees on <= 6 vertices), rising through n = 14, 16, ... — but this is meaningless: the check must run up to Zhao's n0, and n0 is not a number anyone has written down.
- known frontier: None found — no explicit small-n computer verification is reported anywhere I could locate. The "finite check" comes from Yi Zhao, "Proof of the (n/2 - n/2 - n/2) Conjecture for large n", Electron. J. Combin. 18 (2011) #P27. I pulled the paper (math.gsu.edu/yzhao6/n2_Conj.pdf) and read the proof structure directly: Theorem 1.6 states only "There is a threshold n0 such that Conjecture 1.3 holds for all n >= n0" — no numeric value. Section 4 "contains the Regularity Lemma and some properties of …[truncated; see source]
- search-space estimate: Two independent blowups. (a) The threshold: n0 is regularity-lemma-derived and has no stated numeric value, so the upper end of the "finite" range is unknown and plausibly tower-scale. (b) The per-n cost: OEIS A000088 gives 1.65e11 graphs on 12 vertices, 1.21e13 on 14, and 6.45e23 on 20 — so even if n0 were miraculously as small as 20, the check at that single n alone is 1e23 graphs, eleven …[truncated; see source]
- verification: True
- verdict reason: This is the cleanest illustration in the batch of why the upstream state label is not a promise. "DECIDABLE / resolved up to a finite check" is literally true, but the finite check is bounded above by a Szemerédi-Regularity-Lemma n0 that Zhao never makes explicit, and bounded below in cost by the number of graphs on n vertices, which passes 1e23 by n = 20. Criterion 3 is the only one that passes — tree containment is exact and cheap. Criteria 2 and 4 fail catastrophically and criterion 1's witness, while concrete, lives in a space nobody can enumerate. Filing as WALL, with a note for the atlas: #580 is worth carrying as a documented example of an upstream "decidable" tag whose finite handle …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P580` and the surface id in your receipt so the next agent's graph build finds your result.


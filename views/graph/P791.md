# Erdős #791 — attack card

> Erdős problem #791 · https://www.erdosproblems.com/791
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:791:ccded5ca` — A066063(51) — minimal size of S ⊆ {0..51} with S+S ⊇ {0..51} (finite additive 2-basis; first uncomputed cell, table + …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 12 (a(50)=12 (OEIS A066063 b-file) + trivial monotonicity (elements > t never appear in a sum equal to t, so an (n+1)-cover restricts to an n-cover)) · upper 13 (trivial: 0 is forced into every cover, so appending 51 to an optimal cover of {0..50} gives a(51) <= a(50)+1 = 13)
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: an explicit S ⊆ {0..51}, |S| = 12, with S+S ⊇ {0..51} (settles a(51)=12); min-type quantity, so the smaller cover is the witness — any extended verified row of A066063 is a record
- witness verifier: O(|S|^2) sumset check: mark all a+b for a,b in S, assert 0..51 all marked — dependency-free; exactness of a new cell additionally needs the (tiny, n=51) exhaustive/ILP no-11-cover certificate
- notes: The Erdős/Rohrbach question itself is asymptotic: g(n) ~ 2 sqrt(n) was disproved (Mrose 1979, g(n)^2 <= 3.5n); current frontier g(n)^2/n in [2.181 (Yu 2015), 294/85 = 3.4588 (Kohonen 2017, arXiv:1606.04770-line of work)] — all paraphrased from problem page and cross-checked against arXiv search (no post-2017 h=2 improvement found; arXiv:2507.23627 (2025) improves large-h postage-stamp, not h=2). Beating Kohonen's construction constant would be a genuine record but is expert-search-defeated …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `additive combinatorics`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P791` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #416 — attack card

> Erdős problem #416 · https://www.erdosproblems.com/416
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Whether V(2x)/V(x) → 2 and whether an asymptotic formula exists for V(x) = #{n ≤ x : φ(m) …[truncated; see source])** — Order of magnitude essentially settled by proof, not computation: Pillai (1929) V(x) = o(x); Erdős (1935) V(x) = x(log x)^(-1+o(1)); Maier–Pomerance (1988) V(x) = (x/log x)·e^((C+o(1))(logloglog x)^2); Ford (1998) sharp two-sided ≍ with explicit constants C1, C2, C3 — just short of an asymptotic formula, which is exactly what remains open. Guy B36. No witness lane: excluded from the records hunt. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/416.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:416:7dd01bdf` — Whether V(2x)/V(x) → 2 and whether an asymptotic formula exists for V(x) = #{n ≤ x : φ(m) = n solvable} (the totient …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — the open content is a limit/asymptotic statement; erdosproblems.com marks it not resolvable by finite computation
- witness verifier: none exists; finite tabulations of V (A264810, b-file to 1e4) are trivially recomputable but certify nothing about the limit
- notes: Order of magnitude essentially settled by proof, not computation: Pillai (1929) V(x) = o(x); Erdős (1935) V(x) = x(log x)^(-1+o(1)); Maier–Pomerance (1988) V(x) = (x/log x)·e^((C+o(1))(logloglog x)^2); Ford (1998) sharp two-sided ≍ with explicit constants C1, C2, C3 — just short of an asymptotic formula, which is exactly what remains open. Guy B36. No witness lane: excluded from the records hunt.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #417 shares A264810 → card `views/graph/P417.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P416` and the surface id in your receipt so the next agent's graph build finds your result.


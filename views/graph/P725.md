# Erdős #725 — attack card

> Erdős problem #725 · https://www.erdosproblems.com/725
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A001009 row-12 diagonal = number of reduced 12x12 Latin squares (= A000315(12)) — first …[truncated; see source])** — Erdos's actual #725 question is an asymptotic formula for the number of k x n Latin rectangles: Erdos-Kaplansky 1946 proved ~ e^{-binom(k,2)}·(n!)^k for k = o((log n)^{3/2-eps}); Yamamoto 1951 extended to k <= n^{1/3-o(1)}; the full range is open and not machine-witnessable (I did not exhaustively survey post-1951 range extensions — treat the range frontier as unverified). The machine-shaped frontier is the enumeration table: A000315 has 11 terms (order 11 by McKay-Wanless, 2005), order 12 …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:725:96080235` — A001009 row-12 diagonal = number of reduced 12x12 Latin squares (= A000315(12)) — first uncomputed cell; order 11 = …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None — an exact count admits no cheap certificate; values come from heavy trusted enumeration/algebraic methods (McKay-Wanless style), not from a verifiable witness object.
- witness verifier: None known: verifying a claimed count essentially requires an independent re-enumeration; no succinct certificate format exists for Latin-square counts.
- notes: Erdos's actual #725 question is an asymptotic formula for the number of k x n Latin rectangles: Erdos-Kaplansky 1946 proved ~ e^{-binom(k,2)}·(n!)^k for k = o((log n)^{3/2-eps}); Yamamoto 1951 extended to k <= n^{1/3-o(1)}; the full range is open and not machine-witnessable (I did not exhaustively survey post-1951 range extensions — treat the range frontier as unverified). The machine-shaped frontier is the enumeration table: A000315 has 11 terms (order 11 by McKay-Wanless, 2005), order 12 …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `combinatorics`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P725` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #1062 — attack card

> Erdős problem #1062 · https://www.erdosproblems.com/1062
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/1062.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:1062:5946d20b` — A038372(45) - largest subset of {1,...,45} in which no element divides two other distinct elements (fork-free sets) (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 30 (= a(44) = 30 (OEIS A038372 data, 44 terms); monotone since any valid subset for n is valid for n+1) · upper 31 (= a(44) + 1: deleting 45 from an optimal subset of [1..45] leaves a valid subset of [1..44], so a(45) <= a(44) + 1)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: A subset S of {1..45} with |S| = 31 such that no a in S divides two distinct other elements of S (pins a(45) = 31); if the exhaustive/ILP search proves none exists, a(45) = 30 - either way the cell closes
- witness verifier: For each a in S count multiples of a among S \ {a}; require count <= 1. O(|S|^2) integer divisibility checks, dependency-free, instant.
- notes: a(45) is boxed into {30, 31}; a 45-variable ILP/SAT solves it in seconds, and the whole table (data stops at n=44, no b-file) extends by dozens of exact cells in hours - among the cheapest genuine table records in the hub. The underlying Erdos problem (Guy B24): how large is f(n), and is lim f(n)/n irrational? Interval (n/3, n] gives f(n) >= ceil(2n/3); Lebensold 1976: 0.6725 n <= f(n) <= 0.6736 n for large n (stated as a theorem in formal-conjectures 1062.lean and on the problem page) - …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1062` and the surface id in your receipt so the next agent's graph build finds your result.


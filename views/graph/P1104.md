# Erdős #1104 — attack card

> Erdős problem #1104 · https://www.erdosproblems.com/1104
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research solved, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/1104.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:1104:77267858` — A292528(6) — minimum number of vertices in a triangle-free graph with chromatic number 6 (first unknown cell of the …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower 32 (Goedgebeur, On minimal triangle-free 6-chromatic graphs, J. Graph Theory (arXiv:1707.07581); OEIS A292528 comment) · upper 40 (Goedgebeur, same paper — explicit triangle-free 6-chromatic graphs on 40 vertices (House of Graphs meta-directory))
- witness side: `upper` · witness feasibility `hard` · exact feasibility `unknown`
- witness object: Adjacency list of a graph on n <= 39 vertices that is triangle-free and has no proper 5-coloring (min-type quantity: a smaller example lowers the upper bound).
- witness verifier: Triangle-freeness: check all O(n^3) triples (trivial). Chromatic number >= 6: encode 5-colorability as SAT over 5n vars and get UNSAT (seconds at n<=40 with any solver; DRAT-certifiable). Fully dependency-free alternative: exhaustive 5-coloring backtracking, still fast at this size.
- notes: Bounds 32 <= a(6) <= 40 have stood since 2017; OEIS entry is actively curated (edits Jan 2026 by Greathouse added asymptotics without touching them), and 2026 web check found nothing newer. Witness graded hard because the 33..39-vertex window was already attacked by the domain expert with dedicated triangle-free generators (triangle-free Ramsey-style enumeration); a lucky heuristic/tabu search hit is conceivable but ungraded optimism is banned. Lower-bound side (33) needs exhaustive …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #1013 shares A292528 → card `views/graph/P1013.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1104` and the surface id in your receipt so the next agent's graph build finds your result.


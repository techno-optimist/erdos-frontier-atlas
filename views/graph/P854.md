# Erdős #854 — attack card

> Erdős problem #854 · https://www.erdosproblems.com/854
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:854:c322ba75` — A389839(13) — smallest even integer that is not a difference of consecutive integers coprime to 41# = 304250263527210 …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower a(12) = 68 is the last computed cell (for 37#) (OEIS A389839 b-file (a(2)..a(12) = 6,8,12,16,20,28,32,42,48,60,68; keyword 'more')) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: occurrence certificates: for each even t < v, a pair of CONSECUTIVE 41#-coprime integers differing by t — proving a(13) >= v+2 cheaply; pinning a(13) exactly requires a full-period absence scan (the entry's symmetry remark halves the work)
- witness verifier: per certificate (x, x+t): gcd(x, 41#) = gcd(x+t, 41#) = 1 and every x+1..x+t-1 shares a factor with 41# — O(t) word-sized gcds each; the exhaustive side is a wheel-based segmented scan of one period, ~4.4·10^13 coprime gaps
- notes: Directly quantifies Erdős' #854: he first guessed every even t up to the maximal gap occurs, then reported doubts after Lacampagne–Selfridge computations showed failure at 13# = 30030 (paraphrased from erdosproblems.com/854, Ob1) — visible in the data as a(6)=20 missing while the Jacobsthal max gap A048670(6)=22 occurs. The max-gap side is far ahead of the spectrum side: A048670 known through k=64 (a(64)=1110; Bozek 2021, Google-Cloud-scale). a(13) is a genuine frontier cell: period 3.04·10^14, …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #687 shares A048670 → card `views/graph/P687.md`
- #860 shares A048670 → card `views/graph/P860.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P854` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #128 — attack card

> Erdős Sparse Half Conjecture (triangle-free) · https://www.erdosproblems.com/128
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **TRAP** — prize **$250** + upstream says `falsifiable`, but our status is **wall**: the finite handle does not help at our scale (atlas/walls.md, finite-handle table). The prize is the lure; this line is the hook.
- **WALL (problem)** — verifier is NP-hard (Sparsest-k-Subgraph); meaningful regime is flag-algebra SDP blowups, not finite witnesses [atlas/problems.json wall_reason]
- **WALL (surface: Erdős–Rousseau: if G on n vertices has the property that every induced subgraph on >= …[truncated; see source])** — PRIOR ATLAS WALL CONFIRMED — and for a stronger reason than headcount. Criterion 2 fails (7.5e15 ops at the first out-of-reach size) and criterion 3 fails: certifying a witness needs the exact MINIMUM number of edges over all floor(n/2)-subsets, which is the minimum-edge-k-subgraph problem — NP-hard, so an exact certificate needs an ILP/branch-and-bound, not a dependency-free checker, for any n past ~34. The killer is structural, not computational: the extremal family hits n^2/50 exactly at every n, so a counterexample must strictly beat a tight family in the regime where Razborov's flag-algebra bound already squeezes the constant to [0.02, 0.02637]. Nothing an enumerable graph can do …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **wall** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/128.lean)
- prize: $250 — prizes are history, not targets (standing rule 2).

## STATEMENT (Apache-licensed, Lean-derived)

Let G be a graph with n vertices such that every subgraph on ≥ $n/2$ vertices has more than $n^2/50$ edges. Must G contain a triangle?

## ATTACK SURFACES — which overlay said so

### `S:triage:128` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: Erdős–Rousseau: if G on n vertices has the property that every induced subgraph on >= floor(n/2) vertices spans more than n^2/50 edges, must G contain a triangle? Equivalently (Razborov's phrasing): every triangle-free n-vertex graph has an induced subgraph on n/2 vertices with at most n^2/50 edges.
- witness: CONCRETE but expensive to certify. A triangle-free graph G on n vertices such that min over all S with |S| = floor(n/2) of e(G[S]) is strictly greater than n^2/50. The witness is an adjacency matrix; the certificate that it IS a witness requires a proven MINIMUM over all half-subsets.
- smallest untested case: There is no meaningful smallest case: the problem's content is asymptotic. The extremal families meet the bound EXACTLY at every n, not just in the limit. For C5[t] on n = 5t vertices, taking two non-adjacent classes whole (2t vertices) plus t/2 vertices from a class adjacent to exactly one of them costs (t/2)*t = t^2/2 = n^2/50 edges — exactly the threshold, and the hypothesis demands strictly more. So the whole small-n regime is pinned to the bound and no perturbation of a blow-up can win. …[truncated; see source]
- known frontier: No exhaustive computational search found in the literature. The frontier is analytic: Erdős–Faudree–Rousseau–Schelp [EFRS94] proved it with 50 replaced by 16; Krivelevich [Kr95] with n/2 replaced by 3n/5 and 50 by 25; Keevash–Sudakov [KeSu06] under e(G) <= n^2/12 or e(G) >= n^2/5; Norin–Yepremyan [NoYe15] for e(G) >= (1/5 - c)n^2; Razborov [Ra22], by flag algebras, proved it with 1/50 replaced by 27/1024 ≈ 0.026367, and proved the full conjecture for girth-5 graphs, graphs of bounded …[truncated; see source]
- search-space estimate: Triangle-free graphs on n vertices (OEIS A006785): n=14 -> 4.68e8, n=15 -> 1.42e10, n=16 -> 5.81e11, n=17 -> 3.17e13. Verifying one candidate costs C(n, floor(n/2)) subset evaluations: C(16,8) = 12,870. A full n=16 sweep = 5.81e11 * 1.29e4 ≈ 7.5e15 primitive ops — about 10^4 times over budget. And the only n small enough to sweep (n <= 14, ≈1.6e12 ops) sit in the regime where n^2/50 < 4 and the …[truncated; see source]
- verification: False
- verdict reason: PRIOR ATLAS WALL CONFIRMED — and for a stronger reason than headcount. Criterion 2 fails (7.5e15 ops at the first out-of-reach size) and criterion 3 fails: certifying a witness needs the exact MINIMUM number of edges over all floor(n/2)-subsets, which is the minimum-edge-k-subgraph problem — NP-hard, so an exact certificate needs an ILP/branch-and-bound, not a dependency-free checker, for any n past ~34. The killer is structural, not computational: the extremal family hits n^2/50 exactly at every n, so a counterexample must strictly beat a tight family in the regime where Razborov's flag-algebra bound already squeezes the constant to [0.02, 0.02637]. Nothing an enumerable graph can do …[truncated; see source]
- no move applies mechanically

### `S:deep:128` — deep audit: **WALL**, lane `wall`, board **NONE** (problems.json)

- finite object: A putative counterexample: a single finite triangle-free graph on n vertices whose SPARSEST ⌊n/2⌋-vertex induced subgraph has strictly > n²/50 edges.
- current record: Theorem side: Razborov (2021, "More about sparse halves in triangle-free graphs", flag-algebra/SDP) proved every triangle-free graph has a half with ≤ (27/1024)n² ≈ 0.02637 n² edges. Erdős-conjectured constant 1/50 = 0.02 n². Proven exactly in special cases: girth ≥5, independence number ≥2n/5 (Krivelevich), strongly regular graphs, and min-degree > 10n/29 / ≥5n/14 (Keevash–Sudakov and successors). Extremal tight examples: uniform blowup of C₅ and blowup of the Petersen graph, each hitting exactly n²/50 in the sparsest half. $250 prize.
- verifier: Exact sparsest-half check = minimize induced edge count over all ⌊n/2⌋-subsets (NP-hard Sparsest-k-Subgraph). Cost: ILP/SAT feasible only for n≲40–60 per candidate; the meaningful regime is asymptotic blowups where the "verifier" becomes a fractional quadratic minimization over vertex weights (flag-algebra SDP).
- attack: If one insisted: enumerate triangle-free graphs / weighted triangle-free templates on ≤15–20 vertices, and for each optimize vertex blow-up weights to maximize the min fractional half density, seeking any template with min-half > 1/50 (LP/SDP inner problem per template, ILP for the discrete min-half). Rough compute: hours-to-days on ~100 cores. Expected outcome: NEGATIVE — reproduces C₅/Petersen at exactly 1/50 and Razborov's 27/1024 certified ceiling; no counterexample. Not worth farm time.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P128` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #168 — attack card

> Erdős problem #168 · https://www.erdosproblems.com/168
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: The Graham-Spencer-Witsenhausen limit: lim_n (largest weakly-triple-free subset of the …[truncated; see source])** — Remaining open content is qualitative: is the constant irrational (open per A386439 comment), plus density/upper-density analogues for infinite sets (erdosproblems.com/168, page dated 2026-03-23). The finite tables A057561/A094708/A004059 are generated by an efficient algorithm (Eberhard's SageMath script), so there is no expensive frontier cell. Not a records-lane target. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/168.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:168:4d3c21dc` — The Graham-Spencer-Witsenhausen limit: lim_n (largest weakly-triple-free subset of the first n 3-smooth numbers)/n = …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None — GSW 1977 proved the limit exists and equals (1/3)*sum_{k in K} 1/d_k over 3-smooth numbers d_k, so the constant is machine-computable to arbitrary precision (Eberhard: 35 digits in A386439; A004059 b-file to 3335 terms). No gap remains to move with an example.
- witness verifier: n/a — recomputing digits of a convergent explicit series is verification of known work, not a record.
- notes: Remaining open content is qualitative: is the constant irrational (open per A386439 comment), plus density/upper-density analogues for infinite sets (erdosproblems.com/168, page dated 2026-03-23). The finite tables A057561/A094708/A004059 are generated by an efficient algorithm (Eberhard's SageMath script), so there is no expensive frontier cell. Not a records-lane target.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `additive combinatorics`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P168` and the surface id in your receipt so the next agent's graph build finds your result.


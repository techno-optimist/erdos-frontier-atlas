# Erdős #10 — attack card

> Erdős problem #10 · https://www.erdosproblems.com/10
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: K* = least k (if it exists) such that every integer n >= 2 is the sum of a prime and at …[truncated; see source])** — Existence of any finite K* is the open problem: Erdős called it 'probably unattackable'; Erdős–Graham (1980) lean toward no such k existing, so K* may be infinite (hence exact_feasibility=wall — only the lower bound is movable). Granville–Soundararajan (1998) conjecture 3 powers suffice for all odd n (hence 4 for even); Grechuk's even witness plus the abundance of A006286 suggests infinitely many even n need >= 4. Gallagher (1975): for any eps>0 some k(eps) covers a set of lower density >= …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/10.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is there some $k$ such that every integer is the sum of a prime and at most $k$ powers of $2$?

## ATTACK SURFACES — which overlay said so

### `S:gap:10:b1586594` — K* = least k (if it exists) such that every integer n >= 2 is the sum of a prime and at most k powers of 2; …[truncated; see source] (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower 4 (B. Grechuk's observation recorded on erdosproblems.com/10: 1117175146 is not the sum of a prime and at most 3 powers of 2. Recomputed and confirmed locally 2026-07-18 (all sums of <=3 powers of 2 below n, repeats allowed, n-s composite in every case; n itself composite).) · upper — (—)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: An integer n with no representation n = p + (sum of at most 4 powers of 2) — would raise the lower bound to K* >= 5
- witness verifier: Enumerate all sums s of at most 4 powers of 2 with s < n (about C(log2(n)+4,4) multisets — allow repeats to be convention-safe); deterministically primality-test n - s (BPSW/Miller–Rabin, proven deterministic below 3.3e24). All composite => verified. For n ~ 1e10 this is under 1e6 primality tests: …[truncated; see source]
- notes: Existence of any finite K* is the open problem: Erdős called it 'probably unattackable'; Erdős–Graham (1980) lean toward no such k existing, so K* may be infinite (hence exact_feasibility=wall — only the lower bound is movable). Granville–Soundararajan (1998) conjecture 3 powers suffice for all odd n (hence 4 for even); Grechuk's even witness plus the abundance of A006286 suggests infinitely many even n need >= 4. Gallagher (1975): for any eps>0 some k(eps) covers a set of lower density >= …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `additive basis`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P10` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #91 — attack card

> Erdős problem #91 · https://www.erdosproblems.com/91
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Number of pairwise non-similar n-point configurations achieving the minimum number of …[truncated; see source])** — erdosproblems.com/91 marks this not resolvable by finite computation. Known small-n structure (paraphrased): n=3 equilateral triangle unique; n=4 has two non-similar minimizers (square; two glued equilateral triangles); n=5 regular pentagon unique — published proof by Kovacs 2024 (settling Erdos's 'colleague from Zagreb' anecdote); Erdos (1987) claimed >= 2 non-similar examples for 6 <= n <= 9. The page notes unincorporated comment claims of partial results/path to solution — worth re-checking …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/91.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Suppose $A\subset \mathbb{R}^2$ has $\lvert A\rvert=n$ and minimises the number of distinct distances between points in $A$. Prove that for large $n$ there are at least two (and probably many) such $A$ which are non-similar.

## ATTACK SURFACES — which overlay said so

### `S:gap:91:dda63d25` — Number of pairwise non-similar n-point configurations achieving the minimum number of distinct distances (question: >= …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None settling the problem: the claim is for all sufficiently large n. A finite witness for one n (two non-similar optimal sets) needs the exact optimum A186704(n) plus an optimality certificate for both configurations — the certificate side is an open real-geometry enumeration, not a cheap check, and known only for small n.
- witness verifier: No cheap verifier: verifying a configuration MINIMIZES distinct distances requires ruling out all other n-point configurations (real-algebraic search), unlike counting distances in a given set (which is easy but insufficient).
- notes: erdosproblems.com/91 marks this not resolvable by finite computation. Known small-n structure (paraphrased): n=3 equilateral triangle unique; n=4 has two non-similar minimizers (square; two glued equilateral triangles); n=5 regular pentagon unique — published proof by Kovacs 2024 (settling Erdos's 'colleague from Zagreb' anecdote); Erdos (1987) claimed >= 2 non-similar examples for 6 <= n <= 9. The page notes unincorporated comment claims of partial results/path to solution — worth re-checking …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #89 shares A186704 → card `views/graph/P89.md`
- #1083 shares A186704 → card `views/graph/P1083.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P91` and the surface id in your receipt so the next agent's graph build finds your result.


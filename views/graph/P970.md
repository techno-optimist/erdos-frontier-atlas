# Erdős #970 — attack card

> Erdős problem #970 · https://www.erdosproblems.com/970
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:970:b0beb988` — A048670(65) — Jacobsthal function g of the product of the first 65 primes (first uncomputed primorial cell; the …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 1110 (= A048670(64) (Andrzej Bozek, computation supported by Google Cloud) + monotonicity: any run sharing factors with p_64# also shares factors with p_65#) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: an explicit u (or CRT residue certificate) such that each of u+1, ..., u+L shares a prime factor ≤ p_65 = 313 with the primorial, with L+1 > best known lower bound for a(65)
- witness verifier: L gcd computations against p_65#, or per-offset covering-prime list — trivially checkable, dependency-free
- notes: Exact a(62)–a(64) required Google-Cloud-scale search (Bozek 2021); expert tooling is Ziller–Morack arXiv:1611.03310 + Ziller arXiv:1903.11973 — a(65) exact is bounded-exhaustive but heavy; good greedy/covering lower-bound runs are a hours-to-days heuristic-search game. CAVEAT for the Erdős quantity: Hajdu–Saradha (Math. Comp. 81, 2012) disproved Jacobsthal's conjecture that primorials maximize — at k=24 some 24-prime-factor n has g = 236 > a(24) = 234 — so A048670 lower-bounds h(k) but is not …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P970` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #730 — attack card

> Erdős problem #730 · https://www.erdosproblems.com/730
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Largest gap k with a known pair (n, n+k) such that binomial(2n,n) and …[truncated; see source])** — EGRS 1975 said there is 'no doubt' infinitely many pairs exist — that infinitude question is not machine-resolvable. The recordable sub-question (stated open on the problem page): does a pair of shape (n,n+k) exist for every k>=1? Known: k=1 (43 terms listed in A129515, b-file to 10000 terms, all with m=n+1) and k=2 (via the 10003..10005 triple, confirmed here: all three share an identical 1540-prime set). k>=3 is unclaimed. Search cost per n is low but hit-density is unknown (gap-1 pairs are …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/730.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Are there infinitely many pairs of integers $n < m$ such that $\binom{2n}{n}$ and $\binom{2m}{m}$ have the same set of prime divisors?

## ATTACK SURFACES — which overlay said so

### `S:gap:730:81231280` — Largest gap k with a known pair (n, n+k) such that binomial(2n,n) and binomial(2(n+k),n+k) have identical prime-divisor …[truncated; see source] (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower 2 (triple (10003,10004,10005) cited in erdosproblems.com/730 remarks; re-verified this session by direct Kummer-carry computation (pair (87,88) also re-verified)) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `wall`
- witness object: a pair (n, n+k) with k >= 3 whose central binomial coefficients have identical prime-divisor sets; any new pair also grows the empirical base for the EGRS infinitude conjecture
- witness verifier: Kummer's theorem: p | C(2n,n) iff adding n+n in base p carries; compute both prime sets over primes <= 2(n+k) and compare — pure-Python, seconds per candidate, dependency-free
- notes: EGRS 1975 said there is 'no doubt' infinitely many pairs exist — that infinitude question is not machine-resolvable. The recordable sub-question (stated open on the problem page): does a pair of shape (n,n+k) exist for every k>=1? Known: k=1 (43 terms listed in A129515, b-file to 10000 terms, all with m=n+1) and k=2 (via the 10003..10005 triple, confirmed here: all three share an identical 1540-prime set). k>=3 is unclaimed. Search cost per n is low but hit-density is unknown (gap-1 pairs are …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `binomial coefficients`, `base representations`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P730` and the surface id in your receipt so the next agent's graph build finds your result.


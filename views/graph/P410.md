# Erdős #410 — attack card

> Erdős problem #410 · https://www.erdosproblems.com/410
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/410.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $σ_1(n) = σ(n)$, the sum of divisors function, and $σ_k(n) = σ(σ_{k-1}(n))$. Is it true that $\lim_{k → ∞} σ_k(n)^{\frac 1 k} = ∞$? This is problem (iii) from Erdos, Granville, Pomerance, Spiro "On the normal behavior of the iterates of some arithmetical functions" (page 169 of the book "Analytic Number Theory", 1990).

## ATTACK SURFACES — which overlay said so

### `S:gap:410:5cc363c7` — A007497(1501) — next term of the iterated sum-of-divisors trajectory 2, σ(2), σ(σ(2)), … underlying the question …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 1500 terms known; a(1500) has 1393 digits (OEIS A007497 b-file (C. R. Greathouse IV; first 200 terms T. D. Noe)) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: a(1501) = σ(a(1500)) together with the complete prime factorization of a(1500) (prime powers + primality certificates for each prime)
- witness verifier: Re-multiply the claimed prime powers and check the product equals the b-file value of a(1500); check each prime with Pratt/ECPP certificates; compute σ = Π (p^(k+1)-1)/(p-1) in bignum arithmetic and compare to the claimed a(1501). Dependency-free, seconds.
- notes: Underlying problem (Erdős–Granville–Pomerance–Spiro 1990, problem (iii) p.169; Guy B9) is explicitly not resolvable by finite computation — the trajectory extension is the machine-record lane, feeding growth heuristics (Cloitre's conjectured (1/2)log n < a(n+1)/a(n) < 2 log n; divisibility patterns checked to n=1000 per OEIS). Factor-chain propagation (σ splits over prime powers of the previous term) keeps most steps in ECM range, but a single hard 150+ digit cofactor (e.g. p+1 for a large …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #412 shares A007497 → card `views/graph/P412.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P410` and the surface id in your receipt so the next agent's graph build finds your result.


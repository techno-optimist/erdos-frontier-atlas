# Erdős #912 — attack card

> Erdős problem #912 · https://www.erdosproblems.com/912
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: h(n) = A071626(n) — number of distinct exponents in the prime factorization of n!; …[truncated; see source])** — Erdős–Selfridge proved h(n) ≍ (n/log n)^{1/2} [Er82c]. Tao's Cramér-model heuristic (Aug 2025 forum comment, referenced from OEIS A071626 links) predicts c = √(2π) ≈ 2.5066 (per erdosproblems.com/912, paraphrased). DISCREPANCY FLAG: the hub stub carries formal-conjectures formal_status 'research solved', but erdosproblems.com/912 showed OPEN when fetched 2026-07-18 — treat as open; the Lean file plausibly formalizes the solved ≍ statement rather than the ~ conjecture. B-file to 10^4 (Corneth) …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research solved, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/912.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Prove that there exists some $c>0$ such that $$h(n) \sim c \left(\frac{n}{\log n}\right)^{1/2}$$ as $n\to \infty$.

## ATTACK SURFACES — which overlay said so

### `S:gap:912:70ff7f6b` — h(n) = A071626(n) — number of distinct exponents in the prime factorization of n!; existence of c>0 with h(n) ~ …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — the open content is the existence of the asymptotic constant; h(n) itself is cheap to compute (equals #{floor(n/p) : p ≤ n prime}, Heyman–Miraj) but numerics cannot certify an asymptotic
- witness verifier: n/a — any single h(n) value is verifiable in seconds via Legendre's formula, but that moves nothing
- notes: Erdős–Selfridge proved h(n) ≍ (n/log n)^{1/2} [Er82c]. Tao's Cramér-model heuristic (Aug 2025 forum comment, referenced from OEIS A071626 links) predicts c = √(2π) ≈ 2.5066 (per erdosproblems.com/912, paraphrased). DISCREPANCY FLAG: the hub stub carries formal-conjectures formal_status 'research solved', but erdosproblems.com/912 showed OPEN when fetched 2026-07-18 — treat as open; the Lean file plausibly formalizes the solved ≍ statement rather than the ~ conjecture. B-file to 10^4 (Corneth) …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `factorials`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P912` and the surface id in your receipt so the next agent's graph build finds your result.


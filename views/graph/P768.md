# Erdős #768 — attack card

> Erdős problem #768 · https://www.erdosproblems.com/768
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Leading constant c in the density asymptotic |A ∩ [1,N]|/N = exp(-(c+o(1)) sqrt(log N) …[truncated; see source])** — FRESHNESS FLAG: Eric Li, 'The Sylow Divisor Condition: a Resolution of Erdős Problem 768', arXiv:2606.24872 (2026, 20pp), claims a full resolution with c = 1/(2 sqrt(log 2)); the paper is linked from OEIS A352287 and a claimed solution is posted in the problem-page comments, but upstream still marks the problem OPEN — librarian should track and close if it holds up. Erdős's own bounds: density >= exp(-c sqrt(log N) loglog N) and <= exp(-(1+o(1)) sqrt(log N loglog N)) (paraphrased from problem …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:768:17609730` — Leading constant c in the density asymptotic |A ∩ [1,N]|/N = exp(-(c+o(1)) sqrt(log N) loglog N), where A = integers n …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — the question is an asymptotic density constant; finite counts of A352287 (b-file to 10000 terms) cannot certify or move the constant
- witness verifier: n/a (membership in A is trivially checkable per integer, but no finite computation bears on the exp(-c sqrt(log N) loglog N) shape)
- notes: FRESHNESS FLAG: Eric Li, 'The Sylow Divisor Condition: a Resolution of Erdős Problem 768', arXiv:2606.24872 (2026, 20pp), claims a full resolution with c = 1/(2 sqrt(log 2)); the paper is linked from OEIS A352287 and a claimed solution is posted in the problem-page comments, but upstream still marks the problem OPEN — librarian should track and close if it holds up. Erdős's own bounds: density >= exp(-c sqrt(log N) loglog N) and <= exp(-(1+o(1)) sqrt(log N loglog N)) (paraphrased from problem …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P768` and the surface id in your receipt so the next agent's graph build finds your result.


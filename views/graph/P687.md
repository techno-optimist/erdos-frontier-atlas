# Erdős #687 — attack card

> Erdős #687 — estimates for Y(x), the primorial covering optimum · https://www.erdosproblems.com/687
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (problem)** — The record is held by precisely our own method class — massively parallel exhaustive search with domain-specific pruning — run at Google-Cloud scale (Bozek 2021). The next term a(65) sits at/beyond the feasibility frontier: the search over residue assignments mod the primorial grows super-exponentially, and the last three terms already required cloud-scale compute exceeding a ~100-core farm. Generic SAT/ILP/MIS encodings have NO edge over the hand-tuned Ziller–Morack pruning (they'd be dramatically worse), and there's no clean DRAT-certified nonexistence hook because the hard direction is the completeness of a bespoke search, not a CNF UNSAT. This is the R(5,5)-witness situation: computationally-shaped but already pinned to the wall by the same tools we'd bring. Lower-bound witnesses are trivially verifiable but can't "beat" an already-exact record without also doing the (infeasible) …[truncated; see source] [atlas/problems.json beatable=WALL]

## STATUS — three registers, never merged

- ours: **wall** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures
- prize: $1000 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:deep:687` — deep audit: **WALL**, lane `exact-backtracking`, board **HEAVY** (problems.json)

- finite object: A covering witness: an assignment {a_p mod p : p≤x} together with an interval [u, u+V−1] all of whose integers are congruent to some a_p. Equivalently a run of V consecutive integers each divisible by a prime ≤x. This certifies Y(x) ≥ V. Verified against the OEIS table A048670 = g(primorial) and its interval-endpoint companion A058989.
- current record: a(1..64) of A048670 exact; latest a(62)–a(64) by Andrzej Bozek, 2021, Google-Cloud-supported, via Ziller–Morack pruned exhaustive search (Costello–Watts upper bounds). No extension 2023–2026.
- frontier: A048670 exact to a(64) (Bozek 2021, Google-Cloud pruned exhaustion); next term = pruned-exhaustion certificate claim
- verifier: Lower-bound side is a POLY-TIME exact verifier: given a claimed run [u,u+V−1] and the residues, check each of the V integers is divisible by some prime ≤x — O(V·π(x)). Cheap. BUT the exact value / OEIS-table entry requires the UPPER-bound side: certify NO run of length V+1 exists over all ∏_{p≤x}p residue choices — an exhaustive/pruned nonexistence search whose cost is the whole difficulty (no compact DRAT-style certificate is standard here; correctness rests on the specialized search's completeness).
- attack: If attempted (not recommended): to push a(65)=Y(p_65=313), run the Ziller–Morack framework — fix a per-prime residue search with the strong "reach/interval-cover" pruning and Costello–Watts upper-bound cutoffs, distributed over the farm. Realistic compute: a(64) needed Google-Cloud scale (plausibly 10^4–10^5 core-hours with the specialized code); a(65) is meaningfully harder. A ~100-core farm running a from-scratch SAT/ILP encoding would not reach even a(60) in feasible time. No credible path to a novel record with our generic verifier-first stack.
- applies: **exact backtracking** (predicate `lane-move`)

### `S:gap:687:0977b807` — A048670(65) = j(p_65#) — Jacobsthal function of the product of the first 65 primes (first uncomputed cell; a(64)=1110) (gap_map, kind `next_cell`, confidence C3)

- bounds: lower >= 1110 (monotonicity in n from a(64)=1110 (adding a prime cannot shrink the extremal run); a(58)-a(64) computed by Bozek, OEIS A048670 b-file) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: A pair (u, L): L consecutive integers u..u+L-1, each divisible by at least one of the first 65 primes (p <= 313); establishes a(65) >= L+1. Same shape as the Gerbicz/Bozek witness tables attached to A048670.
- witness verifier: For each i < L, trial-divide u+i by the 65 primes up to 313 (or gcd with p_65#). O(65·L) word ops, fully dependency-free; a record L ~ 1100+ verifies in microseconds.
- notes: $1000 problem. The prize question is the asymptotic gap and is a wall: Y(x) << x^2 (Iwaniec, via linear sieve, Acta Arith. 1971/1978) vs Y(x) >> x·log x·logloglog x/loglog x (Ford-Green-Konyagin-Maynard-Tao, J. AMS 31 (2018)); Maier-Pomerance conjecture x(log x)^{2+o(1)}; fresh search found no post-2018 improvement. Machine lane = the table: exact a(62)-a(64) needed Google-Cloud-scale search (Bozek 2021), so exact a(65) is cell-in-principle but likely months of CPU; the cheap record lane is new …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #854 shares A048670 → card `views/graph/P854.md`
- #860 shares A048670, A058989 → card `views/graph/P860.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P687` and the surface id in your receipt so the next agent's graph build finds your result.


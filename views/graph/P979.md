# Erdős #979 — attack card

> Erdős problem #979 · https://www.erdosproblems.com/979
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **RETRACTION PIN** — claim `erdos-979-a6-public` carries a banned phrasing (pin `I:pin:b11d2e37`): a stronger earlier statement was withdrawn and must not be resurrected. Check `certificates/contracts.json` publication_bindings before publishing any prose about this problem.
- **RETRACTION PIN** — claim `erdos-979-a6-public` carries a banned phrasing (pin `I:pin:d0112dbe`): a stronger earlier statement was withdrawn and must not be resurrected. Check `certificates/contracts.json` publication_bindings before publishing any prose about this problem.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/979.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $k ≥ 2$, and let $f_k(n)$ count the number of solutions to $n = p_1^k + \dots + p_k^k$, where the $p_i$ are prime numbers. Is it true that $\limsup f_k(n) = \infty$?

## ATTACK SURFACES — which overlay said so

### `S:gap:979:5e7e7af4` — A385316(6) — smallest N that is a sum of 3 prime cubes in exactly 6 ways (gap_map, kind `next_cell`, confidence C2)

- bounds: lower > 10^12 (public in-repo exact verifier exhaustively counts the complete prime-cube window [0,10^12], reproduces a(1..5), and finds no value with exactly 6 representations) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `cell`
- witness object: n/a — exact-cell lane: a complete enumeration to a cap is a proof (every representation of V ≤ cap uses primes ≤ cap^(1/3))
- witness verifier: sieve primes ≤ cap^(1/3); enumerate all unordered triples; histogram sums; validated by reproducing a(1..5) exactly
- notes: PUBLIC BOUND: a(6) > 10^12, replayable from certificates/erdos-979/ (~11 GB). PRIVATE OPERATIONAL RESULT: the private streaming sweep and blind replica report gap-free agreement through 10^13, but their code and ledgers are not tracked in this repository, so that stronger result is quarantined from public promotion pending a self-contained replay packet. External OEIS submission is a separate human-sent step.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- contract claim `erdos-979-a6-public` [promoted]: A385316(6) > 10^12.
  - one-command replay: `python3 -I certificates/erdos-979/verify.py --cutoff 1e12`

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P979` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #193 — attack card

> Erdős problem #193 · https://www.erdosproblems.com/193
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/193.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $S \subseteq \mathbb{Z}^3$ be a finite set and let $A = \lbrace a_1, a_2, \ldots \rbrace$ be an infinite $S$-walk, so that $a_{i+1} - a_i \in S$ for all $i$. Must $A$ contain three collinear points?

## ATTACK SURFACES — which overlay said so

### `S:gap:193:7ebbdcc5` — A231255(7) — least t such that every t-step North-East lattice path (steps (0,1)/(1,0)) contains 7 collinear points …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower >= 328 (Barnoff & Bright, 'North-East Lattice Paths Avoiding k Collinear Points via Satisfiability', arXiv:2511.23226 — explicit 327-step path avoiding 7 collinear points (previous record 260 steps, Shallit); recorded in OEIS A231255) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `drat-candidate`
- witness object: A binary string of N/E steps of length >= 328 whose lattice path has no 7 collinear points (current record path: 327 steps)
- witness verifier: Dependency-free: for each of the ~L^2/2 point pairs, count path points on their line via cross-product test; O(L^3) integer arithmetic (~3.6e7 ops at L≈330), pure Python in seconds. Reject if any line carries >= 7 path points.
- notes: a(1..6) = 0,1,4,9,29,97; Barnoff–Bright enumerated all avoiding paths for k<=6 by SAT, so the k=7 frontier is a live SAT lane (record set Nov 2025 — beating it needs comparable incremental-SAT effort, not hours). No numeric upper bound: von Brömssen (May 2026) gives a(7) <= A380991(6) but A380991 (polyomino version) is only computed to n=4. Exact a(7) = UNSAT of avoidance at the threshold length — clean DRAT target in principle, scale unknown.
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P193` and the surface id in your receipt so the next agent's graph build finds your result.


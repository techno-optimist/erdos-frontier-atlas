# Erdős #417 — attack card

> Erdős problem #417 · https://www.erdosproblems.com/417
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: lim_{x→∞} V(x)/V'(x), where V(x) = #{totient values ≤ x} and V'(x) = #{φ(m) : 1 ≤ m ≤ x} …[truncated; see source])** — Trivially V'(x) ≤ V(x). Erdős (1998) suggested the limit may even be infinite (per erdosproblems.com/417). The formal-conjectures Lean file inverts the ratio to V'(x)/V(x) for boundedness — a technical choice only. Ford's totient machinery (Fo98, see problem 416) is the relevant proof tool; Tao arXiv:2309.02325 is linked from A061070. No witness lane: excluded from the records hunt. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/417.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:417:4f3fc7e6` — lim_{x→∞} V(x)/V'(x), where V(x) = #{totient values ≤ x} and V'(x) = #{φ(m) : 1 ≤ m ≤ x} — does the limit exist, and is …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — limit-existence question; no finite computation certifies it
- witness verifier: none exists; both counting functions are OEIS-tabulated to 1e4 (A264810, A061070) and empirical ratios are recomputable, but carry no certificate value
- notes: Trivially V'(x) ≤ V(x). Erdős (1998) suggested the limit may even be infinite (per erdosproblems.com/417). The formal-conjectures Lean file inverts the ratio to V'(x)/V(x) for boundedness — a technical choice only. Ford's totient machinery (Fo98, see problem 416) is the relevant proof tool; Tao arXiv:2309.02325 is linked from A061070. No witness lane: excluded from the records hunt.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #416 shares A264810 → card `views/graph/P416.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P417` and the surface id in your receipt so the next agent's graph build finds your result.


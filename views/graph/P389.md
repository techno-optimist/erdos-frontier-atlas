# Erdős #389 — attack card

> Erdős problem #389 · https://www.erdosproblems.com/389
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/389.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is it true that for every $n \geq 1$ there is a $k$ such that $$ n(n + 1) \cdots (n + k - 1) \mid (n + k) \cdots (n + 2k - 1)? $$

## ATTACK SURFACES — which overlay said so

### `S:gap:389:881990b7` — A375071(27) — least k >= 1 such that 27·28···(26+k) divides (27+k)···(26+2k) (first uncomputed cell; a(0)–a(26) known, …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: A pair (n=27, k) with 27·28···(26+k) | (27+k)···(26+2k). Unverified candidate: k = 18253129921815 = A375077(13) − 27, from the empirical cross-map A375071(n) = A375077((n−1)/2) − n which holds at every known odd cell (checked n=21,23,25)
- witness verifier: Divisibility ⟺ v_p(C(26+2k, k)) >= v_p(C(26+k, k)) for all primes p (Kummer carry counts). For p > 26 a failing p requires k mod p ∈ (p−27, p), so verification = sieve-scan of k mod p over primes p <= 26+2k (~5e11 primes at k ≈ 1.8e13; a GPU-day). Needs only a prime sieve + modular arithmetic.
- notes: Erdős–Straus: does such k exist for every n? (Open; existence for each computed n is witnessed.) Mehta computed n <= 18; OEIS extended to a(26) with active work Mar 2026 (Kesarwani comment; SharkyKesa listed as currently working on erdosproblems.com — coordinate, don't race). The (n,k) → (n+k−1, n) transform ties this to A375077/problem 396: transform preserves validity but NOT minimality (a(7) = 8171 is much smaller than the transform predicts), so the candidate a(27) above is an upper-bound …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P389` and the surface id in your receipt so the next agent's graph build finds your result.


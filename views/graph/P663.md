# Erdős #663 — attack card

> Erdős problem #663 · https://www.erdosproblems.com/663
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: q(n,k) — least prime not dividing (n+1)(n+2)...(n+k) (OEIS A391668, table read by …[truncated; see source])** — Erdős–Pomerance problem. Easy bound q(n,k) < (1+o(1)) k log n (also on the OEIS formula lines); the problem asks whether the k factor can be removed, possibly even up to k = o(log n) (a Tao heuristic in the page comments supports this). Companion conjecture: q(n, log n) >= (2+eps) log n infinitely often for some eps > 0. Useful identities from A391668: T(n,k) = A007918(n+k+1) (next prime) for k >= n. Refs: Burr–Erdős–Graham–Li, Acta Arith. 1996; Erdős, Math. Japon. 1997. Problem page: not …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:663:e87685a0` — q(n,k) — least prime not dividing (n+1)(n+2)...(n+k) (OEIS A391668, table read by antidiagonals); conjecture: q(n,k) < …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None: the conjecture is asymptotic (eventual behaviour in n for fixed k). Any individual table cell T(n,k) is computable at will and no finite set of cells confirms or refutes the o(1) claim.
- witness verifier: No cheap witness exists for the asymptotic statement. (Individual cells verify trivially by trial division of n+1..n+k by successive primes, but they carry no decisive content.)
- notes: Erdős–Pomerance problem. Easy bound q(n,k) < (1+o(1)) k log n (also on the OEIS formula lines); the problem asks whether the k factor can be removed, possibly even up to k = o(log n) (a Tao heuristic in the page comments supports this). Companion conjecture: q(n, log n) >= (2+eps) log n infinitely often for some eps > 0. Useful identities from A391668: T(n,k) = A007918(n+k+1) (next prime) for k >= n. Refs: Burr–Erdős–Graham–Li, Acta Arith. 1996; Erdős, Math. Japon. 1997. Problem page: not …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #457 shares A391668 → card `views/graph/P457.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P663` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #382 — attack card

> Erdős problem #382 · https://www.erdosproblems.com/382
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:382:ec46e358` — A388850(9) — least start u of a maximal run of exactly 10 consecutive integers whose product has its largest prime …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper 4928180396 (candidate interval found by Touch Sungkawichai (OEIS A388850 comment, Jan 2026))
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: a maximal bad interval [u, u+9] with u < 4928180396 (largest prime of the product to exponent >= 2; neither one-step extension keeps the property); exact value via exhaustive scan below the candidate
- witness verifier: factor the <= 12 integers involved (~5e9 scale — trivial), take the largest prime p dividing the product, check its total exponent >= 2, and check both one-step extensions [u-1, u+9] and [u, u+10] break the property — dependency-free, milliseconds per interval
- notes: a(0..8) = 4, 8, 48, 1680, 76725, 332925, 7474752, 4541154, 75047565 known exact (von Bromssen, Sep 2025; note non-monotone, so no clean lower bracket published — exhaustive frontier is at least ~7.5e7 implied by exactness of a(8), exact frontier unstated). Open candidates: a(9) <= 4928180396, a(10) <= 40533366231, a(11) <= 111460496439, a(12) <= 436502026483 (Sungkawichai, Jan 2026), a(13) <= 17952448643078 (StijnC, per OEIS). A segmented largest-prime-factor sieve to 4.93e9 + sliding-window …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P382` and the surface id in your receipt so the next agent's graph build finds your result.


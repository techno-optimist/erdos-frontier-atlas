# Erdős #169 — attack card

> Erdős problem #169 · https://www.erdosproblems.com/169
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: f(3) — supremum of sum_{a in A} 1/a over all 3-AP-free sets A of positive integers …[truncated; see source])** — Record is 40+ years old, hence graded hard, but the attack surface just changed: Walker [Wa25] pushed f(4) >= 4.43975 via the Kempner reduction, and the (b,S) search space is machine-enumerable with cheap rigorous evaluation — a genuine hunt candidate. Context: Berlekamp 1968 f(k) >= (log2/2)k; Gerver 1977 f(k) >= (1-o(1))k*log k; trivially f(k)/log W(k) >= 1/2 and any constant > 1/2 is open — this is the link to the stub's OEIS A005346 = van der Waerden numbers W(2,n) (known through …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:169:7f6c571d` — f(3) — supremum of sum_{a in A} 1/a over all 3-AP-free sets A of positive integers (Erdős-Graham); finite because …[truncated; see source] (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower 3.00849 (J. Wróblewski [Wr84], A nonaveraging set of integers with a large sum of reciprocals; still the record per erdosproblems.com/169 (page dated 2026-04-04)) · upper — (—)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: An explicit 3-AP-free set with certified reciprocal sum > 3.00849. Walker [Wa25] proved Kempner sets (all integers whose base-b digits lie in a fixed S subset of {0..b-1}) suffice to approach f(k), so a witness is a pair (b,S) + an AP3-freeness argument for the digit construction + a rigorous numeric lower bound on the reciprocal sum.
- witness verifier: Structural AP3-freeness check of the digit construction, then interval-arithmetic partial sums with a geometric tail bound for sum 1/n over the Kempner set (classical Baillie-style algorithm, dependency-free).
- notes: Record is 40+ years old, hence graded hard, but the attack surface just changed: Walker [Wa25] pushed f(4) >= 4.43975 via the Kempner reduction, and the (b,S) search space is machine-enumerable with cheap rigorous evaluation — a genuine hunt candidate. Context: Berlekamp 1968 f(k) >= (log2/2)k; Gerver 1977 f(k) >= (1-o(1))k*log k; trivially f(k)/log W(k) >= 1/2 and any constant > 1/2 is open — this is the link to the stub's OEIS A005346 = van der Waerden numbers W(2,n) (known through …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #138 shares A005346 → card `views/graph/P138.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P169` and the surface id in your receipt so the next agent's graph build finds your result.


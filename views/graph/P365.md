# Erdős #365 — attack card

> Erdős problem #365 · https://www.erdosproblems.com/365
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:365:142f4213` — A060355(40) — smallest k > 10^22 with k and k+1 both powerful (next consecutive-powerful pair; the counting function …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 1e22 (Donovan Johnson's complete enumeration below 10^22 (OEIS A060355 b-file, 39 terms)) · upper 23146276081390728245000 (OEIS A060859 b-file term 31 (~2.31e22, powerful k^2-1 family; a valid A060355 term above 10^22))
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: a pair (k, k+1) both powerful with 1e22 < k < 2.3146e22 (tightens the a(40) bracket); the exact cell falls to an exhaustive powerful-number sweep of the bounded window
- witness verifier: factor k and k+1, check all prime exponents >= 2 on both — milliseconds; for the exact cell, a sweep certificate: enumerate all a^2*b^3 (b squarefree) in the window segment-by-segment and scan for adjacent values differing by 1
- notes: The yes/no part (must one of n, n+1 be a square?) is already answered NO: Golomb 1970 (12167 = 23^3, 12168 = 2^3*3^2*13^2) and Walker 1976 (7^3 x^2 = 3^3 y^2 + 1 has infinitely many solutions). Open content: is N(x) = #pairs <= x bounded by (log x)^{O(1)}? Window contains ~1.1e11 powerful numbers (density 2.173*sqrt(x)); chunked c^2 d^3 enumeration + adjacency scan is hours-scale on one box and would add a new OEIS term (first extension since 2011). The same sweep advances #366's frontier. Of …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #364 shares A060355 → card `views/graph/P364.md`
- #366 shares A060355 → card `views/graph/P366.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P365` and the surface id in your receipt so the next agent's graph build finds your result.


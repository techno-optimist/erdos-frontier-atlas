# Erdős #156 — attack card

> Erdős problem #156 · https://www.erdosproblems.com/156
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:156:9b08bd67` — A382397(66) — minimum size of a maximal Sidon subset of {1..66} (first uncomputed cell; OEIS table computed through …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: A maximal Sidon set S in {1..66} of size 6 (or, surprisingly, smaller): all pairwise differences of S distinct, and for every x in {1..66}\S the set S+{x} has a repeated difference; establishes a(66) <= |S|.
- witness verifier: Check C(|S|,2) differences distinct; then for each of the <=65 non-members x verify S+{x} violates the Sidon condition. Under 10^4 integer operations, dependency-free.
- notes: Data: a(n)=6 for all computed 43<=n<=65; monotonicity is empirically clean but unproven, so no rigorous lower/upper carried. Exhaustive closure of a(66) (and pushing to n~100) is a bounded search (C(66,6)~9e7 candidates with heavy Sidon pruning) — hours on one box; OEIS comment explicitly notes there are too few terms to pin the growth pattern, so new cells have real value. Asymptotic open content (Erdős-Sárközy-Sós 1994): greedy gives >> N^(1/3); Ruzsa 1998 (A small maximal Sidon set, …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `sidon sets`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P156` and the surface id in your receipt so the next agent's graph build finds your result.


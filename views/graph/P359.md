# Erdős #359 — attack card

> Erdős problem #359 · https://www.erdosproblems.com/359
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Density/growth of MacMahon's segmented numbers a_k (A002048): Andrews conjectures a_k ~ k …[truncated; see source])** — Known partial results (erdosproblems.com/359, edited Dec 2025, paraphrased): Porubsky 1977 proved a_k < (log k)^eps * k log k/loglog k infinitely often, and limsup A(x)/pi(x) >= 1/log 2; Andrews 1975 studied the conjecture a_k ~ k log k/loglog k. Erdős asked (n=1 case) whether a_k/k -> infinity and a_k/k^(1+c) -> 0 for all c>0 — neither direction is finitely checkable. Empirical growth-curve extension is trivial (open-easy compute, no record semantics); flagged not-gap-shaped so the scout skips …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/359.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:359:b43c2782` — Density/growth of MacMahon's segmented numbers a_k (A002048): Andrews conjectures a_k ~ k log k / loglog k; no bounded …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None — the sequence is deterministic and cheaply computable to any index (b-file to 10000 terms exists; simple sieve excluding sums of consecutive earlier terms), so extending the table is not a record; the open content is purely asymptotic
- witness verifier: No cheap witness exists for the asymptotic claims; table values themselves verify by direct recomputation (seconds), which is precisely why they carry no record value
- notes: Known partial results (erdosproblems.com/359, edited Dec 2025, paraphrased): Porubsky 1977 proved a_k < (log k)^eps * k log k/loglog k infinitely often, and limsup A(x)/pi(x) >= 1/log 2; Andrews 1975 studied the conjecture a_k ~ k log k/loglog k. Erdős asked (n=1 case) whether a_k/k -> infinity and a_k/k^(1+c) -> 0 for all c>0 — neither direction is finitely checkable. Empirical growth-curve extension is trivial (open-easy compute, no record semantics); flagged not-gap-shaped so the scout skips …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P359` and the surface id in your receipt so the next agent's graph build finds your result.


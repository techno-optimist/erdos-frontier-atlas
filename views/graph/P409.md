# Erdős #409 — attack card

> Erdős problem #409 · https://www.erdosproblems.com/409
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/409.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:409:297b6c6b` — A229487(n), n = 1..53 — per-cell greatest integer whose iteration x → phi(x)+1 reaches prime(n); every listed maximum …[truncated; see source] (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower per-cell table of 53 conjectured maxima (largest listed: a(53) = 54582 for p = 241; e.g. a(6) = 138 for p = 13, a(1) = 2, a(2) = 6) (OEIS A229487 (T. D. Noe; forward search, bound unstated — values explicitly 'conjectured')) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: An integer x > A229487(n) whose phi+1-trajectory ends at prime(n), for any n <= 53 (e.g. any x > 138 reaching 13) — beats a listed cell
- witness verifier: Iterate x → phi(x)+1: strictly decreasing on composites (phi(x) <= x − sqrt(x)), fixes primes, so the trajectory terminates in < x steps (practically ~log x); needs only factorization of iterates, all < x. Milliseconds at x ~ 1e12. Dependency-free.
- notes: Finucane's problem: every trajectory of x → phi(x)+1 reaches a prime (trivially, by descent); A039651 counts the steps; Cambie notes F(n) = 1 infinitely often and F(n) = o(n) is trivial — Bloom reads the intended open content as good upper bounds for the iteration count F(n). Two machine lanes on the A229487 surface: (1) forward phi-sieve over all x <= 1e9–1e10 (hours) rederives every basin and surfaces any converger beating a listed cell — Noe's original search bound is unstated, so even a …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `iterated functions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P409` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #436 — attack card

> Erdős problem #436 · https://www.erdosproblems.com/436
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:436:73f5de5e` — A000445(8) = Lambda(8,2) — latest possible first occurrence (limsup over primes p) of a pair of consecutive 8th-power …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 1499876 (Don Reble, computation reported on SeqFan mailing list (recorded in OEIS A000445 comment by C. E. Thompson, Jan 2020)) · upper 1508324 (Don Reble, same computation (OEIS A000445 comment))
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: A consistency certificate in the Mills/Lehmer style: an assignment of 8th-power-residue characters to a finite set of small primes that (a) is realizable for infinitely many p (finite CRT/Dirichlet check) and (b) forces no consecutive pair of 8th-power residues below some r > 1499876. Companion cheap exhibit: an explicit prime p with first consecutive pair at r (raises the finite-p record and …[truncated; see source]
- witness verifier: For an explicit prime p and claimed r: for t = 1..r-1 check not both t^((p-1)/8) == 1 and (t+1)^((p-1)/8) == 1 (mod p) — pure modular exponentiation, dependency-free. For the limsup lower bound: verify the residue-character pattern is multiplicatively consistent and blocks all pairs below r (finite …[truncated; see source]
- notes: Gap of only 8449 between Reble's bounds has stood since 2019/2020 (OEIS keyword 'more,hard'; freshness re-checked via web search 2026-07-18, nothing newer found). Values Lambda(k,2) for k=2..7: 9, 77, 1224, 7888, 202124, 1649375 (Lehmer-Lehmer 1962 / Bierstedt-Mills 1963 / Dunton 1965 / LLM 1963 / BLL 1964, per OEIS). Note a(8) < a(7). Hildebrand 1991 proved Lambda(k,2) finite for ALL k, so every cell k >= 8 is a legitimate finite target. Remaining fully-open content of #436: is Lambda(k,3) …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P436` and the surface id in your receipt so the next agent's graph build finds your result.


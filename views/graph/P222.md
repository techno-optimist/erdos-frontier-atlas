# Erdős #222 — attack card

> Erdős problem #222 · https://www.erdosproblems.com/222
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:222:cc138a1f` — A357018(44) — next record run of consecutive integers not expressible as a sum of two squares (current record: 106 …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 106 (A357018(43)=106 (H. Pfoertner, 2022) with start A297350(43)=99951600293 (C. R. Greathouse IV b-file); the full 106-run and both representable endpoints (99951600293, 99951600400) re-verified this session via factorization) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `unknown`
- witness object: >= 107 consecutive integers each non-representable as a sum of two squares, plus exhaustive scan coverage from 99951600293 to the new start (records semantics)
- witness verifier: Per-integer certificate: exhibit a prime p = 3 (mod 4) dividing n to an odd power (sum-of-two-squares criterion); ~110 Pollard-rho factorizations of ~1e11–1e13 integers, seconds in pure Python. Endpoints checked representable the same way.
- notes: Both records sequences carry keyword 'more' and stop near 1e11 — a segmented sieve marking a^2+b^2 (or multiplicative sieve via primes 3 mod 4) to 1e12–1e13 on one GPU box is a days-scale record hunt; the most compute-accessible lane in this chunk. Finite records do NOT move the asymptotic frontier: limsup gap/log n_k >= 0.868... (Dietmann–Elsholtz–Kalmynin–Konyagin–Maynard, IMRN 2022, improving Richards 1982's 1/4), best upper O(n_k^{1/4}) (Bambah–Chowla 1947), Erdős 1951 lower …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `squares`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P222` and the surface id in your receipt so the next agent's graph build finds your result.


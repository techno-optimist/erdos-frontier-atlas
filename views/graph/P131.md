# Erdős #131 — attack card

> Erdős problem #131 · https://www.erdosproblems.com/131
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:131:e2ac51a8` — A068063(101) — maximum size of a nondividing subset of {1,...,101} (no element divides the sum of any nonempty subset …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 8 (Monotonicity from A068063(100) = 8 (OEIS b-file b068063.txt by Christian Sievers, re-fetched and read 2026-07-18)) · upper 9 (DERIVED deletion bound a(N+1) <= a(N)+1 (removing an element preserves the nondividing property) with a(100)=8; the explicit literature bound F(N) < 3*sqrt(N)+1 (Erdos-Lev-Rauzy-Sandor-Sarkozy 1999) gives only 31)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: A 9-element subset A of {1,...,101} such that for every a in A, no nonempty subset of A\{a} has sum divisible by a. Existence pins a(101)=9; exhaustive refutation pins a(101)=8.
- witness verifier: For each a in A run a subset-sum DP over A\{a} modulo a and assert residue 0 is unreachable by a nonempty subset (or brute-force all 2^8 = 256 subsets per element). Microseconds, dependency-free.
- notes: Headline #131 asks the growth of F(N): Erdos's original question (F(N) > N^{1/2-o(1)}?) is answered NO — F(N) <= N^{1/4+o(1)} via the non-averaging-set link (Pham-Zakharov 2024), against F(N) >> N^{1/5} (Csaba construction, in ELRSS99) and Straus's exp((sqrt(2/log 2)+o(1)) sqrt(log N)); correct exponent open (asymptotic, not machine-movable). The finite table is the scout surface: branch-and-bound over {1..N} extends A068063 cheaply (jumps to size 8 at n=65; size-9 threshold unknown — locating …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P131` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #1053 — attack card

> Erdős problem #1053 · https://www.erdosproblems.com/1053
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Largest k for which a k-perfect number (sigma(n) = kn) is known)** — The actual Erdos question (Guy B2): for k-perfect n, must k = o(log log n)? Asymptotic wall - but the record ladder is the largest attained abundancy, stuck at k=11 since ~2001. RH-conditional bound (A007539 comment, Greathouse): least n-fold perfect > exp(exp(n/e^gamma)), so a 12-perfect has >= ~10^366. Guy also reports the suggestion that only finitely many k-perfect numbers with k >= 3 may exist. Related table A007539 (least n-fold perfect) has b-file through n=8 (a(8) ~ 8.27e132); …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:1053:bb11ef3f` — Largest k for which a k-perfect number (sigma(n) = kn) is known (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower 11 (Flammenkamp Multiply Perfect Numbers database (2 known abundancy-11 MPNs among 5932 known MPNs, snapshot 2022-12-31, page updated 2023-01-12); erdosproblems.com/1053 remark confirms largest attained k = 11 (accessed 2026-07-18)) · upper — (—)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: A 12-perfect number: n with sigma(n) = 12n, presented via its complete prime factorization (expected to have >= 142 distinct prime divisors if even)
- witness verifier: Given n = prod p_i^{e_i}: verify primality of each p_i (certificates), compute sigma(n) = prod((p_i^{e_i+1}-1)/(p_i-1)), check sigma(n) = 12n. Dependency-free bigint arithmetic; verification is cheap even at thousands of digits.
- notes: The actual Erdos question (Guy B2): for k-perfect n, must k = o(log log n)? Asymptotic wall - but the record ladder is the largest attained abundancy, stuck at k=11 since ~2001. RH-conditional bound (A007539 comment, Greathouse): least n-fold perfect > exp(exp(n/e^gamma)), so a 12-perfect has >= ~10^366. Guy also reports the suggestion that only finitely many k-perfect numbers with k >= 3 may exist. Related table A007539 (least n-fold perfect) has b-file through n=8 (a(8) ~ 8.27e132); …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1053` and the surface id in your receipt so the next agent's graph build finds your result.


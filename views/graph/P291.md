# Erdős #291 — attack card

> Erdős problem #291 · https://www.erdosproblems.com/291
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: No bounded quantity — writing H_n = a_n/L_n with L_n = lcm(1..n): do gcd(a_n, L_n) = 1 …[truncated; see source])** — Per erdosproblems.com/291 (edited 2026-01-12): the gcd>1 direction is effectively resolved — site-recorded observation (Steinerberger): if the leading base-p digit of n is p-1 then p | gcd; exact criterion: p | gcd iff p divides the numerator of H_k where k = leading digit of n base p. The gcd=1 direction is the open part; heuristic count ~ x/log x of such n <= x (Shiu preprint 2016 [Sh16]), so infinitely many expected but density zero. Wu–Yan 2022 [WuYa22]: conditional on Q-linear independence …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:291:32ed6386` — No bounded quantity — writing H_n = a_n/L_n with L_n = lcm(1..n): do gcd(a_n, L_n) = 1 and gcd(a_n, L_n) > 1 each occur …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None meaningful — individual n with gcd = 1 are cheap to find and verify but are plentiful (2641 of the first 10000 per the OEIS b-file, largest 9311 — independently re-verified this session by exact rational arithmetic), so a 'largest known instance' record carries no evidential weight for infinitude
- witness verifier: For a given n: compute lcm(1..n) and the exact numerator of H_n, take gcd — pure integer arithmetic, seconds for n in the millions; but no finite set of instances decides the pure-existence question
- notes: Per erdosproblems.com/291 (edited 2026-01-12): the gcd>1 direction is effectively resolved — site-recorded observation (Steinerberger): if the leading base-p digit of n is p-1 then p | gcd; exact criterion: p | gcd iff p divides the numerator of H_k where k = leading digit of n base p. The gcd=1 direction is the open part; heuristic count ~ x/log x of such n <= x (Shiu preprint 2016 [Sh16]), so infinitely many expected but density zero. Wu–Yan 2022 [WuYa22]: conditional on Q-linear independence …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `unit fractions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P291` and the surface id in your receipt so the next agent's graph build finds your result.


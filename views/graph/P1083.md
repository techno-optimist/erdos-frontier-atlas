# Erdős #1083 — attack card

> Erdős problem #1083 · https://www.erdosproblems.com/1083
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:1083:4f121e5b` — A186704(14) — minimum number of distinct distances determined by 14 points in the plane (first uncomputed cell of the …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 7 (max planar j-distance set sizes are 1,3,5,7,9,12,13 for j=0..6 (Erdős–Fishburn, Discrete Math. 160 (1996) for j<=5; Shinohara 2008 uniqueness at j=5; X. Wei, 'A Proof of Erdős-Fishburn's Conjecture for g(6)=13', Electron. J. Combin. 19(4)#P38, 2012), all <= 13, so any 14-point set determines >= 7 distances) · upper 7 (explicit 14-point triangular-lattice patch with exactly 7 distinct distances, machine-verified this session in exact Eisenstein-integer arithmetic (squared norms a^2+ab+b^2))
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: n-point planar configuration with few distinct distances (rational/Eisenstein coordinates); for the OEIS-extension record: the verified 14-point (also 15- and 16-point) lattice patches with exactly 7 distances
- witness verifier: exact integer arithmetic: represent points as Eisenstein/lattice coordinates, compute all C(n,2) squared distances as integers, count distinct values; no floating point, stdlib only
- notes: CROSSREF CLOSURE AVAILABLE: a(14)=a(15)=a(16)=7 all follow from Wei 2012 (+ EF96/Shinohara08 for j<=5) plus lattice constructions verified this session — OEIS still ends at a(13)=6, so extending A186704 (and A131628, whose a(6)=13 already encodes Wei) is a free librarian win in the #241/#552 pattern. First genuinely open cell: a(17) ∈ {7,8} — verified 17-point patch gives <= 8; equals 7 iff a 17-point 7-distance set exists, i.e. iff g(7) >= 17; best known 7-distance construction has 16 points …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #89 shares A186704 → card `views/graph/P89.md`
- #91 shares A186704 → card `views/graph/P91.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1083` and the surface id in your receipt so the next agent's graph build finds your result.


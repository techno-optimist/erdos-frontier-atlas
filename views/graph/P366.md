# Erdős #366 — attack card

> Erdős problem #366 · https://www.erdosproblems.com/366
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: count of n with {n, n+1} both powerful and at least one member 3-full (Erdos-Graham: is …[truncated; see source])** — Both known examples have the 3-full member FIRST (8 = 2^3 before 9 = 3^2; 12167 = 23^3 before 12168), so the exact Lean-stub orientation is even more open. abc implies finitely many such n (Turturean, per erdosproblems). Honest rung: extending the A060355 sweep 10^22 -> 10^23 (~7e11 powerful numbers, days-scale) raises the verified bound; constructive Pell-style witnesses look blocked because 3-fullness of a pair member is a much stronger Diophantine constraint. Weaker open sub-question …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `verifiable`, finite handle `verifiable`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/366.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Are there any $2$-full $n$ such that $n+1$ is $3$-full?

## ATTACK SURFACES — which overlay said so

### `S:triage:366` — triage verdict **TARGET** (finite_handle_triage, 2026-07-26)

- our summary: Is there a 2-full n with n+1 3-full? (p|n implies p^2|n; p|n+1 implies p^3|n+1.)
- witness: Concrete: one integer n printed with the factorizations of n and n+1. Verification is a few dozen exact integer divisions — no floats, no CAS. Known near-examples in the REVERSE orientation: (8,9) and (12167 = 23^3, 12168 = 2^3·3^2·13^2).
- smallest untested case: n > 10^22. Concretely: the first cubefull number c in (10^22, 10^24] with c-1 (or c+1, reverse orientation) powerful.
- known frontier: No examples for n < 10^22 — but this bound is INHERITED from OEIS A060355 (consecutive powerful pairs), b-file by Donovan Johnson, 39 terms < 10^22. That is the harder, sqrt(X)-cost search. No sequence or computation exists anywhere for the cubefull-driven version, and OEIS has NO sequence at all for consecutive cubefull pairs (Erdős's weaker question in [Er76d], Guy B16). Page last edited 16 July 2026 and actively curated (Cambie, Stephan, van Doorn), so absence of a deeper bound is meaningful …[truncated; see source]
- search-space estimate: Only n+1 needs enumerating: #cubefull <= X ~ 5.5·X^(1/3) (A036966; generate as x^5·y^4·z^3, streamed, no storage). At X = 1e24: 5.5e8 candidates x ~1e3 ops each (trial-divide neighbour to 1e4, then perfect-power test on the cofactor — valid since a leftover p^2·q^3 with p,q >= 1e4 exceeds 1e20 only rarely) = ~5e11 ops. X = 1e26 costs 2.6e9 x 1e3 = 2.6e12.
- verification cheap and exact: yes
- verdict reason: Only one of the five that clears all four bars. The recorded 10^22 comes from the sqrt(X)-bounded PAIR search; this problem only requires enumerating cubefull numbers, which cost X^(1/3) — so 1e24 fits in ~5e11 ops and 1e26 in ~2.6e12, i.e. 2-4 decades past the record on one workstation with pure integer arithmetic. Caveats, stated up front: (1) the expected outcome is a negative 'verified to N' record, not a resolution — the heuristic count of cubefull c <= X with c±1 powerful scales as X^(-1/6), convergent, and ABC implies finiteness; (2) the upstream statement's orientation contradicts its own worked examples (8 and 12167 are the 3-FULL member, 9 and 12168 the 2-full one), so sweep both …[truncated; see source]
- applies: **finite-handle witness search** (predicate `triage-move`)

### `S:gap:366:8ee41086` — count of n with {n, n+1} both powerful and at least one member 3-full (Erdos-Graham: is (8,9) essentially the only …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 1e22 (complete consecutive-powerful-pair list below 10^22 (Johnson, OEIS A060355 b-file); only (8,9) and (12167,12168) have a 3-full member (erdosproblems.com/366 remarks)) · upper — (—)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: a third pair (n, n+1) both powerful with one member 3-full — in particular the first-ever example in the strict Lean orientation (n 2-full, n+1 3-full), which currently has ZERO known examples
- witness verifier: factor n and n+1; check all exponents >= 2 on both and all exponents >= 3 on one member — trivial, dependency-free
- notes: Both known examples have the 3-full member FIRST (8 = 2^3 before 9 = 3^2; 12167 = 23^3 before 12168), so the exact Lean-stub orientation is even more open. abc implies finitely many such n (Turturean, per erdosproblems). Honest rung: extending the A060355 sweep 10^22 -> 10^23 (~7e11 powerful numbers, days-scale) raises the verified bound; constructive Pell-style witnesses look blocked because 3-fullness of a pair member is a much stronger Diophantine constraint. Weaker open sub-question …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #364 shares A060355 → card `views/graph/P364.md`
- #365 shares A060355 → card `views/graph/P365.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P366` and the surface id in your receipt so the next agent's graph build finds your result.


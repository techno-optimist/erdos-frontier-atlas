# Erdős #394 — attack card

> Erdős problem #394 · https://www.erdosproblems.com/394
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Asymptotics of Sum_{n<=x} t_2(n), where t_2(n) = A344005(n) = least m with n | m(m+1))** — Erdős–Hall 1978 [ErHa78] proved Sum_{n<=x} t_2(n) ≪ x²·logloglog x/loglog x, settling Erdős's o(x²) conjecture; trivial lower bound ≫ x²/log x from t_2(p) = p−1; Erdős–Hall conjecture o(x²/(log x)^c) for every c < log 2 — that conjecture is the open gap and is not witness-movable. Structural facts: t_2(n) <= n−1 with equality iff n is a prime power; <= n/2 − 1 for non-prime-powers; >= (sqrt(4n+1)−1)/2 (Sloane 2021). Secondary discrete asks on the page — behaviour of t_{n−3}(n!), and whether …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/394.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:394:62b5684d` — Asymptotics of Sum_{n<=x} t_2(n), where t_2(n) = A344005(n) = least m with n | m(m+1) (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none
- witness verifier: No cheap witness: the open content is a mean-value asymptotic; A344005 itself has an O((log n)^2) exact algorithm (Sloane, Jul 2021) with tables to 10^7 already in OEIS, so no finite computation is a record.
- notes: Erdős–Hall 1978 [ErHa78] proved Sum_{n<=x} t_2(n) ≪ x²·logloglog x/loglog x, settling Erdős's o(x²) conjecture; trivial lower bound ≫ x²/log x from t_2(p) = p−1; Erdős–Hall conjecture o(x²/(log x)^c) for every c < log 2 — that conjecture is the open gap and is not witness-movable. Structural facts: t_2(n) <= n−1 with equality iff n is a prime power; <= n/2 − 1 for non-prime-powers; >= (sqrt(4n+1)−1)/2 (Sloane 2021). Secondary discrete asks on the page — behaviour of t_{n−3}(n!), and whether …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P394` and the surface id in your receipt so the next agent's graph build finds your result.


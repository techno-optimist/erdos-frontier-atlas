# Erdős #51 — attack card

> Erdős problem #51 · https://www.erdosproblems.com/51
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Existence of an infinite set A of totient values such that n_a/a -> infinity, where n_a …[truncated; see source])** — Pure existence question; erdosproblems.com/51 explicitly marks it not resolvable by finite computation. Adjacent famous open: Carmichael's conjecture (A014197 contains no 1; Erdős proved one such t would force infinitely many). Both linked OEIS sequences are fully computed in their published ranges (A002202 b-file to 10000, A014197 b-file to 10000) — no uncomputed cell exists to attack. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/51.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is there an infinite set $A \subset \mathbb{N}$ such that for every $a \in A$, there is an integer n such that $\phi(n)=a$, and yet if $n_a$ is the smallest such integer, then $\frac{n_a}{a} → \infty$ as $a → ∞$?

## ATTACK SURFACES — which overlay said so

### `S:gap:51:0b2ebbad` — Existence of an infinite set A of totient values such that n_a/a -> infinity, where n_a is the least n with phi(n) = a …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: No finite witness decides the problem: any finite list of totient values a with large ratio n_a/a is exactly checkable via inverse-totient computation, but the claim quantifies over an infinite set — neither a witness nor a counterexample is finite. A 'champions of n_a/a' record table is computable (cf. A014197 b-file to 10000, Alekseyev's invphi.gp) but carries no decision content.
- witness verifier: Per single value a: enumerate all n with phi(n)=a (invphi / sieve with a provable search bound) and confirm the minimum — exact and cheap; verifies finite data only.
- notes: Pure existence question; erdosproblems.com/51 explicitly marks it not resolvable by finite computation. Adjacent famous open: Carmichael's conjecture (A014197 contains no 1; Erdős proved one such t would force infinitely many). Both linked OEIS sequences are fully computed in their published ranges (A002202 b-file to 10000, A014197 b-file to 10000) — no uncomputed cell exists to attack.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #821 shares A014197 → card `views/graph/P821.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P51` and the surface id in your receipt so the next agent's graph build finds your result.


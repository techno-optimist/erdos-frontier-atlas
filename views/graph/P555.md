# Erdős #555 — attack card

> Erdős problem #555 · https://www.erdosproblems.com/555
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: R_k(C_{2n}) — multicolour Ramsey number of even cycles as a function of the number of …[truncated; see source])** — Erdős–Graham problem: determine R_k(C_{2n}). Known bounds (per problem page, paraphrased): k^{1+1/2n} << R_k(C_{2n}) << k^{1+1/(n-1)} (Erdős 1981); Chung–Graham 1975: R_k(C_4) > k^2-k+1 when k-1 is a prime power, and R_k(C_4) <= k^2+k+1 for all k. The hub's linked OEIS sequence A389313 (Faudree–Schelp 1974, Rosta 1973) is CLOSED — no record lane there. Adjacent machine-shaped record surface lives in Radziszowski DS1's multicolour cycle cells (not verified here). Problem page marks this as not …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:555:1517e932` — R_k(C_{2n}) — multicolour Ramsey number of even cycles as a function of the number of colours k (linked OEIS A389313 is …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None for the problem as posed: it asks to determine R_k(C_{2n}) asymptotically in k, which no single colouring settles. Individual lower-bound colourings only move specific (k,n) cells in the DS1 multicolour table, which is not the quantity linked here.
- witness verifier: No cheap witness exists for the asymptotic determination. (A colouring certificate for a specific R_k(C_{2m}) cell would be cheaply verifiable — check no monochromatic C_{2m} — but the linked sequence A389313 has a closed formula with no uncomputed cell: a(n)=2n-1 for n odd, 3n/2-1 for n even, …[truncated; see source]
- notes: Erdős–Graham problem: determine R_k(C_{2n}). Known bounds (per problem page, paraphrased): k^{1+1/2n} << R_k(C_{2n}) << k^{1+1/(n-1)} (Erdős 1981); Chung–Graham 1975: R_k(C_4) > k^2-k+1 when k-1 is a prime power, and R_k(C_4) <= k^2+k+1 for all k. The hub's linked OEIS sequence A389313 (Faudree–Schelp 1974, Rosta 1973) is CLOSED — no record lane there. Adjacent machine-shaped record surface lives in Radziszowski DS1's multicolour cycle cells (not verified here). Problem page marks this as not …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `ramsey theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P555` and the surface id in your receipt so the next agent's graph build finds your result.


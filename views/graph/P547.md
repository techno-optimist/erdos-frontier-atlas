# Erdős #547 — attack card

> Erdős problem #547 · https://www.erdosproblems.com/547
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `decidable`, finite handle `decidable`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:triage:547` — triage verdict **MAYBE** (finite_handle_triage, 2026-07-26)

- our summary: If T is a tree on n vertices then R(T,T) <= 2n-2.
- witness: CONCRETE: a tree T on n vertices together with a red/blue colouring of the edges of K_{2n-2} containing no monochromatic copy of T. Verification is exact subgraph-containment of a 9-vertex tree in a 16-vertex graph — at most 16!/7! ~ 4.2x10^9 embeddings, seconds with pruning, pure integer arithmetic, no floats, no CAS.
- smallest untested case: n = 9: the 47 unlabelled trees on 9 vertices (A000055), each tested against K_16 (2n-2 = 16). DS1's exhaustive tables stop at order 8.
- known frontier: Radziszowski, Small Ramsey Numbers DS1 rev. 17 (2024), section 5.4 item (p): "Extensive tables of R(T_m,T_n) for 6 <= m,n <= 8, for many concrete pairs of trees", obtained by adiabatic quantum optimization [RanMCG = arXiv:1606.01078], whose Table 16 covers all 23 trees of order 8. Theory: Zhao [Zh11] proved R(T)<=2n-2 for large n via his resolution of the Loebl-Komlos-Sos conjecture (regularity-based, threshold not explicit); Ajtai-Komlos-Simonovits-Szemeredi announced a full proof for large n, …[truncated; see source]
- search-space estimate: n=9: 47 trees x 2^C(16,2) = 47 x 2^120 ~ 6x10^37 colourings of K_16 — direct enumeration impossible; the only route is CEGAR/SMS SAT over 120 variables with S_16 symmetry breaking, since a direct encoding needs ~4x10^9 no-mono-T clauses per colour. Number of CEGAR blocking clauses required is UNKNOWN.
- verification: True
- verdict reason: The only one of the five that is not obviously dead, and I am flagging it as MAYBE rather than TARGET deliberately. In its favour: the witness is fully concrete, verification is cheap, exact and dependency-free (tree containment in a 16-vertex graph), the frontier is genuinely at order 9 (DS1 stops at order 8), 47 trees is a small outer loop, and 120 Boolean variables is a size where SMS-style co-certificate learning is the natural and proven tool — the same machinery that cleared EFL n<=12. Against it, honestly: (a) closing n=9 does NOT resolve #547, because Zhao's and AKSS's thresholds are non-explicit, so this is an uncomputed-table-cell result in our records lane, not a problem closure …[truncated; see source]
- applies: **finite-handle witness search** (predicate `triage-move`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `ramsey theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P547` and the surface id in your receipt so the next agent's graph build finds your result.


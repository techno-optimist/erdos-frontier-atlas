# Erdős #928 — attack card

> Erdős problem #928 · https://www.erdosproblems.com/928
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: natural density of {n : P(n) < n^α and P(n+1) < (n+1)^β} for fixed α,β ∈ (0,1) — …[truncated; see source])** — State of the art (per erdosproblems.com/928, paraphrased; page last edited 2026-04-03): Teräväinen [Te18] proved the LOGARITHMIC density exists and equals ρ(1/α)ρ(1/β); Wang [Wa21] gets the natural density conditionally on Elliott–Halberstam for friable integers; single-condition density is Dickman 1930. Open content: unconditional natural density. Infinitude of simultaneous smooth pairs follows from Schinzel [Sc67b] (Meza observation). No record lane. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:928:463b5358` — natural density of {n : P(n) < n^α and P(n+1) < (n+1)^β} for fixed α,β ∈ (0,1) — existence (conjectured value …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — density-existence question; no finite computation or witness object bears on it
- witness verifier: n/a — individual smooth pairs are trivially checkable (factor n, n+1 via A006530 logic) but carry no weight
- notes: State of the art (per erdosproblems.com/928, paraphrased; page last edited 2026-04-03): Teräväinen [Te18] proved the LOGARITHMIC density exists and equals ρ(1/α)ρ(1/β); Wang [Wa21] gets the natural density conditionally on Elliott–Halberstam for friable integers; single-condition density is Dickman 1930. Open content: unconditional natural density. Infinitude of simultaneous smooth pairs follows from Schinzel [Sc67b] (Meza observation). No record lane.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #683 shares A006530 → card `views/graph/P683.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P928` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #170 — attack card

> Erdős problem #170 · https://www.erdosproblems.com/170
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/170.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:170:b859ac9b` — A046693(214) — minimal number of marks in a sparse ruler / restricted difference basis for {0..214} (first cell beyond …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 22 (counting bound: C(k,2) >= 214 forces k >= 22) · upper 26 (one-step bound a(n+1) <= a(n)+1 (OEIS A046693 comment) with a(213)=25 from Pegg's b-file)
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: S subset of {0..214} with |S| <= 25 whose pairwise differences cover every value 1..214; establishes a(214) <= |S|. Expected truth is 25 or 26: the excess of a(n) over round(sqrt(3n+9/4)) is 0 or 1 for every verified n (round(sqrt(3*214+9/4)) = 25).
- witness verifier: Compute all |S|^2 pairwise differences and check {1..214} is covered; <10^5 integer ops, pure python.
- notes: The named problem (Erdős-Gál 1948, answering Rédei) is the limit constant lim a(n)/sqrt(n), proven to exist and known to lie in [1.56, sqrt(3)=1.73205...]: lower Leech 1956, upper Wichmann 1963; Pegg's computations [Pe20] give evidence the Wichmann upper bound is the truth. Finite witnesses cannot move the limit's bounds — but each new exactly-closed cell extends the excess-conjecture evidence (cf. A308766 divergence list, A326499 best-known solutions). Beware non-monotonicity: a(137) < a(136) …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `additive combinatorics`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P170` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #731 — attack card

> Erdős problem #731 · https://www.erdosproblems.com/731
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Asymptotics of m(n) = least integer not dividing binomial(2n,n) (A006197): find f(n) with …[truncated; see source])** — EGRS 1975 state it is not hard to show m(n) = exp((log n)^{1/2+o(1)}) for almost all n; the open content is pinning a genuine asymptotic f(n). All values are prime powers (OEIS comment, corrected Eldar 2024). Data-rich but not gap-shaped: no bounded quantity with a cheap witness. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:731:252ea3e4` — Asymptotics of m(n) = least integer not dividing binomial(2n,n) (A006197): find f(n) with m(n) ~ f(n) for almost all n (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — the problem asks for an almost-everywhere asymptotic formula; A006197 is already computed to n=10000 (b-file, T. D. Noe) and further table extension does not move the question
- witness verifier: n/a (no finite witness changes the epistemic state; the EGRS claim m = exp((log n)^{1/2+o(1)}) for almost all n is an analytic statement)
- notes: EGRS 1975 state it is not hard to show m(n) = exp((log n)^{1/2+o(1)}) for almost all n; the open content is pinning a genuine asymptotic f(n). All values are prime powers (OEIS comment, corrected Eldar 2024). Data-rich but not gap-shaped: no bounded quantity with a cheap witness.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `binomial coefficients`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P731` and the surface id in your receipt so the next agent's graph build finds your result.


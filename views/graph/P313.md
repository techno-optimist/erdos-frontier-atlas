# Erdős #313 — attack card

> Erdős problem #313 · https://www.erdosproblems.com/313
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Count of known primary pseudoperfect numbers (N>1 with 1/N + sum_{p|N} 1/p = 1); each is …[truncated; see source])** — Known terms have exactly n distinct prime factors for n<9; new terms come from structured searches (Wang, 'Port Fillings for Primary Pseudoperfect Numbers', arXiv:2605.21518, 2026), not bounded sweeps — the 9th/10th terms are 31 and 38 digits. The two 2026 terms are known terms but not certified as the in-order a(9)/a(10) (they were moved out of the data field), so the ordered next-cell is also open. Infinitude cannot fall to computation. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/313.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Are there infinitely many pairs `(m, P)` where `m ≥ 2` is an integer and `P` is a set of distinct primes such that the following equation holds: $\sum_{p \in P} \frac{1}{p} = 1 - \frac{1}{m}$?

## ATTACK SURFACES — which overlay said so

### `S:gap:313:031c16ae` — Count of known primary pseudoperfect numbers (N>1 with 1/N + sum_{p|N} 1/p = 1); each is the modulus m of a solution to …[truncated; see source] (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower 10 (OEIS A054377: 8 terms in data (2..5998279018951962402) plus 8490421583559688410706771261086 and 35979351189199316534587473905773572006 (Martins & Wang, Apr 2026; confirmed in Irvine comment May 2026); erdosproblems.com/313 still says 8 known (stale)) · upper — (—)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: An 11th primary pseudoperfect number N, supplied together with its complete prime factorization p_1 < ... < p_k
- witness verifier: Check each p_i is prime (BPSW/Miller-Rabin), the p_i are distinct, prod p_i = N (terms are squarefree), and 1 + sum_i N/p_i = N in exact integer arithmetic — milliseconds even for 40-digit N, dependency-free
- notes: Known terms have exactly n distinct prime factors for n<9; new terms come from structured searches (Wang, 'Port Fillings for Primary Pseudoperfect Numbers', arXiv:2605.21518, 2026), not bounded sweeps — the 9th/10th terms are 31 and 38 digits. The two 2026 terms are known terms but not certified as the in-order a(9)/a(10) (they were moved out of the data field), so the ordered next-cell is also open. Infinitude cannot fall to computation.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `unit fractions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P313` and the surface id in your receipt so the next agent's graph build finds your result.


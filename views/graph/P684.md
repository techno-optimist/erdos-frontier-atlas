# Erdős #684 — attack card

> Erdős problem #684 · https://www.erdosproblems.com/684
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Growth order of f(n) = least k such that the k-smooth part u of binom(n,k) exceeds n^2 …[truncated; see source])** — HOT in 2026 — recheck before any use as best-known. Superseded: Tang-ChatGPT f(n) <= n^{30/43+o(1)} (and 2/3 on RH). Newer preprints (both unrefereed, found in fresh search, not yet on the curated page as of its 2026-04-01 edit): Bae arXiv:2604.23784 claims limsup f(n)/log n = infinity (would strengthen the lower side beyond any C log n); Li arXiv:2606.08216 claims density-one asymptotic f(n) = (2/(1-gamma)+o(1))·log n ≈ 4.7305 log n for a.e. n. Mahler's theorem gives f(n)->infinity …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:684:bd2a5397` — Growth order of f(n) = least k such that the k-smooth part u of binom(n,k) exceeds n^2 (A392019) (gap_map, kind `value_gap`, confidence C3)

- bounds: lower f(n) >= (1/2 - o(1))·log n for infinitely many n (Alexeev-Putterman-Sawhney-Sellke-Valiant [APSSV26] as cited in erdosproblems.com/684 remarks (elementary construction; argument credited to an internal OpenAI model)) · upper f(n) = O((log n)^2) for all n (Alexeev-Putterman-Sawhney-Sellke-Valiant [APSSV26] as cited in erdosproblems.com/684 remarks)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None for the asymptotic gap. Per-n f(n) is exactly computable (I reproduced A392019(1..25) from the definition), so individual values are table cells, not records; a specimen table of record f(n)/log n values is possible but moves no proven bound.
- witness verifier: n/a for the bounds; a claimed value f(n)=k is checked by extracting prime valuations of binom(n,k) for p <= k (Legendre sums, no factoring) and verifying u(n,k) > n^2 while u(n,k-1) <= n^2.
- notes: HOT in 2026 — recheck before any use as best-known. Superseded: Tang-ChatGPT f(n) <= n^{30/43+o(1)} (and 2/3 on RH). Newer preprints (both unrefereed, found in fresh search, not yet on the curated page as of its 2026-04-01 edit): Bae arXiv:2604.23784 claims limsup f(n)/log n = infinity (would strengthen the lower side beyond any C log n); Li arXiv:2606.08216 claims density-one asymptotic f(n) = (2/(1-gamma)+o(1))·log n ≈ 4.7305 log n for a.e. n. Mahler's theorem gives f(n)->infinity …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`, `binomial coefficients`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P684` and the surface id in your receipt so the next agent's graph build finds your result.


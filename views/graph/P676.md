# Erdős #676 — attack card

> Erdős problem #676 · https://www.erdosproblems.com/676
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Infinitude of A390181 — integers not representable as a*p^2 + b with p prime, a >= 1, 0 …[truncated; see source])** — From the problem page (paraphrased): the sieve of Eratosthenes gives that almost all integers are representable; Brun–Selberg sieve bounds exceptions in [1,x] by << x/(log x)^c. Erdős [1979] called it 'rather unlikely' that all large integers are representable — i.e. he leaned toward the exception set A390181 being infinite (the OEIS comment echoes 'seems likely' infinite). Empirically the exception set is robustly populous: b-file has 100000 terms, the 100000th being 1,211,719 (~8.3% density …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:676:42428c7b` — Infinitude of A390181 — integers not representable as a*p^2 + b with p prime, a >= 1, 0 <= b < p (equivalently: is …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None: each individual exception is trivially certifiable (n is an exception iff n mod p^2 >= p for every prime p <= sqrt(n)), but no finite list of exceptions settles infinitude, and no finite computation proves the 'every large n' direction.
- witness verifier: Per-exception check is cheap and dependency-free (trial over primes p <= sqrt(n), test n mod p^2 < p), but it moves nothing: the open content is asymptotic.
- notes: From the problem page (paraphrased): the sieve of Eratosthenes gives that almost all integers are representable; Brun–Selberg sieve bounds exceptions in [1,x] by << x/(log x)^c. Erdős [1979] called it 'rather unlikely' that all large integers are representable — i.e. he leaned toward the exception set A390181 being infinite (the OEIS comment echoes 'seems likely' infinite). Empirically the exception set is robustly populous: b-file has 100000 terms, the 100000th being 1,211,719 (~8.3% density …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P676` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #114 — attack card

> Erdős–Herzog–Piranian lemniscate max length · https://www.erdosproblems.com/114
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **TRAP** — prize **$250** + upstream says `falsifiable`, but our status register records a **wall**: the finite handle does not make it reachable here (reasons in atlas/walls.md, finite-handle table). Decidable in principle is not reachable in practice.
- **WALL (problem)** — transcendental objective (elliptic-integral arc length); no exact/DRAT verifier exists; Tao proved the conjecture for large n (Dec 2025) [atlas/problems.json wall_reason]
- **WALL (surface: For monic p in C[z] of degree n, is the length of the lemniscate {z : |p(z)|=1} maximised …[truncated; see source])** — The existing atlas wall is CORRECT and got stronger since it was set. Fails criterion 3 outright: confirming a candidate means computing the length of {|p|=1} to certified precision, i.e. rigorous numerical integration with interval arithmetic on an implicit curve — the exact floating-point dependency we refuse to ship. Fails criterion 2 in spirit: the space is a non-compact 3-real-parameter continuum at n=3 and a 2n-parameter one in general, and Fryntov-Nazarov already proved z^n-1 is a local maximiser, so any counterexample is a distant competing local max that local search will not stumble onto. And the payoff is nil: Tao closed all large n in Dec 2025 with an unstated n_0, so even a …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **wall** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: not in formal-conjectures
- prize: $250 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:triage:114` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: For monic p in C[z] of degree n, is the length of the lemniscate {z : |p(z)|=1} maximised by p(z) = z^n - 1? Not formalised (Lean file absent — 404 in google-deepmind/formal-conjectures).
- witness: Semi-concrete: a single monic polynomial of degree n whose lemniscate is LONGER than that of z^n-1. Finitely many complex coefficients — but the discriminating quantity is an arc length, a transcendental integral, not a combinatorial count.
- smallest untested case: n = 3. Eremenko-Hayman [ErHa99] proved the full conjecture for n=2; Tao [Ta25] (arXiv:2512.12455v2, Dec 2025, 56 pp.) proved z^n-1 is the unique maximiser up to rotation/translation for all SUFFICIENTLY LARGE n. Crucially, the abstract states no explicit threshold n_0, so the residual window is not even a known finite range of n — the same ineffective-n_0 pattern we already logged on Sendov.
- known frontier: Purely analytic, no computational sweep reported anywhere. Dolzhenko [Do61] f(n) <= 4*pi*n; Pommerenke [Po61] f(n) << n^2; Borwein [Bo95] f(n) << n; Eremenko-Hayman [ErHa99] f(n) <= 9.173n and n=2 exactly; Danchenko [Da07] f(n) <= 2*pi*n; Fryntov-Nazarov [FrNa09] proved z^n-1 is a LOCAL maximiser and f(n) <= 2n + O(n^{7/8}); Tao [Ta25] all sufficiently large n. Nobody has published a numerical search over coefficient space, which is itself telling.
- search-space estimate: n=3: monic cubic modulo translation (kill the z^2 term) and rotation gives p(z)=z^3+az+b with a,b in C and one residual real symmetry — about 3 free real parameters, and the region is NON-compact. Small in dimension, but the objective at each point is a certified arc length of an implicit algebraic curve, so there is no primitive-operation count to quote: UNKNOWN as a discrete search, because it …[truncated; see source]
- verification cheap and exact: no
- verdict reason: The existing atlas wall is CORRECT and got stronger since it was set. Fails criterion 3 outright: confirming a candidate means computing the length of {|p|=1} to certified precision, i.e. rigorous numerical integration with interval arithmetic on an implicit curve — the exact floating-point dependency we refuse to ship. Fails criterion 2 in spirit: the space is a non-compact 3-real-parameter continuum at n=3 and a 2n-parameter one in general, and Fryntov-Nazarov already proved z^n-1 is a local maximiser, so any counterexample is a distant competing local max that local search will not stumble onto. And the payoff is nil: Tao closed all large n in Dec 2025 with an unstated n_0, so even a …[truncated; see source]
- no move applies mechanically

### `S:deep:114` — deep audit: **WALL**, lane `wall`, board **NONE** (problems.json)

- finite object: None in our sense. The "object" is a monic polynomial in continuous coefficient space ℂ^{n−1}; the frontier is a transcendental arc-length functional, not a discrete combinatorial witness.
- current record: Conjecture PROVEN for all sufficiently large n (Terence Tao, arXiv 2512.12455, Dec 2025), via Stokes/area-integral analysis; asymptotic bound ℓ ≤ 2n+4log2+o(1). z^n−1 proven a LOCAL maximum (Fryntov–Nazarov). n=2 (Bernoulli lemniscate) proven maximal (Eremenko–Hayman). n=3, n=4 remain OPEN. Conjectured max = B(1/2,n/2)=2n+4log2+O(1/n).
- verifier: Compute arc length of the level set {|p(z)|=1}: trace the (possibly multi-oval) curve and integrate ds numerically. NOT poly-time-exact — it is transcendental; rigorous certification would need interval-arithmetic enclosures of an elliptic-type integral over the whole level set. Cost: cheap for a heuristic float estimate, expensive and non-standard for a certified enclosure; no DRAT analog exists.
- attack: The only in-family attack is SA/local-search over monic coefficients maximizing numerically-estimated lemniscate length per small degree (n=3,4,5), hoping to beat ℓ(z^n−1). Rough compute: trivial (hours on a few cores) to run, but near-zero expected value — the objective's global max is a proven local max and the conjecture is proven for large n, so a "hit" is a lottery; and any numerical hit could not be exactly certified, only motivate a separate validated-numerics proof (branch-and-bound with rigorous integral enclosures over ℂ^{n−1}), which is a different discipline from our exact-witness farm.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `polynomials`, `analysis`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P114` and the surface id in your receipt so the next agent's graph build finds your result.


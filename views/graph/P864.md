# Erdős #864 — attack card

> Erdős problem #864 · https://www.erdosproblems.com/864
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:864:b17f400c` — A389182(101) — max size of A ⊆ {1..101} such that at most one integer has ≥2 representations as a+b (a≤b∈A); first …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 16 (monotonicity from OEIS A389182 b-file a(100)=16 (David Spencer)) · upper 17 (increment bound a(n+1) ≤ a(n)+1 (deleting the element n+1 preserves validity; derived here))
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: a 17-element subset of {1..101} in which at most one sum value has multiple representations (pins a(101)=17; any exhaustive search closing the cell also counts)
- witness verifier: O(|A|^2): build multiset {a+b : a≤b∈A}, count values with multiplicity ≥2, accept iff ≤1 such value; pure-python, milliseconds
- notes: Sequence created Oct 2025 — everything past n=100 is fresh territory; branch-and-bound/ILP plausibly extends the table well past 150 in hours. Asymptotic layer (the actual Erdős question): Erdős–Freud [ErFr91] construction gives liminf a(N)/√N ≥ 2/√3 ≈ 1.1547 (Sidon set in [1,N/3] ∪ its reflection); conjectured tight. One-exception sets are quasi-Sidon (only one colliding sum ⇒ |A+A| ~ |A|²/2, containment derived here), so Pikhurko's #840 upper bound (1/4+1/(π+2)²)^{-1/2} ≈ 1.8626 applies …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `sidon sets`, `additive combinatorics`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P864` and the surface id in your receipt so the next agent's graph build finds your result.


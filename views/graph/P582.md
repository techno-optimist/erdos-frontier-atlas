# Erdős #582 — attack card

> Folkman number Fe(3,3;4) · https://www.erdosproblems.com/582
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **solved-upstream** (atlas/stubs.json)
- upstream: **Solved** (raw `proved (Lean)`)
- formal: not in formal-conjectures
- prize: $100 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:deep:582` — deep audit: **UNKNOWN**, lane `witness-local-search`, board **HEAVY** (problems.json)

- finite object: A finite graph G on n vertices that (a) contains no K4 and (b) arrows (K3,K3) edge-wise. Beating the record = exhibiting such G with n < 786; the lower-bound direction = certifying no such G exists on n ≤ 21, 22, ...
- current record: 21 ≤ Fe(3,3;4) ≤ 786. Upper bound 786 = Lange, Radziszowski & Xu 2012 (explicit algebraic/Ramsey-based construction; prior chain: Lu 9697 in 2007, Dudek-Rödl 941 in 2008). Lower bound 21 = Bikov & Nenov 2020 (heavy independence-number computation on (3,3)-Ramsey graphs). Graham offers $100 for N ≤ 100; Radziszowski-Xu speculate N ≤ 127. No improvement to 786 in 14 years as of July 2026.
- frontier: 21 <= Fe(3,3;4) <= 786 (Lange-Radziszowski-Xu 2012); Graham's $100 for a K4-free graph <= 100 vertices arrowing (3,3)
- verifier: K4-free: trivial poly check (enumerate 4-subsets or clique-free test). Arrowing: encode "there EXISTS a 2-edge-coloring with no monochromatic triangle" as SAT (one Boolean per edge; for each triangle two clauses forbidding all-red and all-blue). G arrows iff that formula is UNSAT. UNSAT is DRAT-certifiable. Cost: one large CDCL UNSAT per candidate — comparable scale to the R(5,5) window-UNSATs we already ran; hours-to-days for a ~200-800-vertex graph, DRAT check linear in proof size.
- attack: (1) Build the arrowing verifier: SAT-encode "no good 2-coloring", solve with a modern CDCL solver (CaDiCaL/Kissat), emit DRAT — reuse of the exact R(5,5) pipeline. (2) Generator is the bottleneck; do NOT blind-SA unstructured graphs. Instead take the 2025 Hermitian-unital seeds (H_3 = 63v, H_4 = 208v and their triangle systems) as structured candidates, then run SAT/MaxSAT-guided vertex/edge deletion that greedily shrinks the seed while re-verifying K4-free + arrowing (UNSAT) at each step — a verifier-in-the-loop minimization rather than a global search. (3) In parallel, run the known 786 LRX graph through the same minimizer to test whether local surgery + DRAT re-cert shaves vertices. …[truncated; see source]
- applies: **witness local search** (predicate `lane-move`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `ramsey theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P582` and the surface id in your receipt so the next agent's graph build finds your result.


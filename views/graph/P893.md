# Erdős #893 — attack card

> Erdős problem #893 · https://www.erdosproblems.com/893
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: f(2n)/f(n) where f(n) = Σ_{k≤n} τ(2^k−1) (τ over A046801) — does the ratio tend to a …[truncated; see source])** — Kovač–Luca (arXiv:2506.04883, 2025) proved the ratio sequence f(2n)/f(n) is unbounded, ruling out every finite limit, with conditional results and numerics supporting lim = ∞ (building on a Cambie heuristic, per erdosproblems.com/893). Remaining open content: whether the limit exists (=∞) — an asymptotic wall, no record lane. Lean-formalized (research open) in formal-conjectures. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/893.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Does the limit $\lim_{n\to\infty} \frac{f(2n)}{f(n)}$ tend to infinity? (Other finite limits have been ruled out by [KoLu25], see below)

## ATTACK SURFACES — which overlay said so

### `S:gap:893:fc2106b0` — f(2n)/f(n) where f(n) = Σ_{k≤n} τ(2^k−1) (τ over A046801) — does the ratio tend to a limit? (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — limit-existence question; no finite witness decides it, and extending A046801 requires factoring Mersenne numbers (Cunningham-project-hard beyond the b-file's n=1206)
- witness verifier: n/a — verifying a new τ(2^k−1) value needs a certified complete factorization of 2^k−1, which is not a cheap check at the open frontier
- notes: Kovač–Luca (arXiv:2506.04883, 2025) proved the ratio sequence f(2n)/f(n) is unbounded, ruling out every finite limit, with conditional results and numerics supporting lim = ∞ (building on a Cambie heuristic, per erdosproblems.com/893). Remaining open content: whether the limit exists (=∞) — an asymptotic wall, no record lane. Lean-formalized (research open) in formal-conjectures.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `divisors`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P893` and the surface id in your receipt so the next agent's graph build finds your result.


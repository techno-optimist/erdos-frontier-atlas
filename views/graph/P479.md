# Erdős #479 — attack card

> Erdős problem #479 · https://www.erdosproblems.com/479
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/479.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is it true that, for all $k\neq 1$, there are infinitely many $n$ such that $2^n\equiv k\pmod{n}$?

## ATTACK SURFACES — which overlay said so

### `S:gap:479:9085dbe5` — A036236(1119) — least n with 2^n ≡ 1119 (mod n); first of 93 unknown entries among k <= 10^4 (Graham's conjecture: for …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower > 1e13 (Nick Hobson search table (OEIS A036236 a-file a036236.log.txt, dated Jan 29 2024, supersedes Havermann): all 93 unknown entries for k <= 10000 exceed 10^13) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `unknown`
- witness object: any integer n (necessarily > 10^13) with 2^n mod n = 1119; identical shape for each of the other 92 open residues k <= 10^4 (next few: 1137, 1179, 1299, 1329, 1455, 1459, ...)
- witness verifier: one modular exponentiation: square-and-multiply 2^n mod n, O(log n) mulmods; microseconds even for n ~ 10^18; fully dependency-free
- notes: Known terms can be astronomically larger than k (a(3)=4700063497). Structured CRT/multiplicative-order constructions (Crump/Alekseyev style) can leapfrog the sieve — the 93 residues survived structured searches to 10^13, so not open-easy. A witness gives only an upper bound on the least n; exactness requires closing the exhaustive scan below it (same compute class as the existing 10^13 sieve if the witness lands near it, unbounded in principle — hence exact=unknown). k=1 provably impossible …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P479` and the surface id in your receipt so the next agent's graph build finds your result.


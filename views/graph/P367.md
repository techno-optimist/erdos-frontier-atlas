# Erdős #367 — attack card

> Erdős problem #367 · https://www.erdosproblems.com/367
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: limsup behavior of prod_{n <= m < n+k} B_2(m) / n^2 for fixed k >= 3, where B_2 = …[truncated; see source])** — van Doorn (per erdosproblems, page edited 2026-03-23): for k <= 2 trivially << n^2, but the clean <<_k n^2 version FAILS for all k >= 3 — prod over [n, n+2] is >> n^2 log n infinitely often. Equivalent up to constants to #935. Empirical sub-lane a scout could run: hunt record values of prod_{m in [n, n+3)} B_2(m)/n^2 via CRT constructions (large squares dividing three consecutive integers) — each example cheaply verifiable but moves no bound. r-full generalization (B_r, r >= 3) also open. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:367:f286a3b6` — limsup behavior of prod_{n <= m < n+k} B_2(m) / n^2 for fixed k >= 3, where B_2 = powerful part (A057521) — conjectured …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none that bounds the conjecture — it is an asymptotic << statement; individual n with record products are verifiable curios but no finite set decides
- witness verifier: n/a for the conjecture itself; any single example (n, k) is checkable by factoring k integers and multiplying their powerful parts
- notes: van Doorn (per erdosproblems, page edited 2026-03-23): for k <= 2 trivially << n^2, but the clean <<_k n^2 version FAILS for all k >= 3 — prod over [n, n+2] is >> n^2 log n infinitely often. Equivalent up to constants to #935. Empirical sub-lane a scout could run: hunt record values of prod_{m in [n, n+3)} B_2(m)/n^2 via CRT constructions (large squares dividing three consecutive integers) — each example cheaply verifiable but moves no bound. r-full generalization (B_r, r >= 3) also open.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #935 shares A057521 → card `views/graph/P935.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P367` and the surface id in your receipt so the next agent's graph build finds your result.


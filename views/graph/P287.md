# Erdős #287 — attack card

> Erdős problem #287 · https://www.erdosproblems.com/287
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: If 1 = 1/n_1 + ... + 1/n_k with 1<n_1<...<n_k distinct, must max(n_{i+1}-n_i) >= 3?)** — Fails criterion 4 for the opposite of the usual reason: the finite search is trivially cheap but empty. For any prime p in [a,b] with floor(b/p)=m small, every multiple of p must be EXCLUDED from S (no nonempty subsum of 1/1..1/m has numerator divisible by p once p > m·lcm(1..m)), yet the excluded set may contain no two consecutive integers. Two forced-adjacent exclusions kill the interval, and that fires for every b in [37, 30000] I tested, with growing slack. This is exactly the page's own remark that a prime p in [N,2N] with (p+1)/2 prime settles it. The residual obstruction is a twin-prime-strength theorem, not compute — no amount of searching produces a witness. [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:triage:287` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: If 1 = 1/n_1 + ... + 1/n_k with 1<n_1<...<n_k distinct, must max(n_{i+1}-n_i) >= 3?
- witness: Concrete: a finite set S of integers with min a, max b, every consecutive gap <= 2, and sum of reciprocals exactly 1. Equivalently S = [a,b] \ T where T contains no two consecutive integers. Verification is one exact rational sum — trivially cheap and exact.
- smallest untested case: None below b = 30000. I closed b <= 36 by exhaustive exact DFS over Fractions (0 counterexamples) and refuted every 37 <= b <= 30000 by the p-adic forced-exclusion sieve. Smallest genuinely untested case is max element b = 30001.
- known frontier: No computational bound is recorded anywhere upstream — the remarks cite only Erdős [Er32] (the all-gaps-equal-1 case: 1 is not a sum of reciprocals of consecutive integers) and a conditional reduction to a prime-pair statement. I established the frontier myself this session: no counterexample with max element <= 30000 (scratchpad p287b.py, ~2 min single core).
- search-space estimate: Cost is NOT the obstacle: one linear sieve per candidate top element b, ~b log log b ops, so b <= 1e9 is ~1e10 ops. The exponential fallback (Fibonacci-many independent subsets T, ~1.618^{b-a}) is never reached — it was needed for 0 of the 30000 values of b I scanned.
- verification cheap and exact: yes
- verdict reason: Fails criterion 4 for the opposite of the usual reason: the finite search is trivially cheap but empty. For any prime p in [a,b] with floor(b/p)=m small, every multiple of p must be EXCLUDED from S (no nonempty subsum of 1/1..1/m has numerator divisible by p once p > m·lcm(1..m)), yet the excluded set may contain no two consecutive integers. Two forced-adjacent exclusions kill the interval, and that fires for every b in [37, 30000] I tested, with growing slack. This is exactly the page's own remark that a prime p in [N,2N] with (p+1)/2 prime settles it. The residual obstruction is a twin-prime-strength theorem, not compute — no amount of searching produces a witness.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `unit fractions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P287` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #1054 — attack card

> Erdős problem #1054 · https://www.erdosproblems.com/1054
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Asymptotics of f(n) = A167485(n) = least m such that n equals the sum of the k smallest …[truncated; see source])** — Guy B2. Proven: f undefined exactly at n=2 and n=5 among small n (Lean textbook theorems in formal-conjectures 1054.lean); OEIS comment conjectures 2 and 5 are the only zeros, which would follow from a strengthened Goldbach statement (every even N > 22 is p+q with 5 < p < q < 5p). Robert Israel: liminf f(n)/n = 0 via f(sigma(n)) <= n. Computed range: b-file to n=20000 (Israel), f(n)/n plotted to 10^6 (OEIS link) - extending these is easy but moves no bound. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/1054.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:1054:9174edf6` — Asymptotics of f(n) = A167485(n) = least m such that n equals the sum of the k smallest divisors of m: is f(n) = o(n) …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none - all three open parts (formal-conjectures 1054.lean parts i-iii) are asymptotic statements not settled by any finite certificate
- witness verifier: n/a. A soft evidence lane exists: record values of f(n)/n (largest observed ~4.5 at n=59, f=265, within OEIS data; each value certifiable by checking every m' < f(n) fails, O(n * f(n)) divisor-sum work) would support limsup = infinity, but no finite record bounds anything.
- notes: Guy B2. Proven: f undefined exactly at n=2 and n=5 among small n (Lean textbook theorems in formal-conjectures 1054.lean); OEIS comment conjectures 2 and 5 are the only zeros, which would follow from a strengthened Goldbach statement (every even N > 22 is p+q with 5 < p < q < 5p). Robert Israel: liminf f(n)/n = 0 via f(sigma(n)) <= n. Computed range: b-file to n=20000 (Israel), f(n)/n plotted to 10^6 (OEIS link) - extending these is easy but moves no bound.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #468 shares A167485 → card `views/graph/P468.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1054` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #17 — attack card

> Erdős problem #17 · https://www.erdosproblems.com/17
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Are there infinitely many cluster primes (odd p such that every even number <= p-3 is a …[truncated; see source])** — Blecksmith–Erdős–Selfridge (Amer. Math. Monthly 106 (1999) 43–48) proved cluster primes are sparse: pi_C(x) <<_A x/(log x)^A for every A, and the sum of reciprocals of cluster primes converges (direction confirmed via MathWorld ClusterPrime). Elsholtz (2003) improved to << x*exp(-c(log log x)^2) for every c < 1/8 (per erdosproblems.com/17). First non-cluster prime is 97; A038133 lists non-cluster primes (b-file 10000 terms, T. D. Noe). Tao has flagged the problem as difficult on the …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/17.lean)

## STATEMENT (Apache-licensed, Lean-derived)

**Erdős Problem 17.** Are there infinitely many cluster primes?

## ATTACK SURFACES — which overlay said so

### `S:gap:17:01a96de6` — Are there infinitely many cluster primes (odd p such that every even number <= p-3 is a difference of two primes both …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — a single additional cluster prime is poly-time checkable (sweep all prime differences below p) but moves no recorded bound; infinitude admits no finite witness
- witness verifier: Membership check for one p: sieve primes <= p, mark all pairwise differences, verify every even number <= p-3 is marked — O(pi(p)^2) but trivial for any p a search could reach. No maintained largest-cluster-prime record exists to improve.
- notes: Blecksmith–Erdős–Selfridge (Amer. Math. Monthly 106 (1999) 43–48) proved cluster primes are sparse: pi_C(x) <<_A x/(log x)^A for every A, and the sum of reciprocals of cluster primes converges (direction confirmed via MathWorld ClusterPrime). Elsholtz (2003) improved to << x*exp(-c(log log x)^2) for every c < 1/8 (per erdosproblems.com/17). First non-cluster prime is 97; A038133 lists non-cluster primes (b-file 10000 terms, T. D. Noe). Tao has flagged the problem as difficult on the …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P17` and the surface id in your receipt so the next agent's graph build finds your result.


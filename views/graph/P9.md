# Erdős #9 — attack card

> Erdős problem #9 · https://www.erdosproblems.com/9
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Upper density of the set of odd numbers not representable as p + 2^a + 2^b (p prime) — is …[truncated; see source])** — State of knowledge (paraphrased from erdosproblems.com/9, edited Apr 2026): Crocker (Pacific J. Math 36, 1971) proved infinitely many such odd integers, with >> log log N of them below N; Pan (arXiv:0905.3809) improved the count to >>_eps N^(1-eps) for every eps>0. The remaining gap — from exponent 1-eps to positive density — is theorem-shaped, not witness-shaped; Erdős believed covering-system arguments cannot settle it (representable numbers exist in every infinite AP). A006286 (terms …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research solved, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/9.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is the upper density of the set of odd numbers that cannot be expressed as a prime plus two powers of 2 positive?

## ATTACK SURFACES — which overlay said so

### `S:gap:9:1fe0cca0` — Upper density of the set of odd numbers not representable as p + 2^a + 2^b (p prime) — is it positive? (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — individual non-representable odd numbers are cheap to verify (A006286 already has a 10000-term b-file) but no finite set of them moves the density question
- witness verifier: A single term t is verifiable dependency-free: for every pair 2^a+2^b < t test t-2^a-2^b for compositeness. But the open content is the density exponent, which no finite witness certifies.
- notes: State of knowledge (paraphrased from erdosproblems.com/9, edited Apr 2026): Crocker (Pacific J. Math 36, 1971) proved infinitely many such odd integers, with >> log log N of them below N; Pan (arXiv:0905.3809) improved the count to >>_eps N^(1-eps) for every eps>0. The remaining gap — from exponent 1-eps to positive density — is theorem-shaped, not witness-shaped; Erdős believed covering-system arguments cannot settle it (representable numbers exist in every infinite AP). A006286 (terms …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `additive basis`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P9` and the surface id in your receipt so the next agent's graph build finds your result.


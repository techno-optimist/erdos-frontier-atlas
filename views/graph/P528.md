# Erdős #528 — attack card

> Erdős problem #528 · https://www.erdosproblems.com/528
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: C_2 — connective constant of the square lattice Z^2 (self-avoiding walk growth constant, …[truncated; see source])** — Numerical value 2.6381585303279... (Jacobsen–Scullard–Guttmann 2016) sits far inside the rigorous bracket — the bracket IS the record. A156816 (positive root of 13x^4-7x^2-581 = 2.63815853034...) is a folklore quartic guess with no known derivation, DOUBTED: disagrees with JSG16 numerics around the 11th decimal (Bauerschmidt et al. 2012 note, in OEIS) — hence exact=wall (no closed form expected). The upper-bound lane looks genuinely movable: Couronné 2022 was a single-author modest-compute …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:528:4aa7fcf0` — C_2 — connective constant of the square lattice Z^2 (self-avoiding walk growth constant, A387897): rigorous bracket vs …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower 2.625622 (Jensen 2004, bridge enumeration (rigorous via bridge supermultiplicativity); quoted as best rigorous lower bound in Bauerschmidt–Duminil-Copin–Goodman–Slade lecture notes (arXiv:1206.2092)) · upper 2.662343 (Couronné, arXiv:2211.16146 — modified Pönitz–Tittmann finite-memory automaton with loops to length 26 (improves Pönitz–Tittmann 2000: 2.679193))
- witness side: `both` · witness feasibility `plausible` · exact feasibility `wall`
- witness object: lower: a longer bridge-enumeration certificate (any finite n gives rigorous C_2 >= b_n^{1/n}); upper: a finite-memory automaton plus a spectral certificate vector v>0 with Mv <= lambda*v proving C_2 <= lambda
- witness verifier: upper side: given automaton + v, verify Mv <= lambda*v by sparse mat-vec in rational/interval arithmetic — cheap if v is shipped, though the automaton-dominates-SAW argument itself is paper-proof; lower side: re-running the bridge count (transfer-matrix scale, counts ~10^30) — expensive, not …[truncated; see source]
- notes: Numerical value 2.6381585303279... (Jacobsen–Scullard–Guttmann 2016) sits far inside the rigorous bracket — the bracket IS the record. A156816 (positive root of 13x^4-7x^2-581 = 2.63815853034...) is a folklore quartic guess with no known derivation, DOUBTED: disagrees with JSG16 numerics around the 11th decimal (Bauerschmidt et al. 2012 note, in OEIS) — hence exact=wall (no closed form expected). The upper-bound lane looks genuinely movable: Couronné 2022 was a single-author modest-compute …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P528` and the surface id in your receipt so the next agent's graph build finds your result.


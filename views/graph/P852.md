# Erdős #852 — attack card

> Erdős problem #852 · https://www.erdosproblems.com/852
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:852:987ceb60` — A079007(31) (records chain of A053597/A078515/A079889) — smallest prime starting a run of 31 pairwise-distinct …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower >= 196948778371 (A079007 is nondecreasing and a(30) = 196948778371 (Donovan Johnson, OEIS extension Oct 2012); record table untouched since 2012) · upper — (—)
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: a prime p starting >= 31 pairwise-distinct consecutive prime gaps (any such p upper-bounds A079007(31) and sets a new record for Erdős' h(x) run-length function)
- witness verifier: from p, iterate next_prime 31 times (deterministic Miller–Rabin/BPSW is exact below 2^64), check the 31 gaps are pairwise distinct — microseconds; claiming the exact cell additionally needs an exhaustive segmented sieve below p (primesieve-class, no exotic dependencies)
- notes: Best machine cell in this chunk: the record frontier (~1.97e11) is 2012-era; primesieve sweeps 10^12 in CPU-hours, and a rolling 32-gap distinctness window is trivial code — hours-to-days on one box should extend A079007 by several cells. Orientation: this moves the record table for h(x), NOT the open asymptotics — Erdős asked whether (log x)^c < h(x) and h(x) = o(log x); only h(x) -> infinity is proven (Brun sieve), so the asymptotic problem itself is a wall. Site marks 'claims of partial …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #5 shares A001223 → card `views/graph/P5.md`
- #853 shares A001223 → card `views/graph/P853.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P852` and the surface id in your receipt so the next agent's graph build finds your result.


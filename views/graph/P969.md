# Erdős #969 — attack card

> Erdős problem #969 · https://www.erdosproblems.com/969
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: exponent theta = inf{t : E(x) ≪ x^t} of the error term E(x) = Q(x) − (6/pi^2)x in …[truncated; see source])** — E(x) ≪ x^{1/4+o(1)} would IMPLY RH (per erdosproblems #969), and even under RH the true order is unknown — a definitional analytic wall for the records lane. Believed true order: x^{1/4}. PNT already gives o(x^{1/2}). Schnirelmann-density curiosity in A013928: min a(n)/n = 53/88 at n=176 (Rogers 1964). [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:969:c2325929` — exponent theta = inf{t : E(x) ≪ x^t} of the error term E(x) = Q(x) − (6/pi^2)x in squarefree counting (A013928 is Q's …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower 1/4 (Evelyn–Linfoot: E(x) = Omega(x^{1/4})) · upper 1/2 (elementary; best unconditional is x^{1/2−o(1)}-type savings, Walfisz; on RH: 11/35 + o(1), Liu) (Walfisz 1963 (unconditional); Liu 2016 (conditional on RH), per erdosproblems #969 remarks)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — an asymptotic exponent has no cheap certificate; numeric extreme-value tables of |E(x)|/x^{1/4} (cf. Cohen–Dress–El Marraki: |E(x)| < 0.02767 sqrt(x) for x ≥ 438653) are exploratory only
- witness verifier: n/a for the exponent; E(x) itself is exactly computable in O(sqrt(x)) via the Moebius sum, so data generation is trivial — but no finite computation moves theta
- notes: E(x) ≪ x^{1/4+o(1)} would IMPLY RH (per erdosproblems #969), and even under RH the true order is unknown — a definitional analytic wall for the records lane. Believed true order: x^{1/4}. PNT already gives o(x^{1/2}). Schnirelmann-density curiosity in A013928: min a(n)/n = 53/88 at n=176 (Rogers 1964).
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #121 shares A013928 → no card (registers only: `atlas/stubs.json`)

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P969` and the surface id in your receipt so the next agent's graph build finds your result.


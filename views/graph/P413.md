# Erdős #413 — attack card

> Erdős problem #413 · https://www.erdosproblems.com/413
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/413.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:413:2e955b6c` — A005236(10001) — next barrier for ω (an n with m + ω(m) ≤ n for all m < n); current frontier a(10000) = 2054598 (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 10000 barriers known; a(10000) = 2054598 (OEIS A005236 b-file (C. R. Greathouse IV)) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: a claimed next barrier n0 > 2054598 (or a batch of new terms extending the b-file)
- witness verifier: Segmented smallest-prime-factor sieve computing ω(m) for all m < n0; check max_{m<n0} (m + ω(m)) ≤ n0. O(n0 log log n0) time, dependency-free; seconds at 1e7, minutes at 1e9.
- notes: Infinitude of ω-barriers is OPEN (Erdős believed yes; Guy B8; Erdős–Graham noted sieve methods not yet strong enough). Recent movement: per erdosproblems.com/413 remarks (page edited 2026-04-17), Lau (2026, arXiv:2604.15042) answered the ε-relaxation (∃ε>0 with infinitely many n s.t. m + εω(m) ≤ n for all m < n) positively and proved a weakened main statement with the condition restricted to m ≤ n − C; we checked only the abstract, not the theorem statements — treat as reported. Erdős (1979) …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `iterated functions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P413` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #786 — attack card

> Erdős problem #786 · https://www.erdosproblems.com/786
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Maximal density of A ⊆ {1..N} (resp. A ⊆ N) in which products of r elements never equal …[truncated; see source])** — State (paraphrased from problem page): density version answered negatively — any valid A has density <= 1/2, and upper density <= 1-c with the same c; constructions: Selfridge 1/e - eps, Tao's one-large-prime-factor set ~0.8285 N for the interval version. What stays open is the variant where the products must use distinct elements (Er80 formulation). The attached OEIS entry is just the decimal expansion of the Hall–Montgomery constant (105 digits) — no table to extend, no witness lane. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/786.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:786:213d897b` — Maximal density of A ⊆ {1..N} (resp. A ⊆ N) in which products of r elements never equal products of s elements with r ≠ …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — the extremal constant for the [1,N] version (repetitions allowed) is already identified: max |A| = (1-c+o(1))N with c = Hall–Montgomery constant 0.17150049... (A143301), sharp via Granville–Soundararajan 2001 (per Tao's comment on the problem page); remaining open content (distinct-factors variant, exact density version) is asymptotic with no finite certificate
- witness verifier: n/a (finite sets certify nothing about the o(1) terms; the constant itself is a closed form 1 + pi^2/6 + 2 Li_2(-sqrt(e)))
- notes: State (paraphrased from problem page): density version answered negatively — any valid A has density <= 1/2, and upper density <= 1-c with the same c; constructions: Selfridge 1/e - eps, Tao's one-large-prime-factor set ~0.8285 N for the interval version. What stays open is the variant where the products must use distinct elements (Er80 formulation). The attached OEIS entry is just the decimal expansion of the Hall–Montgomery constant (105 digits) — no table to extend, no witness lane.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #121 shares A143301 → no card (registers only: `atlas/stubs.json`)

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P786` and the surface id in your receipt so the next agent's graph build finds your result.


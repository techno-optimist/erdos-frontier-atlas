# Erdős #390 — attack card

> Erdős problem #390 · https://www.erdosproblems.com/390
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Asymptotic constant c in f(n) − 2n ~ c·n/log n, where f(n) = A193429(n) = minimum …[truncated; see source])** — Erdős–Guy–Selfridge [EGS82] proved f(n) − 2n ≍ n/log n (both order bounds); the open question is whether f(n) − 2n ~ c·n/log n for a specific constant c. The stub's Lean formalization marks 'research solved' — that plausibly reflects the proven ≍ statement; the constant question is the open residue per erdosproblems.com. a(n) = 0 only for n = 1, 2 (Adams-Watters); sequence appears O(n). Empirical c-estimation from an extended table (b-file n <= 168, Kesarwani) is possible scout work but …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research solved, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/390.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Does there exists a constant `c` such that `f n - 2 * n ~ c * (n / log n)`?

## ATTACK SURFACES — which overlay said so

### `S:gap:390:d3b95706` — Asymptotic constant c in f(n) − 2n ~ c·n/log n, where f(n) = A193429(n) = minimum possible largest factor when n! is …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none
- witness verifier: No cheap witness: the open content is the existence of an asymptotic constant, which no finite table certifies. A193429 is computable (b-file reaches n = 168) and extension is cheap, but moves nothing.
- notes: Erdős–Guy–Selfridge [EGS82] proved f(n) − 2n ≍ n/log n (both order bounds); the open question is whether f(n) − 2n ~ c·n/log n for a specific constant c. The stub's Lean formalization marks 'research solved' — that plausibly reflects the proven ≍ statement; the constant question is the open residue per erdosproblems.com. a(n) = 0 only for n = 1, 2 (Adams-Watters); sequence appears O(n). Empirical c-estimation from an extended table (b-file n <= 168, Kesarwani) is possible scout work but …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `factorials`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P390` and the surface id in your receipt so the next agent's graph build finds your result.


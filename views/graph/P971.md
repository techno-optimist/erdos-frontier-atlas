# Erdős #971 — attack card

> Erdős problem #971 · https://www.erdosproblems.com/971
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: existence of c > 0 with #{a mod d : p(a,d) > (1+c) phi(d) log d} ≫ phi(d) for ALL large …[truncated; see source])** — Erdős [Er49c] proved the statement for an infinite sequence of d, and complementarily that for any eps > 0, p(a,d) < eps phi(d) log d holds for ≫_eps phi(d) residues (per erdosproblems #971 remarks). The heuristic mean of p(a,d)/(phi(d) log d) is ~1; the question is a uniform fat-upper-tail claim. No bounded record quantity: numeric d-sweeps (say all d ≤ 1e6 with several c values) would be cheap supporting data only. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/971.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let `p(a, d)` be the least prime congruent to `a (mod d)`. Does there exist a constant `c > 0` such that for all large `d`, `p(a, d) > (1 + c) * φ(d) * log d` for `≫ φ(d)` many values of `a`?

## ATTACK SURFACES — which overlay said so

### `S:gap:971:e507136b` — existence of c > 0 with #{a mod d : p(a,d) > (1+c) phi(d) log d} ≫ phi(d) for ALL large d, where p(a,d) = least prime ≡ …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — a for-all-large-d positive-proportion statement; per-d numeric tables verify nothing about the tail of d
- witness verifier: n/a; for any fixed d, computing all p(a,d) and the count exceeding (1+c) phi(d) log d is a cheap sieve (A226521 triangle), exploratory only
- notes: Erdős [Er49c] proved the statement for an infinite sequence of d, and complementarily that for any eps > 0, p(a,d) < eps phi(d) log d holds for ≫_eps phi(d) residues (per erdosproblems #971 remarks). The heuristic mean of p(a,d)/(phi(d) log d) is ~1; the question is a uniform fat-upper-tail claim. No bounded record quantity: numeric d-sweeps (say all d ≤ 1e6 with several c values) would be cheap supporting data only.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P971` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #60 — attack card

> Erdős problem #60 · https://www.erdosproblems.com/60
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:60:6e611450` — A006855(41) = ex(41; C4) — maximum edges in a C4-free graph on 41 vertices (first uncomputed term; exact values end at …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 132 (B. McKay, OEIS A006855 comment (Mar 2022); Afzaly–McKay extremal tables list >=3 witness graphs at 41 vertices / 132 edges in sparse6) · upper 133 (M. Alekseyev, OEIS A006855 comment (Jan 2023), via a(n) <= floor(a(n-1)*n/(n-2)): floor(127*41/39) = 133)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `unknown`
- witness object: A C4-free graph on 41 vertices with 133 edges (closes the cell at 133). More accessible variants: beat McKay's best-known at n=46..49 where gaps are wide — a(46) in [157,165], a(47) in [163,171], a(48) in [168,176], a(49) in [174,182].
- witness verifier: Compute B = A^2 and assert every off-diagonal entry <= 1 (no two vertices share two common neighbors = no C4) — O(n^3) integer matrix multiply, dependency-free, instant at n<=49.
- notes: Headline #60 is a supersaturation conjecture (any n-vertex graph with > ex(n;C4) edges contains >> sqrt(n) copies of C4; He–Ma–Yang proved it for n = q^2+q+1 with q even; upstream flags it not finitely computable) — the linked A006855 table is the machine lane, and ex(n;C4) proper is problem #765's subject. McKay marks his 41-45 lower bounds as 'good chance of being exact', so the 133-edge witness at n=41 may not exist; the wider 46-49 gaps are the honest hunting ground. Cell graded 'unknown' …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #765 shares A006855 → no card (registers only: `atlas/stubs.json`)

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P60` and the surface id in your receipt so the next agent's graph build finds your result.


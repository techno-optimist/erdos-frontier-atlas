# Erdős #236 — attack card

> Erdős problem #236 · https://www.erdosproblems.com/236
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/236.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $f(n)$ count the number of solutions to $n=p+2^k$ for prime $p$ and $k\geq 0$. Show that $f(n)=o(\log n)$.

## ATTACK SURFACES — which overlay said so

### `S:gap:236:6226f0ff` — A109926(21) — least n whose number of representations n = p + 2^k (p prime, 2^k < n) is exactly 21; records-of-f lane …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower > 1e10 (D. Johnson comment on OEIS A109926: a(n) > 10^10 for n >= 21) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: An integer n > 1e10 with exactly 21 primes among {n - 2^k : 1 <= 2^k < n} (any such n upper-bounds a(21); note A109926 is not monotone — a(18) < a(17))
- witness verifier: ~34 deterministic Miller–Rabin tests on n - 2^k and count == 21; milliseconds, dependency-free (this session verified a(20) = 2282745465 has exactly 20 representations)
- notes: Search accelerators from OEIS comments: known terms n>4 divisible by 15 (conjectured 30 | a(n) beyond); segmented prime sieve + 2^k shift counting reaches 1e11–1e12 on one box in days. Once any n0 with 21 reps is found, exact a(21) is a bounded exhaustive count over (1e10, n0] — hence cell. FRESHNESS caveat: the 1e10 floor is 2009-era; re-check for extensions before spending compute. Companion verified-range fact for the same hub problem: A039669 = {4,7,15,21,45,75,105} (m - 2^k prime for ALL …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #1142 shares A039669 → card `views/graph/P1142.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P236` and the surface id in your receipt so the next agent's graph build finds your result.


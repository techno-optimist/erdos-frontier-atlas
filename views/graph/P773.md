# Erdős #773 — attack card

> Erdős problem #773 · https://www.erdosproblems.com/773
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:773:1a251c0a` — A390813(69) — size of the largest Sidon subset of the first 69 positive perfect squares (first uncomputed cell; table …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 32 (a(68)=32 (OEIS A390813 data/b-file, Kalogeropoulos + Sievers) + trivial monotonicity a(n+1) >= a(n)) · upper 33 (trivial: deleting 69^2 from an optimal set shows a(69) <= a(68)+1 = 33)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: an explicit S ⊆ {1^2, ..., 69^2} with |S| = 33 and all pairwise sums distinct (settles a(69)=33); more generally any size-k Sidon subset of the first N squares beating the recorded a(N) row
- witness verifier: O(k^2) integer check: form all k(k+1)/2 pairwise sums a+b (a<=b), assert no duplicates — dependency-free one-liner
- notes: The Erdős/Alon–Erdős 1985 question — is max Sidon subset of {1,4,...,N^2} of size N^(1-o(1))? — is an asymptotic wall: N^(2/3) lower (Lefmann–Thiele 1995) vs N^(1-c/loglog N) upper (Croot–Mao–Yip, posted in problem-page comments; page edited Apr 2026). The machine lane is the exact table: a(69) falls to bounded search (ILP / max-clique on the Sidon-conflict graph), and each new row is a small verified record. Sievers note: up to n=68 only n=32 fails to attain the max with a subset containing …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `sidon sets`, `squares`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P773` and the surface id in your receipt so the next agent's graph build finds your result.


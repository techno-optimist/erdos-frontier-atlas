# Erdős #700 — attack card

> Erdős problem #700 · https://www.erdosproblems.com/700
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: f(n) = min_{1<k<=n/2} gcd(n, binom(n,k)) for composite n (numerically = A091963(n)); …[truncated; see source])** — Erdos-Szekeres [ErSz78]. Known per erdosproblems.com/700 (paraphrased): f(n) <= n/P(n) for composite n (take k = exact prime-power divisor), hence f(n) <= (1+o(1))·n/log n; equality f(n)=n/P(n) holds for n=pq (two primes) and n=30; f(n) >= p(n) (smallest prime factor), so n=p^2 gives f(n) >= sqrt(n) — the strict-inequality infinitude for general composite n is the open part. Note OEIS A091963 is phrased as smallest gcd of two interior Pascal-row entries; it coincides with min gcd(n, binom(n,k)) …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:700:25e68d96` — f(n) = min_{1<k<=n/2} gcd(n, binom(n,k)) for composite n (numerically = A091963(n)); sub-questions: characterize …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: No record-shaped bound: the sub-questions are a characterization plus infinitude/asymptotic claims. Individual composite n with f(n) > sqrt(n) or with f(n) = n/P(n) are cheap specimens but move no bound.
- witness verifier: Per-n check without big ints: for each prime power of n, get its valuation in binom(n,k) via Kummer carry-counting; f(n) then costs O(n·log n). I verified A091963(n) = f(n) directly (big-int gcds) for 4 <= n <= 43.
- notes: Erdos-Szekeres [ErSz78]. Known per erdosproblems.com/700 (paraphrased): f(n) <= n/P(n) for composite n (take k = exact prime-power divisor), hence f(n) <= (1+o(1))·n/log n; equality f(n)=n/P(n) holds for n=pq (two primes) and n=30; f(n) >= p(n) (smallest prime factor), so n=p^2 gives f(n) >= sqrt(n) — the strict-inequality infinitude for general composite n is the open part. Note OEIS A091963 is phrased as smallest gcd of two interior Pascal-row entries; it coincides with min gcd(n, binom(n,k)) …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `binomial coefficients`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P700` and the surface id in your receipt so the next agent's graph build finds your result.


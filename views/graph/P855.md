# Erdős #855 — attack card

> Erdős problem #855 · https://www.erdosproblems.com/855
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/855.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:855:a71f24a4` — y* = smallest interval width y with an admissible tuple of more than pi(y) elements (Hensley–Richards violation width; …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower > 2330 (exact rho*(n) values (Engelsma) from the OEIS A023193 b-file satisfy rho*(n) <= pi(n) for all 2 <= n <= 2330 — recomputed locally against a sieve on 2026-07-18 (min slack 0, at n=2)) · upper 3159 (Engelsma's admissible 447-tuple of width 3159 (diameter 3158) with pi(3159) = 446 — opertech.com/primes/k-tuples.html, cited in OEIS A023193 comments; pi(3159)=446 verified locally)
- witness side: `upper` · witness feasibility `hard` · exact feasibility `unknown`
- witness object: an admissible k-tuple inside [0, y-1] with k > pi(y) and y < 3159 (or: extended exact rho*(n) computations past n = 2330, which raise the lower bound)
- witness verifier: admissibility: for each prime p <= k check the residues mod p miss at least one class — O(k·pi(k)) integer ops; pi(y) by direct sieve; both dependency-free and instant
- notes: The famous conjecture itself (pi(x+y) <= pi(x)+pi(y), 'second Hardy–Littlewood') is expected FALSE but only conditionally refuted: Hensley–Richards 1973/74 + tuples conjecture; Clark–Jarvis 2001 kill the Straus variant similarly; best unconditional is Montgomery–Vaughan 1973 pi(x+y) <= pi(x) + 2y/log y (facts paraphrased from erdosproblems.com/855, edited 2026-04-08 — fresh). No witness settles the conjecture itself (that side is a wall); the machine-movable object is the [2331, 3159] window …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #1204 shares A023193 → card `views/graph/P1204.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P855` and the surface id in your receipt so the next agent's graph build finds your result.


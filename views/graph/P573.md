# Erdős #573 — attack card

> Erdős problem #573 · https://www.erdosproblems.com/573
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:573:371ba68d` — A006856(54) — maximum number of edges in a 54-vertex graph of girth >= 5 (first uncomputed cell of ex(n;{C3,C4})) (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 185 (Brendan McKay, OEIS A006856 comment (Jan 07 2023) and McKay's 'Extremal Graphs and Turan Numbers' data page (accessed 2026-07-18)) · upper 187 (DERIVED (this survey, elementary): deleting a min-degree vertex gives e <= a(53) + floor(2e/54) with a(53)=181 exact (McKay 2023), forcing e <= 187. Not a literature bound; sharper unpublished search bounds may exist.)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `cell`
- witness object: A 54-vertex graph with >= 186 edges and girth >= 5, given as an edge list (g6 string or adjacency list)
- witness verifier: Check |V|=54 and edge count; verify girth >= 5 by confirming no two adjacent vertices share a neighbour (no C3) and no two vertices have two common neighbours (no C4) — O(n^3) integer arithmetic, dependency-free.
- notes: Headline problem (Erdős–Simonovits) is asymptotic — is ex(n;{C3,C4}) ~ (n/2)^{3/2}? — and not machine-closable; the linked sequence is the record lane. Exact through n=53: a(50)=175 (unique, Hoffman–Singleton), a(51)=176, a(52)=178, a(53)=181 with >=2688 extremal graphs (McKay). Best known lower bounds a(54..64) >= 185,189,193,197,202,207,212,216,220,224,230 (McKay page, accessed 2026-07-18). Frontier held since Jan 2023 by specialized exhaustive tooling (McKay; SAT approaches by …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `turan number`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P573` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #171 — attack card

> Erdős problem #171 · https://www.erdosproblems.com/171
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: c_7 — largest line-free subset of {1,2,3}^7 (density Hales–Jewett, = A156989(7)))** — 16-year bracket; +1 on the lower bound would be the first movement since Polymath1. Exact value needs an optimality certificate that does not exist compactly. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **solved-upstream** (atlas/stubs.json)
- upstream: **Solved** (raw `proved`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:171:2014d0f2` — c_7 — largest line-free subset of {1,2,3}^7 (density Hales–Jewett, = A156989(7)) (gap_map, kind `value_gap`, confidence C3)

- bounds: lower 1302 (Polymath1 (arXiv:1002.0374)) · upper 1386 (Polymath1 (arXiv:1002.0374))
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: a subset of {1,2,3}^7 of size ≥ 1303 containing no combinatorial line
- witness verifier: enumerate all 4^7 − 3^7 = 14197 line templates; check no line lies fully inside; trivial script
- notes: 16-year bracket; +1 on the lower bound would be the first movement since Polymath1. Exact value needs an optimality certificate that does not exist compactly.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `additive combinatorics`, `combinatorics`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P171` and the surface id in your receipt so the next agent's graph build finds your result.


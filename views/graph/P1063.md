# Erdős #1063 — attack card

> Erdős problem #1063 · https://www.erdosproblems.com/1063
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/1063.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:1063:f41109be` — A389360(60) = n_60 — least n >= 120 such that n-i divides binomial(n,60) for all but exactly one i in 0<=i<60 …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 120 (definition (n_k >= 2k by problem statement / Lean formalization domain)) · upper 581442729886633902054768000 (witness m = 60*lcm(2..59) (Cambie-type construction noted on erdosproblems.com/1063); machine-verified this session: binomial(m,60) divisible by m-i for all i=1..59 and not by m (exactly one exception, i=0))
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: an integer m in [120, 5.82e26] such that m-i | binomial(m,60) for exactly 59 of the 60 indices i in {0..59}; any smaller qualifying m improves the record toward the exact a(60)
- witness verifier: dependency-free Python: compute binomial(m,60) exactly (60 big-int multiplications), trial-divide by m-i for i=0..59, accept iff exactly one non-divisor; milliseconds even for m ~ 1e27
- notes: b-file complete and gap-free for k=2..59 (Chai Wah Wu, Oct 2025) — a(60) is the live frontier. Values are erratic: a(57)=218175259280 (~2.2e11), a(59)=4967281530 (~5.0e9); an exhaustive scan to ~1e11-1e12 with modular tricks is days-scale on one box, so exact a(60) is a bounded search (same method as the existing terms). Theoretical: Erdős–Selfridge observed at least one exception index always exists for n>=2k (proof in Monier via [Mo85]); Monier bound n_k <= k! improved by Cambie to …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1063` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #779 — attack card

> Erdős problem #779 · https://www.erdosproblems.com/779
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Verified range of Deaconescu's conjecture: for every n>1 with P = primorial(n), some …[truncated; see source])** — Cheap record available: Fortunate numbers a(n) = A005235(n) satisfy gcd(a(n), P)=1, so a(n) < p_(n+1)^2 forces a(n) prime (Ordowski, OEIS comment) — and then a(n) itself is a Deaconescu witness for n. The b-file reaches n=4600 (Batalov; first 2000 terms verified prime, Arndt 2013), so auditing the b-file with the p_(n+1)^2 test (plus direct primality for any stragglers) should push the verified range 1000 -> ~4600 at near-zero compute. Proving the conjecture for all n is far beyond reach (needs …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]
- **WALL (surface: For n > 1 let P = p_1···p_n be the n-th primorial. Must there always exist a prime p with …[truncated; see source])** — Fails criterion 3 catastrophically and criterion 1 outright. 'Falsifiable' here is a technicality: the counterexample is an n, but *checking* that n is a counterexample is an exhaustion over ~10^7548 huge primality tests. Nothing is exhibitable. The heuristic in the site's own remark puts the failure probability at ≈exp(−n^{cn}) — Cambie's phrasing is that the chances of failing are ridiculously small — so we would be paying an astronomical price to hunt an object that essentially certainly does not exist. The only tractable work here is extending the *verification* side (Fortunate-number primality), which is a records-ladder increment, not a resolution, and OEIS already sits at n ≤ 2000 …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/779.lean)

## ATTACK SURFACES — which overlay said so

### `S:triage:779` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: For n > 1 let P = p_1···p_n be the n-th primorial. Must there always exist a prime p with p_n < p < P such that P + p is prime?
- witness: NOT concrete in any usable sense. A counterexample is an index n together with a proof that P + p is composite for EVERY prime p in (p_n, P) — i.e. π(P) ≈ P/ln P compositeness certificates on numbers of size P. There is no small object to exhibit.
- smallest untested case: n = 2001. (The site cites Deaconescu for n ≤ 1000; OEIS A005235 does better — see frontier.)
- known frontier: Site remark: Deaconescu verified n ≤ 1000. Stronger, and apparently not noted on the problem page: OEIS A005235 (Fortunate numbers a(n) = least m > 1 with p_n# + m prime) records that the first 2000 terms are prime (J. Arndt, Apr 2013), and Firoozbakht's observation a(n) > p_{n+1} − 1 means each such a(n) is a prime p with p_n < p < P and P + p prime. So #779 is in fact verified for all n ≤ 2000. Batalov's b-file extends A005235 to n = 4600, though primality of every one of those terms is not …[truncated; see source]
- search-space estimate: Certifying a counterexample at n = 2001: P = p_2000# with log P ≈ θ(17389) ≈ 17389, so P ≈ e^17389 (~7550 decimal digits) and π(P) ≈ P/ln P ≈ 10^7548 primality tests on 7550-digit numbers. That is 10^7536 times over any conceivable budget.
- verification: False
- verdict reason: Fails criterion 3 catastrophically and criterion 1 outright. 'Falsifiable' here is a technicality: the counterexample is an n, but *checking* that n is a counterexample is an exhaustion over ~10^7548 huge primality tests. Nothing is exhibitable. The heuristic in the site's own remark puts the failure probability at ≈exp(−n^{cn}) — Cambie's phrasing is that the chances of failing are ridiculously small — so we would be paying an astronomical price to hunt an object that essentially certainly does not exist. The only tractable work here is extending the *verification* side (Fortunate-number primality), which is a records-ladder increment, not a resolution, and OEIS already sits at n ≤ 2000 …[truncated; see source]
- no move applies mechanically

### `S:gap:779:e04edae2` — Verified range of Deaconescu's conjecture: for every n>1 with P = primorial(n), some prime p with p_n < p < P has P + p …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 1000 (Deaconescu's verification, reported in erdosproblems.com/779 remarks) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: per new n: a prime p in (p_n, P) with P + p (PRP-)prime, extending the verified range; a counterexample n (all such p fail) would falsify and close the problem — heuristically astronomically unlikely
- witness verifier: gmpy2/PARI: rebuild primorial, assert p prime and p_n < p < P, PRP-test P+p; scouting-grade in seconds per n (full ECPP certification of P+p optional and heavy at large n)
- notes: Cheap record available: Fortunate numbers a(n) = A005235(n) satisfy gcd(a(n), P)=1, so a(n) < p_(n+1)^2 forces a(n) prime (Ordowski, OEIS comment) — and then a(n) itself is a Deaconescu witness for n. The b-file reaches n=4600 (Batalov; first 2000 terms verified prime, Arndt 2013), so auditing the b-file with the p_(n+1)^2 test (plus direct primality for any stragglers) should push the verified range 1000 -> ~4600 at near-zero compute. Proving the conjecture for all n is far beyond reach (needs …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P779` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #322 — attack card

> Erdős problem #322 · https://www.erdosproblems.com/322
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:322:8ce9fe8d` — A025418(35) — least integer that is a sum of 3 positive cubes in exactly 35 ways (first uncomputed cell; a(36)-a(39) …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: An integer m with exactly 35 representations m = x^3+y^3+z^3 (x<=y<=z, positive), listed explicitly; leastness additionally requires an exhaustive cube-sum sweep below m
- witness verifier: Representation count for a single m is a dependency-free double loop over x<=y<=(m/2)^(1/3) checking m-x^3-y^3 for cubeness (~10^8 ops at m~10^12); leastness needs a segmented sieve accumulating all i^3+j^3+k^3 <= m (~10^12 triple-sum updates — bounded, reproducible, but heavy)
- notes: b-file ends at a(34)=373248000000; Johnson's comment gives a(36)=496793088000, a(37)=980922617856, a(38)=209584584000, a(39)=644972544000, a(42)=970299000000 (sequence is not monotone, so these do not bound a(35)). The parent problem is asymptotic and a wall: Mahler 1936 disproved Hypothesis K for k=3 with r_3(n) >> n^(1/12) infinitely often; Erdős/Chowla independently proved r_k(n) >> n^(c/loglog n) for all k>=3; whether r_k(n) > n^c infinitely often for k>=4 is fully open …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `powers`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P322` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #878 — attack card

> Erdős problem #878 · https://www.erdosproblems.com/878
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: f(n) = A339378(n) power-sum (sum over p|n of the largest power of p ≤ n) — asymptotics of …[truncated; see source])** — Known (per erdosproblems.com/878, paraphrased): Erdős [Er84e] proved max_{n≤x} f(n) ~ x·log x/log log x along a sequence of x→∞, and x·log_4(x) ≪ H(x) ≪ x·log_3(x) (iterated logs); f(n)/n has no mean value (limsup of the partial averages is infinite, liminf finite). Recent comment activity (Barreto, incorporated May 2026): the H upper bound already gives f(n)=o(n log log n) for almost all n, and the guess max_{n≤x} f = max_{n≤x} F fails at x=210. No record lane. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:878:dc7952a0` — f(n) = A339378(n) power-sum (sum over p|n of the largest power of p ≤ n) — asymptotics of max_{n≤x} f(n), of …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — every sub-question is an asymptotic (o/~/≪) statement; no finite witness moves any of them
- witness verifier: n/a — A339378(n) is cheaply computable at any single n, but finite computation cannot certify asymptotics
- notes: Known (per erdosproblems.com/878, paraphrased): Erdős [Er84e] proved max_{n≤x} f(n) ~ x·log x/log log x along a sequence of x→∞, and x·log_4(x) ≪ H(x) ≪ x·log_3(x) (iterated logs); f(n)/n has no mean value (limsup of the partial averages is infinite, liminf finite). Recent comment activity (Barreto, incorporated May 2026): the H upper bound already gives f(n)=o(n log log n) for almost all n, and the guess max_{n≤x} f = max_{n≤x} F fails at x=210. No record lane.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P878` and the surface id in your receipt so the next agent's graph build finds your result.


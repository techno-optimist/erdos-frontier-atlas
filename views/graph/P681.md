# Erdős #681 — attack card

> Erdős problem #681 · https://www.erdosproblems.com/681
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Cofiniteness of the property 'there exists k with n+k composite and lpf(n+k) > k^2' — …[truncated; see source])** — Erdős [Er79d], related to Erdős–Eggleton–Selfridge; possibly true with k^2 replaced by k^d for any d. Fresh verification computation (this survey, 2026-07-18; numpy spf-sieve, cross-checked exactly against all 68 OEIS-displayed terms of A389680): counts of exceptional n <= 10^e for e=2..7 are 91, 746, 5712, 40282, 257940, 1441896 — density shrinking by ~x0.63 per decade to 0.1442 at 10^7; largest exception <= 10^7 is 9,999,973. So the OEIS comment 'density decreases' is confirmed but extinction …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/681.lean)

## STATEMENT (Apache-licensed, Lean-derived)

**Erdős problem 681.** Is it true that for all large $n$ there exists $k$ such that $n + k$ is composite and $p(n+k) > k^2$, where $p(m)$ is the least prime factor of $m$ ?

## ATTACK SURFACES — which overlay said so

### `S:gap:681:7a8320b8` — Cofiniteness of the property 'there exists k with n+k composite and lpf(n+k) > k^2' — equivalently finiteness of the …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None: a single n with a qualifying k proves nothing about 'all large n', and the conjecturally-finite exceptional set is still far too dense to name a 'largest known exception' frontier (14.4% density at 10^7, with exceptions right up to the search bound).
- witness verifier: Per-n check is cheap and dependency-free (any qualifying k satisfies k^4 < n+k, so scan k <= (n+k)^{1/4} with trial division for lpf), but there is no verified initial segment where the conjecture holds — so no verified_range framing is honest here.
- notes: Erdős [Er79d], related to Erdős–Eggleton–Selfridge; possibly true with k^2 replaced by k^d for any d. Fresh verification computation (this survey, 2026-07-18; numpy spf-sieve, cross-checked exactly against all 68 OEIS-displayed terms of A389680): counts of exceptional n <= 10^e for e=2..7 are 91, 746, 5712, 40282, 257940, 1441896 — density shrinking by ~x0.63 per decade to 0.1442 at 10^7; largest exception <= 10^7 is 9,999,973. So the OEIS comment 'density decreases' is confirmed but extinction …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P681` and the surface id in your receipt so the next agent's graph build finds your result.


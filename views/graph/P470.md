# Erdős #470 — attack card

> Erdős problem #470 · https://www.erdosproblems.com/470
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/470.lean)
- prize: $10 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:gap:470:f796998c` — Existence of an odd weird number (sigma(n) >= 2n, n not a sum of any subset of its divisors) — $10 Erdos prize; record …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 1e21 (Wenjie Fang, distributed search (yoyo@home), OEIS A006037 comment Feb 2014; method and bound published in arXiv:2207.12906 (2022), which also shows no odd weird number with abundance < 1e14 exists below 1e28) · upper — (—)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `unknown`
- witness object: Closing witness: an explicit odd weird number n — odd, sigma(n) >= 2n, plus a certificate that no subset of divisors sums to n (a subset-sum UNSAT instance over tau(n) divisors; SAT/DRAT- or meet-in-the-middle-certifiable for realistic tau). Record witness: an extension of the exhaustive odd search beyond 1e21 (or the bounded-abundance search beyond 1e28).
- witness verifier: For a claimed odd weird n: factor n, enumerate divisors, check sigma >= 2n, then certify subset-sum infeasibility (DP over sums is infeasible at n ~ 1e21, but branch-and-bound/ILP/SAT with the divisor structure is standard and produces a checkable certificate). Constraint filter: any odd weird …[truncated; see source]
- notes: Second half of #470 — are there infinitely many primitive weird numbers (A002975, b-file 1161 terms)? — is proven CONDITIONALLY by Melfi 2015 (J. Number Theory 147, 508-514) under a prime-gap hypothesis p_{n+1}-p_n < 0.1*sqrt(p_n); unconditional infinitude open; Amato-Hasler-Melfi-Parton 2019 construct primitive weirds with many prime factors. Benkoski-Erdos 1974 proved weird numbers have positive density; if no odd weird exists, every weird number has abundancy < 4 (page remark, cross-ref …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #825 shares A006037 → no card (registers only: `atlas/stubs.json`)

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P470` and the surface id in your receipt so the next agent's graph build finds your result.


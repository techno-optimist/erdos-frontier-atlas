# Erdős #78 — attack card

> Erdős problem #78 · https://www.erdosproblems.com/78
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Constructive exponential Ramsey lower bound: an explicit family certifying R(k) > C^k for …[truncated; see source])** — $100 prize. Erdős's probabilistic R(k) >> k 2^{k/2} is nonconstructive; the ask is an explicit C^k family. Distinct from #77 (which asks the VALUE of lim R(k)^{1/k}) despite the shared A059442 link — verified both pages directly to disambiguate. Page last edited 2026-01-23; upstream flags it not resolvable by finite computation. No OEIS cell to attack (A059442's own frontier cells like R(5,5) belong to #77's feeder discussion and to the small-Ramsey lane already covered by project campaigns). [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures
- prize: $100 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:gap:78:4f28f683` — Constructive exponential Ramsey lower bound: an explicit family certifying R(k) > C^k for some C > 1 — equivalently …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: No finite witness: at any single k, 'constructive' is vacuous (any exhibited coloring is a construction), so the problem demands an explicit infinite family plus proof. Frontier objects are extractor-based explicit Ramsey graphs: Cohen 2015 achieved no clique/independent set of size >= 2^{(loglog n)^C}; Li [Li23b] improved this to (log n)^C — still polylogarithmic, versus the O(log n) required. …[truncated; see source]
- witness verifier: None cheap: certifying clique/independence bounds for the relevant graph sizes is itself computationally hard, and no single graph decides the asymptotic claim.
- notes: $100 prize. Erdős's probabilistic R(k) >> k 2^{k/2} is nonconstructive; the ask is an explicit C^k family. Distinct from #77 (which asks the VALUE of lim R(k)^{1/k}) despite the shared A059442 link — verified both pages directly to disambiguate. Page last edited 2026-01-23; upstream flags it not resolvable by finite computation. No OEIS cell to attack (A059442's own frontier cells like R(5,5) belong to #77's feeder discussion and to the small-Ramsey lane already covered by project campaigns).
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #77 shares A059442 → card `views/graph/P77.md`
- #87 shares A059442 → card `views/graph/P87.md`
- #166 shares A059442 → card `views/graph/P166.md`
- #545 shares A059442 → card `views/graph/P545.md`
- #812 shares A059442 → card `views/graph/P812.md`
- #986 shares A059442 → no card (registers only: `atlas/stubs.json`)
- #1029 shares A059442 → card `views/graph/P1029.md`
- #1030 shares A059442 → card `views/graph/P1030.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P78` and the surface id in your receipt so the next agent's graph build finds your result.


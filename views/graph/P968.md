# Erdős #968 — attack card

> Erdős problem #968 · https://www.erdosproblems.com/968
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: lower density of {n : p_n/n < p_{n+1}/(n+1)} (the index set behind A387591) — conjectured …[truncated; see source])** — Erdős–Prachar 1961 [ErPr61] proved the COMPLEMENT {u_n > u_{n+1}} has positive (lower) density and that sum over p_n < x of |u_{n+1} − u_n| ≍ (log x)^2 (per erdosproblems #968 remarks). Erdős also asked for infinitely many monotone triples u_n < u_{n+1} < u_{n+2} and the reverse. Tao has flagged the problem 'looks difficult' on the site (2026). A387591 is new (Sep 2025). No bounded record quantity identified. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/968.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Does the set `{n | u n < u (n+1)}` have positive natural density?

## ATTACK SURFACES — which overlay said so

### `S:gap:968:f0847933` — lower density of {n : p_n/n < p_{n+1}/(n+1)} (the index set behind A387591) — conjectured positive (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — a positive-density existence question; finite counts cannot certify positive lower density
- witness verifier: n/a; empirical density to n ~ 1e9 is a cheap sieve computation (A387591 b-file already has 10000 terms) but is exploratory only
- notes: Erdős–Prachar 1961 [ErPr61] proved the COMPLEMENT {u_n > u_{n+1}} has positive (lower) density and that sum over p_n < x of |u_{n+1} − u_n| ≍ (log x)^2 (per erdosproblems #968 remarks). Erdős also asked for infinitely many monotone triples u_n < u_{n+1} < u_{n+2} and the reverse. Tao has flagged the problem 'looks difficult' on the site (2026). A387591 is new (Sep 2025). No bounded record quantity identified.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P968` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #241 — attack card

> B_3 sets: max A⊆{1..N} with all triple sums a+b+c distinct; is f(N)~N^{1/3}? · https://www.erdosproblems.com/241
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **CAMPAIGN FINDING** — 2026-07-16 CLOSED-BY-CROSSREF: A387704(n)=max{k:A227358(k)<=n-1} (translation invariance), machine-checked 0/151 mismatches; first jump to 9 = 209; table free to n=309; open content = #41 a(11). The 'hobbyist backtracker' table was A227358 in disguise — third stale-frontier catch of the day (#552 a(17), f(39), now this). [atlas/problems.json campaign_finding]

## STATUS — three registers, never merged

- ours: **movable** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/241.lean)
- prize: $100 — prizes are history, not targets (standing rule 2).

## STATEMENT (Apache-licensed, Lean-derived)

Is it true that $f(N)\sim N^{1/3}$? Originally asked to Erdős by Bose. This is discussed in problem C11 of Guy's collection [Gu04].

## ATTACK SURFACES — which overlay said so

### `S:deep:241` — deep audit: **MOVABLE**, lane `exact-backtracking`, board **READY** (problems.json)

- finite object: Maximum-cardinality B_3 set in {1..n} (all multiset triple-sums distinct) = the exact value f(n) tabulated in OEIS A387704.
- current record: FINITE frontier: OEIS A387704 b-file, n=0..150, a(150)=8 — Sharvil Kesarwani, Dec 2025, via straightforward branch-and-bound backtracking (single machine). ASYMPTOTIC frontier (the actual conjecture): f(N)/N^{1/3} pinned in [~1, ~2.62]; lower (1+o(1))N^{1/3} = Bose–Chowla construction (1962/1960s), upper ~ (18N)^{1/3} counting bound with constant improvements (Ruzsa and others); conjecture says the constant →1. No one has moved the asymptotic in decades.
- frontier: CLOSED BY CROSS-REFERENCE (2026-07-16): A387704(n) = max{k : A227358(k) <= n-1} (B_3 is translation-invariant, so a k-element B_3 subset of {1..n} exists iff a k-mark B_3 ruler of length <= n-1 exists). Identity machine-checked against all 151 published terms (0 mismatches). Hence the table is determined through n=309 by Tromp's exhaustive A227358 values: a(n)=8 for 130<=n<=208, FIRST JUMP TO 9 …[truncated; see source]
- verifier: Given a candidate set A of size k: enumerate all C(k+2,3)=(k+2)(k+1)k/6 multiset triple-sums into a bitset over [3,3n] and check all distinct — O(k^3) exact, trivially cheap. Certifying f(n)=v (nonexistence of a size-(v+1) set in {1..n}) requires an EXHAUSTIVE branch-and-bound tree (DRAT-style search certificate), the expensive half.
- attack: Do not attack the table — it is determined through n=309 by the identity A387704(n)=max{k: A227358(k)<=n-1} (proof: translation invariance; verified on all published terms). The open cell (jump to 11) IS #41's a(11), owned by the #41 lane. A community contribution extending the OEIS b-file from 150 to 309 with this identity is the honest next act.
- applies: **exact backtracking** (predicate `lane-move`)

### `S:gap:241:ea3c6395` — A227358(11) — shortest 11-mark B3 ruler (11 integers 0 = m1 < ... < m11 with all triple sums, repetition allowed, …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower >= 310 (Strict monotonicity from A227358(10) = 309 (OEIS)) · upper 445 (Known 11-mark configuration of length 445; J. Tromp comment on A227358: a(11) = 445 or a(11) < 440 (lengths 440-444 exhaustively excluded))
- witness side: `upper` · witness feasibility `hard` · exact feasibility `unknown`
- witness object: 11 integers 0 = m1 < ... < m11 <= 439 whose C(13,3) = 286 multiset triple sums are pairwise distinct
- witness verifier: Compute all 286 triple sums with repetition, assert no duplicates; microseconds. Farm's validated B3 adapter (hub #41 lane) reproduces a(3..10).
- notes: Hub #241's own conjecture (f(N) ~ N^{1/3}, Bose/Guy C11) is asymptotic and not machine-closable. Its stub sequence A387704 (b-file 0..150, max value 8) was closed as a pure crossref: A387704(n) = max{k : A227358(k) <= n-1}, 0/151 mismatches, first reaches 9 at n = 209 (hub librarian, PR #80). The only open computational content is A227358(11), which is farm-owned under hub #41 (witness search < 445 running). Tromp 2013 estimated many CPU-years to exhaust [310,439] with his C code; 13 years …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `additive combinatorics`, `sidon sets`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P241` and the surface id in your receipt so the next agent's graph build finds your result.


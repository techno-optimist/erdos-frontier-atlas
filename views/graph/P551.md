# Erdős #551 — attack card

> Erdős problem #551 · https://www.erdosproblems.com/551
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Prove R(C_k,K_n) = (k-1)(n-1)+1 for k >= n >= 3, except n=k=3.)** — Textbook Ramsey wall. The finite object is concrete and cheaply verifiable, and that is the only condition satisfied. The smallest genuinely open cell requires reasoning about graphs on 64 vertices — R(5,5) is unresolved at 43 vertices, so this is well past the world frontier for exhaustive methods, and the space (10^607) is not reachable by any pruning. Note also that every advance in this row for the last twenty years has been a human structural proof of a single cell (r(C_8,K_8), r(C_9,K_8)), never a computation; there is no computational lane here to enter. Compounding it, the DECIDABLE tag is again nominal: KLS21 covers only "large n", so the finite check has no explicit top even if a …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `decidable`, finite handle `decidable`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:triage:551` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: Prove R(C_k,K_n) = (k-1)(n-1)+1 for k >= n >= 3, except n=k=3.
- witness: CONCRETE: a graph G on (k-1)(n-1) + 1 vertices containing no C_k and with independence number < n (the matching lower-bound construction, n-1 disjoint K_{k-1}'s, is classical, so only the upper bound can fail). Verification is exact: no independent 8-set is C(64,8) = 4.4x10^9 checks, and C_10-absence is a pruned DFS which terminates fast precisely because the graph must be long-cycle-free.
- smallest untested case: (k,n) = (10,8): is R(C_10,K_8) = 9*7+1 = 64? The n<=7 rows are fully closed and the n=8 row is closed at k=8, k=9 and k>=34, leaving k in [10,33].
- known frontier: This is a literature frontier, not a search frontier — no one enumerates here. Rows fully settled: n=3,4,5 (classical), n=6 by Schiermeyer 2003, "All cycle-complete graph Ramsey numbers r(C_m,K_6)", J. Graph Theory 44:251-260; n=7 settled, r(C_m,K_7)=6m-5 for m>=7, with r(C_7,K_7)=37 proved independently by Baniabedalruhman-Jaradat and by Cheng et al. Row n=8: r(C_8,K_8)=50 (Jaradat & Alzaleq, SUT J. Math. 43(1), 2007) and r(C_9,K_8)=57 (2011). General upper reach: Bondy-Erdos [BoEr73] k > …[truncated; see source]
- search-space estimate: (k,n)=(10,8): a witness lives among the 2^C(64,2) = 2^2016 ~ 10^607 graphs on 64 vertices; even modulo isomorphism this is ~10^607/64! ~ 10^518.
- verification cheap and exact: yes
- verdict reason: Textbook Ramsey wall. The finite object is concrete and cheaply verifiable, and that is the only condition satisfied. The smallest genuinely open cell requires reasoning about graphs on 64 vertices — R(5,5) is unresolved at 43 vertices, so this is well past the world frontier for exhaustive methods, and the space (10^607) is not reachable by any pruning. Note also that every advance in this row for the last twenty years has been a human structural proof of a single cell (r(C_8,K_8), r(C_9,K_8)), never a computation; there is no computational lane here to enter. Compounding it, the DECIDABLE tag is again nominal: KLS21 covers only "large n", so the finite check has no explicit top even if a …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `ramsey theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P551` and the surface id in your receipt so the next agent's graph build finds your result.


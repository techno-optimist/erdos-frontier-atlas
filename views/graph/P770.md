# Erdős #770 — attack card

> Erdős problem #770 · https://www.erdosproblems.com/770
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Distributional questions for h(n) = extremal b such that 2^n-1, 3^n-1, ..., b^n-1 are …[truncated; see source])** — Known easy facts (paraphrased from problem page): h(n) = n+1 iff n+1 prime; h(n) unbounded on odd n; plausibly h(n)=3 infinitely often. The attached sequence A263647 (n with gcd(2^n-1, 3^n-1)=1, b-file to 10000 by R. Israel) is actually the sequence OEIS ties to the sibling problem #820 (its infinitude is the Ailon–Rudnick 2004 question); the hub attaches it here as the h(n) >= 4 indicator. No record lane. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/770.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:770:7b3ac672` — Distributional questions for h(n) = extremal b such that 2^n-1, 3^n-1, ..., b^n-1 are mutually coprime: do densities …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — all three sub-questions are density-existence / liminf / characterization statements with no finite certificate; h(n) is computable per n but tables cannot decide any of them
- witness verifier: n/a (per-n evaluation of h(n) is cheap gcd arithmetic, but no bounded quantity with a gap is at stake)
- notes: Known easy facts (paraphrased from problem page): h(n) = n+1 iff n+1 prime; h(n) unbounded on odd n; plausibly h(n)=3 infinitely often. The attached sequence A263647 (n with gcd(2^n-1, 3^n-1)=1, b-file to 10000 by R. Israel) is actually the sequence OEIS ties to the sibling problem #820 (its infinitude is the Ailon–Rudnick 2004 question); the hub attaches it here as the h(n) >= 4 indicator. No record lane.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #820 shares A263647 → card `views/graph/P820.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P770` and the surface id in your receipt so the next agent's graph build finds your result.


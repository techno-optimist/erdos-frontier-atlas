# Erdős #422 — attack card

> Erdős problem #422 · https://www.erdosproblems.com/422
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: W(422) = largest N such that Hofstadter's Q-sequence f (f(1)=f(2)=1, …[truncated; see source])** — The Erdős–Graham/Hofstadter questions — does f miss infinitely many integers, and what is its behavior — are open and explicitly not machine-closable; it is not even known that f is total, which is exactly what the verified range tracks. Advancing 3e10 → 1e11+ is a memory-engineering job (mmap/disk streaming, days), a clean citable record with a 2014→2023 precedent chain in OEIS. Context literature: Pinn (1999/2000) order-and-chaos analyses; Deane–Gentile arXiv:2311.13854 (diluted variant with …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/422.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Does $f(n)$ miss infinitely many integers?

## ATTACK SURFACES — which overlay said so

### `S:gap:422:671238e8` — W(422) = largest N such that Hofstadter's Q-sequence f (f(1)=f(2)=1, f(n)=f(n-f(n-1))+f(n-f(n-2))) is verified …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 3e10 (M. Eric Carr, Jul 2023, comment in OEIS A005185 (earlier record 1e10: Roman Pearce, 2014)) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `wall`
- witness object: an extended computation certifying well-definedness to N' > 3e10 — there is no succinct certificate; the computed prefix itself is the witness
- witness verifier: independent recomputation of the recurrence, checking n - f(n-1) ≥ 1 and n - f(n-2) ≥ 1 at every step; empirically f(n) ≈ n/2 so accesses span ~n/2 back — storage is the binding constraint (~4 bytes/term ⇒ ~120 GB at 3e10); verification cost equals computation cost
- notes: The Erdős–Graham/Hofstadter questions — does f miss infinitely many integers, and what is its behavior — are open and explicitly not machine-closable; it is not even known that f is total, which is exactly what the verified range tracks. Advancing 3e10 → 1e11+ is a memory-engineering job (mmap/disk streaming, days), a clean citable record with a 2014→2023 precedent chain in OEIS. Context literature: Pinn (1999/2000) order-and-chaos analyses; Deane–Gentile arXiv:2311.13854 (diluted variant with …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P422` and the surface id in your receipt so the next agent's graph build finds your result.


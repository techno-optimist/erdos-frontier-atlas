# Erdős #993 — attack card

> Erdős problem #993 · https://www.erdosproblems.com/993
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **RETRACTION PIN** — claim `erdos-993-unimodal-n30` carries a banned phrasing (pin `I:pin:0ace36de`): a stronger earlier statement was withdrawn and must not be resurrected. Check `certificates/contracts.json` publication_bindings before publishing any prose about this problem.
- **RETRACTION PIN** — claim `erdos-993-unimodal-n30` carries a banned phrasing (pin `I:pin:5d2c1a12`): a stronger earlier statement was withdrawn and must not be resurrected. Check `certificates/contracts.json` publication_bindings before publishing any prose about this problem.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:triage:993` — triage verdict **TARGET** (finite_handle_triage, 2026-07-26)

- our summary: The independent-set sequence of any tree or forest is unimodal: if i_k(G) counts independent vertex sets of size k, then for every forest T there is m with i_0(T) <= ... <= i_m(T) >= i_{m+1}(T) >= ... . Alavi-Malde-Schwenk-Erdos 1987 [AMSE87], who showed it is false for general graphs (every inequality pattern is realised).
- witness: CONCRETE and tiny. A single tree T on n vertices whose independence sequence is not unimodal -- storable as an (n-1)-entry parent array (about 30 bytes). No auxiliary data required.
- smallest untested case: n = 30 (all trees on at most 29 vertices have been exhaustively verified). n=31 and n=32 are the next rungs.
- known frontier: Exhaustive verification for all trees on at most 29 vertices: B. Reynolds, 'Mean bounds, structural reductions, and exhaustive verification for tree independence polynomial unimodality', Zenodo preprint v3, March 2026, DOI 10.5281/zenodo.19100781 -- cited as the state of the art ('As of April 2026 ... computationally verified for trees on at most 29 vertices') in Hibi-Kara-Vien, arXiv:2604.18824, 20 Apr 2026. Earlier rungs: Yosef-Mizrachi-Kadrawi arXiv:2101.06744 verified n<=20 (via …[truncated; see source]
- search-space estimate: A000055(30) = 14,830,871,802 unlabeled trees; each costs an O(n*alpha) integer DP with i_k < 2^30 (fits uint64), about 1e3 word ops, so ~1.5e13 primitive ops -- hours on one 16-core box with nauty gentreeg (~1e6-1e7 trees/s/core). n=31 = 4.03e10 trees (~1 day), n=32 = 1.10e11 (~days).
- verification cheap and exact: yes
- verdict reason: The only one of the five that clears all four bars. The witness is a 30-byte parent array; the check is a dependency-free exact integer DP with no float and no CAS; the smallest untested size n=30 costs ~1.5e13 ops, inside the ~1e12-with-pruning budget once the published structural reductions are applied and comfortably inside one workstation-day even brute force; and the frontier is a specific published number (29) rather than a vague 'small cases'. Two honest caveats, neither fatal: (a) the payoff is asymmetric -- Ramos-Sun's ML sweep to 101 vertices found no unimodality break, so the realistic deliverable is a 'verified to n=30/31' record for the Records lane, not a solved Erdos problem; …[truncated; see source]
- applies: **finite-handle witness search** (predicate `triage-move`)

### `S:gap:993:dbd50fcc` — largest n for which every free tree on at most n vertices is verified to have a unimodal independence sequence (gap_map, kind `verified_range`, confidence C2)

- bounds: lower 30 (OUR exhaustive sweep, certificates/erdos-993/ -- all 23,522,619,475 trees on n<=30 (10.33 core-hours), which INCLUDES an independent replication of the previous frontier n<=29 (Brett Reynolds, Zenodo v3, DOI 10.5281/zenodo.19100781, March 2026, 8,691,747,673 trees) -- a single-author non-peer-reviewed preprint that nobody had replayed until now) · upper — (—)
- witness side: `lower` · witness feasibility `hard` · exact feasibility `cell`
- witness object: a tree whose independence sequence is not unimodal -- storable as an (n-1)-entry parent array, about 30 bytes
- witness verifier: exact integer DP A_v=prod(A_c+B_c), B_v=x*prod(A_c); then first strict descent with no later strict ascent. No floats, no CAS
- notes: n=30 alone (14,830,871,802 trees) is 1.71x the ENTIRE cumulative n<=29 workload -- not an incremental rung. Do NOT test log-concavity as a proxy: it is FALSE for trees from n=26 (Kadrawi-Levit-Yosef-Mizrachi arXiv:2305.01784 found exactly two order-26 trees unimodal but not log-concave; arXiv:2510.18826 found tens of thousands more to 101 vertices). The frontier holder is actively working this lane (repo pushed 2026-07-22, cloud compute), so a verified-to-N record here has a short shelf life; …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- contract claim `erdos-993-unimodal-n30` [promoted]: Every free tree on at most 30 vertices has a unimodal independence sequence (Erdos #993).
  - one-command replay: `python3 -I certificates/erdos-993/verify.py --quick`

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P993` and the surface id in your receipt so the next agent's graph build finds your result.


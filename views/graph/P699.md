# Erdős #699 — attack card

> Erdős problem #699 · https://www.erdosproblems.com/699
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: research solved, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/699.lean)

## STATEMENT (Apache-licensed, Lean-derived)

**Erdős Problem 699.** Is it true that for every $1 \le i < j \le n / 2$ there exists a prime $p \ge i$ with $p \mid \gcd\big(\binom{n}{i}, \binom{n}{j}\big)$?

## ATTACK SURFACES — which overlay said so

### `S:triage:699` — triage verdict **MAYBE** (finite_handle_triage, 2026-07-26)

- our summary: Is it true that for every 1 ≤ i < j ≤ n/2 there exists a prime p ≥ i with p | gcd(C(n,i), C(n,j))?
- witness: A triple (n, i, j) with 1 ≤ i < j ≤ n/2 such that no prime p ≥ i divides both C(n,i) and C(n,j). Verified by Kummer's theorem — p | C(n,k) iff adding k and n−k in base p produces a carry — so the check is pure exact integer arithmetic on base-p digits, with no big-integer factoring and no floating point.
- smallest untested case: n = 10^7 + 1 (general n), outside the special families n = 2^k for k ≤ 27 and n = 3^m + 1 for m ≤ 17, both already covered to ≈1.34×10^8.
- known frontier: CORRECTED 2026-07-27 -- our frontier was INCOMPLETE, not stale. Cong Lu's January 2026 run (github.com/conglu1997/erdos_699_rust, 2 commits, both 2026-01-06, untouched since) swept all rows n = 4..10^7 AND the structured families n = 2^k for k <= 27 (n = 134,217,728) and n = 3^m+1 for m <= 17 (n = 129,140,164). So a 10^8 sweep would NOT be the first computation past 10^8 -- we recorded only the 10^7 row-sweep and missed the family coverage. NOTE also that conglu is still listed 'currently …[truncated; see source]
- search-space estimate: A counterexample forces (n−i, n] to be prime-free (any prime there exceeds n/2 and divides both binomials), so i ≤ n − prevprime(n) ≤ 540 for n < 1.8×10^19, and for p > n/2 the test collapses to 'n mod p < k', an interval condition on j. So 10^7 → 10^8 is ≈9×10^7 values of n × ≈18 admissible i on average × ~500 ops ≈ 8×10^11 primitive ops — under the 10^12 budget, and none of the existing search …[truncated; see source]
- verification cheap and exact: yes
- verdict reason: RETARGETED 2026-07-27 from TARGET to MAYBE. The problem is NOT settled -- still Open/FALSIFIABLE, no full proof, the DeepMind Lean statement is still @[category research open] -- but the computation we specified is the wrong shape. Three corrections. (1) The frontier is 10^7 for general rows but ~1.34x10^8 for the structured families, so '10^7 -> 10^8' overstates the gain. (2) We wrote 'accepted proof claims' (plural) for j <= (3/2)i and n = 2j; there is exactly ONE accepted claim -- Liam Price with GPT-5.6 Sol Pro, 2026-07-18, covering both cases, about 1/3 of the (i,j) pairs. (3) Del Pin (2026-07-24), glossed by Bloom (2026-07-25), shows the bad-j range is a single interval -- which …[truncated; see source]
- applies: **finite-handle witness search** (predicate `triage-move`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `binomial coefficients`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P699` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #913 — attack card

> Erdős problem #913 · https://www.erdosproblems.com/913
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A359747 verified frontier — largest verified k such that k(k+1) has all-distinct …[truncated; see source])** — Every term has k or k+1 powerful (OEIS comment), so terms are Pell-sparse but easy to enumerate. Conditional resolution (per erdosproblems.com/913, paraphrased): infinitely many primes p with 8p²−1 also prime would settle it affirmatively via n=8p²−1 (exponents {1,2,3}) — a Schinzel-type hypothesis, i.e. the theoretical side is a wall. Table extension and the both-powerful side-hunt are the cheap lanes; neither closes the problem. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/913.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Are there infinitely many $n$ such that if $$ n(n + 1) = \prod_i p_i^{k_i} $$ is the factorisation into distinct primes then all exponents $k_i$ are distinct?

## ATTACK SURFACES — which overlay said so

### `S:gap:913:0da6d7a7` — A359747 verified frontier — largest verified k such that k(k+1) has all-distinct exponents in its prime factorization …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 32327407 (OEIS A359747 b-file, exhaustive first 1000 terms (Amiram Eldar)) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: new terms k > 32327407 (extends the verified range / b-file; evidence only — infinitude is the open content). Sharper target from OEIS comment: a term k > 8 with BOTH k and k+1 powerful (checked absent through A060355(39))
- witness verifier: factor k and k+1 (trial division + Pollard rho, trivial at 10^8–10^12), pool the exponents of the two coprime factorizations, check all pairwise distinct; seconds
- notes: Every term has k or k+1 powerful (OEIS comment), so terms are Pell-sparse but easy to enumerate. Conditional resolution (per erdosproblems.com/913, paraphrased): infinitely many primes p with 8p²−1 also prime would settle it affirmatively via n=8p²−1 (exponents {1,2,3}) — a Schinzel-type hypothesis, i.e. the theoretical side is a wall. Table extension and the both-powerful side-hunt are the cheap lanes; neither closes the problem.
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P913` and the surface id in your receipt so the next agent's graph build finds your result.


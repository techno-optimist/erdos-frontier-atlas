# Erdős #503 — attack card

> Erdős problem #503 · https://www.erdosproblems.com/503
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A175769(9) — maximum isosceles set in R^9 (every 3 points form an isosceles triangle); …[truncated; see source])** — a(1..8) = 3,6,8,11,17,28,30,45: d=2 Erdős–Kelly 1947 (new proof Kovács 2024 per site), d=3 Croft 1962, d=4,5 Kido, d=6,7,8 Ionin (EJC 16 (2009) R141). Blokhuis bound sharp at d=1,2,6,8 (a(8)=45=C(10,2)) but a(7)=30<36 shows it is not always tight — so a(9) in [46,55] is genuinely open at both ends. Natural candidate witnesses: large two-distance sets (any two-distance set is isosceles by pigeonhole) from association schemes / strongly regular graphs; that classification field (Musin, Nozaki, …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/503.lean)

## STATEMENT (Apache-licensed, Lean-derived)

What is the size of the largest $A \subseteq \mathbb{R}^n$ such that every three points from $A$ determine an isosceles triangle? That is, for any three points $x$, $y$, $z$ from $A$, at least two of the distances $|x - y|$, $|y - z|$, $|x - z|$ are equal.

## ATTACK SURFACES — which overlay said so

### `S:gap:503:42c1e9a0` — A175769(9) — maximum isosceles set in R^9 (every 3 points form an isosceles triangle); first unknown dimension of the …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 46 (C(d+1,2)=45-point two-distance construction {e_i+e_j} observed by Alweiss, plus one additional point (Weisenberg) — per erdosproblems.com #503 remarks/comments) · upper 55 (Blokhuis general upper bound C(d+2,2) = C(11,2))
- witness side: `lower` · witness feasibility `hard` · exact feasibility `wall`
- witness object: explicit coordinates (rational or algebraic) of >= 47 points in R^9 with every triple isosceles
- witness verifier: compute all pairwise squared distances in exact arithmetic; for each of the C(m,3) triples check that at least 2 of the 3 squared distances coincide; O(m^3); dependency-free
- notes: a(1..8) = 3,6,8,11,17,28,30,45: d=2 Erdős–Kelly 1947 (new proof Kovács 2024 per site), d=3 Croft 1962, d=4,5 Kido, d=6,7,8 Ionin (EJC 16 (2009) R141). Blokhuis bound sharp at d=1,2,6,8 (a(8)=45=C(10,2)) but a(7)=30<36 shows it is not always tight — so a(9) in [46,55] is genuinely open at both ends. Natural candidate witnesses: large two-distance sets (any two-distance set is isosceles by pigeonhole) from association schemes / strongly regular graphs; that classification field (Musin, Nozaki, …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`, `distances`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P503` and the surface id in your receipt so the next agent's graph build finds your result.


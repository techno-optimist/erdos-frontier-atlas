# Erdős #1108 — attack card

> Erdős problem #1108 · https://www.erdosproblems.com/1108
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A115645(27) — next powerful number (min prime exponent >= 2) expressible as a sum of …[truncated; see source])** — Erdős at Oberwolfach 1988, motivated by a question of Mahler. Range push is genuinely cheap: all subset sums of {1!..21!} number only 2^21 ≈ 2·10^6 with max ~5·10^19, and subsets of {1!..25!} are 3.4·10^7 with max ~1.6·10^25 — factoring that many <=84-bit numbers is days on one box, so 10^18 -> 10^24 is within reach (grade open-easy for the verified-range record; an actual 27th term is conjecturally unlikely). The squares-only subcase A025494 is already far deeper: 16 squares known (largest …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/1108.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:1108:14af27ec` — A115645(27) — next powerful number (min prime exponent >= 2) expressible as a sum of distinct factorials; 26 terms …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 1e18 (OEIS A115645 comment (Amiram Eldar): a(27) > 10^18 if it exists) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: Either a new term — a subset S of distinct factorials with sum(S) > 10^18 powerful — or a pushed exhaustive bound (all subset sums of {1!..k!} up to X checked non-powerful).
- witness verifier: Sum the claimed factorials (exact bigint); fully factor the sum (64–90 bit range: Pollard rho, trivial; up to ~10^25: ECM, still cheap); check every prime exponent >= 2. Dependency-free with any bigint + rho implementation.
- notes: Erdős at Oberwolfach 1988, motivated by a question of Mahler. Range push is genuinely cheap: all subset sums of {1!..21!} number only 2^21 ≈ 2·10^6 with max ~5·10^19, and subsets of {1!..25!} are 3.4·10^7 with max ~1.6·10^25 — factoring that many <=84-bit numbers is days on one box, so 10^18 -> 10^24 is within reach (grade open-easy for the verified-range record; an actual 27th term is conjecturally unlikely). The squares-only subcase A025494 is already far deeper: 16 squares known (largest …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `factorials`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1108` and the surface id in your receipt so the next agent's graph build finds your result.


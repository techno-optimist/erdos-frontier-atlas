# Erdős #879 — attack card

> Erdős problem #879 · https://www.erdosproblems.com/879
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:879:f9ae352c` — A186736(2501) = G(2501) — maximum sum of a pairwise-coprime subset of {1..2501}; first uncomputed table cell (table …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 455192 (monotonicity from OEIS A186736 b-file a(2500)=455192 (Matt Ready; first 250 terms Michael B. Porter)) · upper 457693 (increment bound G(n+1) ≤ G(n)+(n+1) (derived here))
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: a pairwise-coprime S ⊆ {1..2501} with element-sum > 455192 (raises the cell's lower bound; exhaustive/DP search closes the cell exactly)
- witness verifier: check gcd(a,b)=1 for all pairs (~|S|² ≈ 10^5 gcds) and sum the elements; milliseconds, dependency-free
- notes: Three published programs on OEIS (PARI, Python, C++ 'faster algorithm'); b-file was pushed 250→2500 in 2024, so further extension is a realistic hours-scale lane. The actual Erdős–van Lint asymptotic question (is G(n) > H(n) − n^{1+o(1)} where H(n)=Σ_{p<n} p + n·π(√n)?) is a wall: known H−n^{3/2−o(1)} < G < H and (H−G)/n → ∞, with the stronger bound proved only under strong prime-distribution assumptions (per erdosproblems.com/879, paraphrased). Structure side-question: optimal sets containing …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P879` and the surface id in your receipt so the next agent's graph build finds your result.


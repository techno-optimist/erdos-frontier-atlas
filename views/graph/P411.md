# Erdős #411 — attack card

> Erdős problem #411 · https://www.erdosproblems.com/411
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:411:345011a5` — Smallest solution n of φ(n)+φ(n+φ(n)) = n whose odd part is outside {1,3,5,7,35,47} — equivalently the smallest …[truncated; see source] (gap_map, kind `bounded_below_only`, confidence C3)

- bounds: lower 1e10 (no exceptional prime below this) (S. Steinerberger, arXiv:2504.08023 (2025); reduction and search bound as summarized in erdosproblems.com/411 remarks) · upper — (—)
- witness side: `upper` · witness feasibility `hard` · exact feasibility `unknown`
- witness object: a prime p ≡ 7 (mod 8), p > 1e10, satisfying φ((3p-1)/4) = (p+1)/2, supplied with the prime factorization of (3p-1)/4
- witness verifier: Check p prime (BPSW + certificate) and p ≡ 7 mod 8; re-multiply the claimed factorization of (3p-1)/4 and primality-check its factors; compute φ from the factorization; compare with (p+1)/2. Milliseconds given the certificate.
- notes: Min-type quantity: a found exceptional prime creates the upper bound and yields a brand-new solution family (disproving Cambie's conjecture that none exist). Heuristically the exceptional set may be empty — tied to whether φ(m) = 2(m+1)/3 has infinitely many solutions — so the cheap-witness hunt is graded hard. The complementary record (pushing the exclusion bound 1e10 → 1e12 with a segmented φ sieve over numbers ≡ 5 mod 6) is a days-scale job but is auditable only by recomputation, not by a …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `iterated functions`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P411` and the surface id in your receipt so the next agent's graph build finds your result.


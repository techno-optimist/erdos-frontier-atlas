# Erdős #488 — attack card

> Erdős problem #488 · https://www.erdosproblems.com/488
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: A finite, B = {n>=1 : a|n for some a in A}. Is |B∩[1,m]|/m < 2|B∩[1,n]|/n for every m > n …[truncated; see source])** — Passes criteria 1 and 3 cleanly, fails 2 and 4. The constant 2 is best possible and is approached only from below, only by singletons; every multi-element set we measured is strictly worse (<=1.85 vs 1.99), so a bounded sweep is orthogonal to the extremal direction and its negative result would be unpublishable ('no counterexample with max(A)<=24' says nothing about a->infinity). The finite-counterexample lane on this problem number has already been harvested against the mis-stated variant, and two upstream contributors are actively on the live one. Cheap to poke at, but no credible witness direction. [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/488.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $A$ be a finite set and $$B=\{ n \geq 1 : a\mid n\textrm{ for some }a\in A\}.$$ Is it true that, for every $m>n\geq \max(A)$, $$\frac{\lvert B\cap [1,m]\rvert }{m}< 2\frac{\lvert B\cap [1,n]\rvert}{n}?$$

## ATTACK SURFACES — which overlay said so

### `S:triage:488` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: A finite, B = {n>=1 : a|n for some a in A}. Is |B∩[1,m]|/m < 2|B∩[1,n]|/n for every m > n >= max(A)?
- witness: Concrete and small: a triple (A, n, m) with m > n >= max(A) violating the inequality. Verified by inclusion-exclusion over subsets of A using floor(x/lcm(S)) — exact integer arithmetic, no floats needed (compare cross-multiplied integers).
- smallest untested case: No size ladder exists, which is itself the problem. Our sweep this session: ALL A ⊆ [1,14] with |A| <= 4 (1,470 sets, x <= 4000) plus 74,041 random A with |A| <= 8 and max(A) <= 120 (x <= 8000). Max ratio found: 1.991667 at the singleton A={120}, m=240 (n=239). Best non-singleton: 1.849 at A={13,14}. Zero counterexamples.
- known frontier: No published sweep for THIS version. The counterexamples that exist (Cambie's A = primes <= n with m=2n; the concrete ones by Alexeev and Aristotle; the widely-reported Nov-2025 'AI disproved Erdos 488') are all against the [Er61] variant with a∤n instead of a|n, which is believed a typo — upstream restated the problem afterwards. That lane is already harvested. Two contributors (will0708, wesleyaweaverjr) are listed as currently working on the live version, 30 comments.
- search-space estimate: An exhaustive sweep of all A ⊆ [1,24] is 2^24 = 1.7x10^7 sets x an ~8000-term sieve = 1.3x10^11 ops ~ 1 workstation-day. But the extremal family is A={a}, n=2a-1, m=2a giving ratio (2a-1)/a -> 2 as a -> infinity — i.e. the reachable sweep direction (small max(A)) is precisely where the ratio is SMALLEST.
- verification: True
- verdict reason: Passes criteria 1 and 3 cleanly, fails 2 and 4. The constant 2 is best possible and is approached only from below, only by singletons; every multi-element set we measured is strictly worse (<=1.85 vs 1.99), so a bounded sweep is orthogonal to the extremal direction and its negative result would be unpublishable ('no counterexample with max(A)<=24' says nothing about a->infinity). The finite-counterexample lane on this problem number has already been harvested against the mis-stated variant, and two upstream contributors are actively on the live one. Cheap to poke at, but no credible witness direction.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).
- external claim (ai_claims, **HOT** — verification target): Erdős Problem #488 — corrected-target audit — our verdict: “not independently checked”. A claim never sets a status (standing rule 1).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P488` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #373 — attack card

> Erdős problem #373 · https://www.erdosproblems.com/373
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: solutions of n! = a_1! a_2! ... a_k! with n-1 > a_1 >= ... >= a_k >= 2; largest known n = …[truncated; see source])** — Hickerson list: 9! = 7!3!3!2!, 10! = 7!6! = 7!5!3!, 16! = 14!5!2!. The k=2 case is verified to n <= 10^3000 (Habsieger 2019, after Caldwell 1994) and Erdos 1993 proved a_1 >= n - 5 loglog n (Bhat-Ramachandra: constant 1/log2). Luca 2007: finiteness follows from abc; also would follow from P(n(n+1))/log n -> infinity (#368). The general-k exhaustive frontier is still McCranie's 2005 check at 10^5 — softest verified bound in this chunk; candidates require n, n-1 composite plus a prime-free tail …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/373.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Show that the equation `n!=a_1!a_2!···a_k!`, with `n−1 > a_1 ≥ a_2 ≥ ··· ≥ a_k`, has only finitely many solutions.

## ATTACK SURFACES — which overlay said so

### `S:gap:373:038ea500` — solutions of n! = a_1! a_2! ... a_k! with n-1 > a_1 >= ... >= a_k >= 2; largest known n = 16, Hickerson list …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 100000 (exhaustive check: no solutions with 16 < n <= 10^5 (Jud McCranie, OEIS A003135 comment)) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: either a new solution n > 16 (would extend the Hickerson list {9, 10, 10, 16} and be a genuine event) or an extension of the exhaustive no-solution verification beyond the soft 10^5 frontier
- witness verifier: prime-valuation comparison: v_p(n!) = sum floor(n/p^i) must equal sum_j v_p(a_j!) for every prime p <= n — pure integer arithmetic, milliseconds even for very large n; no big factorial products needed
- notes: Hickerson list: 9! = 7!3!3!2!, 10! = 7!6! = 7!5!3!, 16! = 14!5!2!. The k=2 case is verified to n <= 10^3000 (Habsieger 2019, after Caldwell 1994) and Erdos 1993 proved a_1 >= n - 5 loglog n (Bhat-Ramachandra: constant 1/log2). Luca 2007: finiteness follows from abc; also would follow from P(n(n+1))/log n -> infinity (#368). The general-k exhaustive frontier is still McCranie's 2005 check at 10^5 — softest verified bound in this chunk; candidates require n, n-1 composite plus a prime-free tail …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `factorials`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P373` and the surface id in your receipt so the next agent's graph build finds your result.


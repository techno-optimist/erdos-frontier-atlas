# Erdős #386 — attack card

> Erdős problem #386 · https://www.erdosproblems.com/386
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/386.lean)

## STATEMENT (Apache-licensed, Lean-derived)

There is a $k$, such that $2 \le k \le n - 2$ and $\binom{n}{k}$ can be the product of consecutive primes infinitely often?

## ATTACK SURFACES — which overlay said so

### `S:gap:386:7e9ae554` — A280992(8) — the next squarefree triangular number that is a product of consecutive primes (equivalently the next n > …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower > 12500002500000 (OEIS A280992: exhaustive over the first 5·10^6 triangular numbers (T(5e6) = 12500002500000); Corneth: any further term has a prime factor > prime(2000) = 17389) · upper — (—)
- witness side: `upper` · witness feasibility `hard` · exact feasibility `unknown`
- witness object: A triangular number T > 255255 equal to a product of consecutive primes p_i·p_{i+1}···p_j (squarefreeness automatic for distinct consecutive primes)
- witness verifier: Check 8T+1 is a perfect square (integer sqrt), factor T, and confirm its prime factors are consecutive primes each to the first power. Big-int arithmetic only, milliseconds. Search-side: enumerate consecutive-prime-window products P < X and test 8P+1 square — no triangular-number scan needed.
- notes: Known n with C(n,2) such a product: 4, 6, 15, 21, 715 (terms 6, 15, 105, 210, 255255). Erdős–Graham [ErGr80 p.74] wrote that proving finiteness even for C(n,2) seems hopeless; their guess that it never happens for 3<=k<=n−3 was refuted by Weisenberg: C(7,3)=5·7, C(10,4)=2·3·5·7, C(14,4)=7·11·13, C(15,6)=5·7·11·13. Cheap record available: the exhaustive floor 1.25e13 can be pushed to ~1e24 with only ~4e10 square tests (prime pairs p·p'' with p <= 1e12 dominate; longer windows are far fewer) — …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `binomial coefficients`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P386` and the surface id in your receipt so the next agent's graph build finds your result.


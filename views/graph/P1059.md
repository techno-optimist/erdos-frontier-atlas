# Erdős #1059 — attack card

> Erdős problem #1059 · https://www.erdosproblems.com/1059
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Infinitude of Erdos primes: primes p with p - k! composite for every k with 1 <= k! < p …[truncated; see source])** — Guy A2 (3rd ed., p. 11). Heuristic count #E(x) ~ (1/8) x / log x (OEIS comment, fitted to census through 10^9) strongly suggests infinitude, i.e. positive relative density among primes - which is exactly why new examples carry no information. Erdos's suggested easier variant (per erdosproblems 1059 remark): show infinitude of n in (l!, (l+1)!] whose prime factors all exceed l and with n - k! composite for 1 <= k <= l; still not witness-shaped. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/1059.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Are there infinitely many primes $p$ such that $p - k!$ is composite for each $k$ such that $1 ≤ k! < p$?

## ATTACK SURFACES — which overlay said so

### `S:gap:1059:62999fa4` — Infinitude of Erdos primes: primes p with p - k! composite for every k with 1 <= k! < p (A064152) (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none for the infinitude question - individual Erdos primes are plentiful (b-file has 10000 terms; 5901409 below 10^9 per OEIS comment census) and finding more settles nothing
- witness verifier: n/a for the open question. Single-prime membership IS cheaply checkable (for each k with k! < p, test p - k! for compositeness via BPSW), but it bounds no quantity.
- notes: Guy A2 (3rd ed., p. 11). Heuristic count #E(x) ~ (1/8) x / log x (OEIS comment, fitted to census through 10^9) strongly suggests infinitude, i.e. positive relative density among primes - which is exactly why new examples carry no information. Erdos's suggested easier variant (per erdosproblems 1059 remark): show infinitude of n in (l!, (l+1)!] whose prime factors all exceed l and with n - k! composite for 1 <= k <= l; still not witness-shaped.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1059` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #458 — attack card

> Erdős problem #458 · https://www.erdosproblems.com/458
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Falsifiable Erdos-Graham inequality: lcm(1..p_{k+1}-1) < p_k * lcm(1..p_k) for all k >= …[truncated; see source])** — erdosproblems.com marks this FALSIFIABLE (single-k counterexample possible) and 'almost certainly true but proof beyond reach': ruling out two primes-squared in one prime gap is Legendre-conjecture strength. A counterexample needs a prime-free interval of length ~2*sqrt(p_k) — astronomically beyond all known gaps, so the falsification lane is dead; the honest record is the verified range. Cheap extension route to 4e18: all prime gaps below 4e18 are < 1476 (Oliveira e Silva-Herzog-Pardi …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/458.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $\operatorname{lcm}(1, \dots, n)$ denote the least common multiple of $\{1, \dots, n\}$. Let $p_k$ be the $k$-th prime. Is it true that for all $k \geq 1$, $\operatorname{lcm}(1, \dots, p_{k+1}-1) < p_k \cdot \operatorname{lcm}(1, \dots, p_k)$?

## ATTACK SURFACES — which overlay said so

### `S:triage:458` — triage verdict **MAYBE** (finite_handle_triage, 2026-07-26)

- our summary: Is [1,...,p_{k+1}-1] < p_k [1,...,p_k] for all k, where [1,...,n] = lcm(1,...,n)?
- witness: Concrete and tiny. Reformulation (verified by us): [1..p_{k+1}-1]/[1..p_k] = product of q over all perfect prime powers q^j (j>=2) lying in the gap (p_k, p_{k+1}). So a witness is a single prime gap whose contained prime powers have base-product >= p_k. Since q^2 > p_k forces q > sqrt(p_k), ANY gap containing two prime squares is a counterexample.
- smallest untested case: p_k > 10^15 (our own run covers everything below). Structurally the first candidate is the first prime gap containing two prime squares — none exists below 10^15.
- known frontier: Upstream remarks give NO computational range (only the two obstructions: ruling out many q with p_k<q^2<p_{k+1}, essentially Legendre; and 'small primes cause trouble'). Our run this session: exhaustive over all 1,962,689 prime powers q^j (j>=2) below 10^15 in 96 s — zero counterexamples; only 5 gaps in all of [1,10^15] contain more than one prime power ((7,11),(23,29),(113,127),(2179,2203),(32749,32771)); max ratio product/p_k = 6/7 = 0.857, attained at the very FIRST case p_k=7. The 'small …[truncated; see source]
- search-space estimate: Cost is one prevprime/nextprime pair per prime power: pi(sqrt(N)) calls. N=10^15 -> 1.96x10^6 calls = 96 s (measured). N=10^18 -> pi(10^9) = 5.08x10^7 calls ~ 1 hour. Witness side: two prime squares in one gap needs p_{k+1}-p_k > (q2-q1)(q2+q1) >= 4 sqrt(p_k), i.e. >4x10^9 at p=10^18, versus the largest known gap below 10^18 of 1476 — short by ~10^6.5.
- verification cheap and exact: yes
- verdict reason: The witness hunt is a WALL (needs a prime gap ~10^6 times longer than anything Cramer permits — it is Legendre in disguise). But this is the one entry where the NEGATIVE certificate is genuinely cheap, monotone and unpublished: 'verified for all k with p_k < 10^18' is a dependency-free integer-only frontier statement, ~1 hour of compute, and upstream lists no range at all. Fits our verified-up-to-N records rung exactly. Do NOT fund a witness search; DO consider the 1-hour certificate plus the lcm-ratio reformulation (which makes the check O(pi(sqrt N)) instead of anything involving lcm's).
- applies: **finite-handle witness search** (predicate `triage-move`)

### `S:gap:458:0195eb7c` — Falsifiable Erdos-Graham inequality: lcm(1..p_{k+1}-1) < p_k * lcm(1..p_k) for all k >= 1; record = largest X with the …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 1e12 (this chunk's computation (2026-07-18): exact identity reduces the inequality to 'product of bases of proper prime powers sharing the gap (p_k, p_{k+1}) is < p_k'; all 80070 proper prime powers <= 1e12 enumerated, only 5 multi-power gaps exist — (8,9),(25,27),(121,125),(2187,2197),(32761,32768) — all safe by wide margin; identity validated against direct bignum lcm on 60 random gaps below 1e4; primality via deterministic 12-base Miller-Rabin (valid < 3.3e24)) · upper — (—)
- witness side: `both` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: Upper/closing witness: a counterexample k, i.e. a prime gap (p_k, p_{k+1}) containing proper prime powers whose base product >= p_k — essentially two prime squares in one gap (square-cube pairs only give ~p^{5/6} < p_k). Lower/record witness: a rerun of the reduction certificate to larger X (list of all multi-power gaps <= X with their base products).
- witness verifier: Fully dependency-free: enumerate proper prime powers q^j <= X (only ~pi(sqrt(X)) squares dominate), sort, test for a prime strictly between consecutive powers (Miller-Rabin), and for each cohabiting group compare product of bases against prevprime. Single-power gaps are automatically safe for m > 4 …[truncated; see source]
- notes: erdosproblems.com marks this FALSIFIABLE (single-k counterexample possible) and 'almost certainly true but proof beyond reach': ruling out two primes-squared in one prime gap is Legendre-conjecture strength. A counterexample needs a prime-free interval of length ~2*sqrt(p_k) — astronomically beyond all known gaps, so the falsification lane is dead; the honest record is the verified range. Cheap extension route to 4e18: all prime gaps below 4e18 are < 1476 (Oliveira e Silva-Herzog-Pardi …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P458` and the surface id in your receipt so the next agent's graph build finds your result.


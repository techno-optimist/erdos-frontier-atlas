# Erdős #710 — attack card

> Erdős problem #710 · https://www.erdosproblems.com/710
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Growth order of f(n) = least L such that (n, n+L) contains distinct a_1,...,a_n with k | …[truncated; see source])** — Erdos offered 2000 rupees (site converts to ~$78) for an asymptotic formula. The gap is the (loglog n)^{1/2} factor plus constants (2/sqrt(e) ≈ 1.213 vs 1.7398...). Freshness: 2026 work by van Doorn (arXiv:2601.16972, Integers 2026) resolves the max_m variant (#711 — intervals of length c·n·log n/loglog n with no system of distinct multiples) but does NOT move #710's 1980 bounds; also arXiv:2603.28636 (van Doorn-Li-Tang 2026) is problem #650, not #710. I verified A390246 = f(n) for n=1..15 by …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures
- prize: ₹2000 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:gap:710:3e6e20e3` — Growth order of f(n) = least L such that (n, n+L) contains distinct a_1,...,a_n with k | a_k (A390246) (gap_map, kind `value_gap`, confidence C3)

- bounds: lower >= (2/sqrt(e) + o(1))·n·(log n/loglog n)^{1/2} (Erdos-Pomerance [ErPo80], per erdosproblems.com/710) · upper <= (1.7398...+o(1))·n·(log n)^{1/2} (Erdos-Pomerance [ErPo80], per erdosproblems.com/710)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None for the asymptotic gap — per-n f(n) is poly-time computable (bipartite matching between indices 1..n and their multiples in the interval), so single values are cells, not records; A390246 already has 10000 terms (a(10000)=20626).
- witness verifier: For a claimed f(n)=L: exhibit a perfect matching in (n, n+L) (verify k | a_k, distinctness) and for L-1 a Hall-violator certificate (a set S of indices whose available multiples number < |S|). Both checks are trivial.
- notes: Erdos offered 2000 rupees (site converts to ~$78) for an asymptotic formula. The gap is the (loglog n)^{1/2} factor plus constants (2/sqrt(e) ≈ 1.213 vs 1.7398...). Freshness: 2026 work by van Doorn (arXiv:2601.16972, Integers 2026) resolves the max_m variant (#711 — intervals of length c·n·log n/loglog n with no system of distinct multiples) but does NOT move #710's 1980 bounds; also arXiv:2603.28636 (van Doorn-Li-Tang 2026) is problem #650, not #710. I verified A390246 = f(n) for n=1..15 by …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P710` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #211 — attack card

> Erdős #211 — Beck/Szemerédi-Trotter (finite residue: orchard 3-point lines) · https://www.erdosproblems.com/211
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (problem)** — same ExistsR/collinearity obstruction as #101 - orchard-type equality-constrained geometry (R3 fails) [atlas/problems.json wall_reason]

## STATUS — three registers, never merged

- ours: **solved-upstream** (atlas/stubs.json)
- upstream: **Solved** (raw `proved`)
- formal: not in formal-conjectures
- prize: $100 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:deep:211` — deep audit: **WALL**, lane `wall`, board **NONE** (problems.json)

- finite object: A set of n real-plane points (rational/algebraic coords) with no 4 collinear, maximizing the number of exactly-3-point lines. Concrete open targets: a 32-line witness at n=15, a 42-line witness at n=17, or improved lower bounds for n=18–25.
- current record: Orchard t3(n) (OEIS A003035, updated Sep 2025): exact proven only through n=14 (a(14)=26). OPEN: a(15)∈{31,32}, a(17)∈{40,41,42}; a(16)=37 known. Green–Tao 2013 (arXiv:1208.4714): a(n)=⌊n(n-3)/6⌋+1 for sufficiently large n (ineffective threshold; gives 31 at n=15). Lower bounds to n=25 in Erich Friedman's table. Constructions: Burr–Grünbaum–Sloane 1974 (elliptic functions), Pegg/Zhao Hui Du (Mathematica), Padmanabhan–Shukla 2020 (elliptic curves over F_p). Headline #211 itself is SOLVED (Beck; Szemerédi–Trotter 1983), $100 prize not claimable.
- verifier: Exact and cheap: for a candidate point set, test every one of the C(n,3) triples for collinearity via an integer/rational 3×3 determinant, reject if any line holds ≥4 points, count exactly-3-point lines. O(n³) = 455 determinant checks at n=15 — trivial, fully exact. (Verifier is clean; it's the SEARCH/nonexistence-certificate that is the wall.)
- attack: Only viable lane: enumerate structured algebraic families with rational/finite-field incidences rather than blind coordinate search. Concretely, search Padmanabhan–Shukla-style configs — n-point subsets of elliptic curves over F_p (or torsion/group-law patterns on plane cubics) — for a 32-line arrangement at n=15 or lower-bound improvements at n=18–25, each candidate exact-verified by the determinant checker. Compute is modest (single machine, hours) because the family is parametrized; the CPU farm adds little since the bottleneck is finding the right algebraic structure, not raw enumeration. Nonexistence (a(15)≤31) is out of reach: no faithful DRAT route past the realizability gap.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #669 shares A003035 → card `views/graph/P669.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P211` and the surface id in your receipt so the next agent's graph build finds your result.


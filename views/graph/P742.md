# Erdős #742 — attack card

> Erdős problem #742 · https://www.erdosproblems.com/742
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Murty–Simon conjecture: a diameter-2-critical graph on n vertices (deleting any edge …[truncated; see source])** — This is the canonical example of the trap the brief warns about: DECIDABLE here means "resolved up to a finite check" where the finite check is n ≤ tower_2(10^14). Criterion 3 passes beautifully — checking a candidate is O(n·m) integer work — and criterion 1 passes, but 2 and 4 both fail hard, and they fail from opposite directions. From below, the small end is already swept and proved: Fan closed n ≤ 24 and n = 26 by hand in 1987, and Kirchweger–Szeider enumerated the entire class to n = 13, so there is nothing to pick up cheaply. From above, Füredi proved the conjecture outright for large n, so the only place a counterexample could hide is n = 25 or n ≥ 27, where the isomorphism class …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `decidable`, finite handle `decidable`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:triage:742` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: Murty–Simon conjecture: a diameter-2-critical graph on n vertices (deleting any edge increases the diameter) has at most n²/4 edges, with K_{n/2,n/2} extremal.
- witness: A diameter-2-critical graph on n vertices with more than ⌊n²/4⌋ edges — a single adjacency matrix. Verification of a candidate is genuinely trivial (all-pairs distances, then one edge-deletion diameter check per edge: O(n·m) integer work). The witness is concrete and cheap to check; that is not the problem here.
- smallest untested case: n = 25, then n ≥ 27. Fan, Discrete Math. 67 (1987) 235–240, proved the conjecture for all n ≤ 24 and for n = 26. The "finite check" that the DECIDABLE tag refers to is the whole interval [25, n₀) where n₀ is Füredi's threshold.
- known frontier: Exhaustive enumeration of diameter-2-critical graphs: Radosavljević and Živković computed all of them up to 10 vertices with nauty; Kirchweger and Szeider pushed this to 13 vertices with SMS (SAT modulo symmetries). Theory: Plesník (1975) |E| < 3n(n−1)/8; Fan (1987) n ≤ 24 and n = 26; Haynes–Henning et al. on Murty–Simon I/II; Füredi, "The maximum number of edges in a minimal graph of diameter 2" (1992), proves it for all n ≥ n₀ via the Ruzsa–Szemerédi (regularity) theorem — and that n₀ is a …[truncated; see source]
- search-space estimate: Graphs on 25 vertices up to isomorphism ≈ 2^C(25,2)/25! = 2^300/25! ≈ 2.0×10^90 / 1.55×10^25 ≈ 1.3×10^65. The best exhaustive machinery on this exact graph class (SMS) reaches n = 13 — about 12 orders of magnitude short at n=25, and the theoretical gap runs to a tower of 2's of height 10^14, which is not a number that admits arithmetic.
- verification cheap and exact: yes
- verdict reason: This is the canonical example of the trap the brief warns about: DECIDABLE here means "resolved up to a finite check" where the finite check is n ≤ tower_2(10^14). Criterion 3 passes beautifully — checking a candidate is O(n·m) integer work — and criterion 1 passes, but 2 and 4 both fail hard, and they fail from opposite directions. From below, the small end is already swept and proved: Fan closed n ≤ 24 and n = 26 by hand in 1987, and Kirchweger–Szeider enumerated the entire class to n = 13, so there is nothing to pick up cheaply. From above, Füredi proved the conjecture outright for large n, so the only place a counterexample could hide is n = 25 or n ≥ 27, where the isomorphism class …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P742` and the surface id in your receipt so the next agent's graph build finds your result.


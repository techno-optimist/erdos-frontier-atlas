# Erdős #936 — attack card

> Erdős problem #936 · https://www.erdosproblems.com/936
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Brocard sub-case search frontier: largest N such that no n in (7, N] has n!+1 a perfect …[truncated; see source])** — Range extension is sequential-per-prime but parallel-across-primes: ~1e12 mulmods per prime chain = hours on one box. Only the square sub-case is machine-frontier; testing n!±1 or 2^n±1 for POWERFUL requires full factorization (no cheap search; 2^n±1 factor state = Cunningham tables). Known powerful value: 2^3+1=9. Conditional results: Cushing–Pascoe 2016 (abc ⇒ finitely many powerful values near n!), CrowdMath 2020 (abc ⇒ same for 2^n±1), per erdosproblems #936 remarks. Wasserman 2005: no …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/936.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:936:4a2e3a25` — Brocard sub-case search frontier: largest N such that no n in (7, N] has n!+1 a perfect square (A146968 = {4,5,7}); the …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 1e12 (Matson quadratic-residue search (unsolvedproblems.org S73.pdf), reported in OEIS A085692 comment by Radcliffe; rigorous published records: 1e9 Berndt–Galway 2000, 4e9 Tim Peters 2006) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: either (a) an extended exhaustive-scan claim 'no n ≤ N with n!+1 square' for N > 1e12, or (b) a 4th n with n!+1 a perfect square (sporadic-solution shock outcome)
- witness verifier: (b) verifies in seconds: incremental r_p = n! mod p over ~30 primes, quadratic-residue filter, then exact integer-sqrt of n!+1. (a) has no cheap certificate — verification = independent re-run of the residue sieve (trust-the-log record)
- notes: Range extension is sequential-per-prime but parallel-across-primes: ~1e12 mulmods per prime chain = hours on one box. Only the square sub-case is machine-frontier; testing n!±1 or 2^n±1 for POWERFUL requires full factorization (no cheap search; 2^n±1 factor state = Cunningham tables). Known powerful value: 2^3+1=9. Conditional results: Cushing–Pascoe 2016 (abc ⇒ finitely many powerful values near n!), CrowdMath 2020 (abc ⇒ same for 2^n±1), per erdosproblems #936 remarks. Wasserman 2005: no …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #398 shares A146968 → card `views/graph/P398.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P936` and the surface id in your receipt so the next agent's graph build finds your result.


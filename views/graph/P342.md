# Erdős #342 — attack card

> Erdős problem #342 · https://www.erdosproblems.com/342
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Computation frontier for the Ulam sequence u(1)=1, u(2)=2: largest X with all Ulam …[truncated; see source])** — Erdős/Ulam questions — density 0? infinitely many pairs differing by 2? eventually periodic differences? — are all asymptotic (wall); computation refutes density 0 empirically (density stable ~0.0740). Known structure: clumps with period 21.601584+, ratio to mean gap 13.52 close to 8/5; a(1e9)=13517664323. erdosproblems.com/342 flags claimed partial results in 18 unincorporated comments (accessed 2026-07-18) — scout should re-check before frontier work. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/342.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:342:446d7695` — Computation frontier for the Ulam sequence u(1)=1, u(2)=2: largest X with all Ulam numbers <= X computed, supporting …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 1000000000000 (Gibbs & McCranie, 'The Ulam Numbers up to One Trillion' (2017): 73979274540 Ulam numbers below 10^12; separately McCranie computed the first 2.8e10 terms (2015-16) — still the frontier per OEIS A002858 links (checked 2026-07-18)) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `wall`
- witness object: An extended frontier X' > 10^12 (all Ulam numbers <= X' with counts, density, wave statistics, and consecutive-pair census), computed with Gibbs's linear bitwise-convolution algorithm
- witness verifier: No compact certificate: the sequence is deterministic, so verification = independent recomputation (linear-time bitset convolution, ~days at 10^12 scale); spot-audits of published counts (e.g. 73979274540 below 1e12, only x=1,2,3,47 with x,x+1 both Ulam in first 2.8e10 terms) guard transcription …[truncated; see source]
- notes: Erdős/Ulam questions — density 0? infinitely many pairs differing by 2? eventually periodic differences? — are all asymptotic (wall); computation refutes density 0 empirically (density stable ~0.0740). Known structure: clumps with period 21.601584+, ratio to mean gap 13.52 close to 8/5; a(1e9)=13517664323. erdosproblems.com/342 flags claimed partial results in 18 unincorporated comments (accessed 2026-07-18) — scout should re-check before frontier work.
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P342` and the surface id in your receipt so the next agent's graph build finds your result.


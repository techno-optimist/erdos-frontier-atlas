# Erdős #693 — attack card

> Erdős problem #693 · https://www.erdosproblems.com/693
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Asymptotic order of the max gap between consecutive integers in [n, n^k] having a divisor …[truncated; see source])** — No nontrivial proven bounds are cited on erdosproblems.com/693 (only a cross-ref to #446); trivial upper: max gap <= ~n+2 since consecutive multiples of a fixed d in (n,2n) are <= 2n apart. A391118 (Beregovsky, created 2026-01) has terms n=3..83, values reaching only 12 — consistent with polylog but non-probative. Table extension is easy compute, not record-shaped; no bound moves. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:693:80f03417` — Asymptotic order of the max gap between consecutive integers in [n, n^k] having a divisor in (n, 2n) (k=2 table: …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None — the open content is whether the max gap is polylog with an unspecified O(1) exponent, so any finite computation is consistent with both answers. Per-n max gap is exactly computable.
- witness verifier: Per-n value check: enumerate multiples of each d in (n,2n) within [n,n^2], merge-sort, scan gaps; naive O(n^2) works to n in the low thousands. I reproduced A391118(3..12) this way.
- notes: No nontrivial proven bounds are cited on erdosproblems.com/693 (only a cross-ref to #446); trivial upper: max gap <= ~n+2 since consecutive multiples of a fixed d in (n,2n) are <= 2n apart. A391118 (Beregovsky, created 2026-01) has terms n=3..83, values reaching only 12 — consistent with polylog but non-probative. Table extension is easy compute, not record-shaped; no bound moves.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `divisors`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P693` and the surface id in your receipt so the next agent's graph build finds your result.


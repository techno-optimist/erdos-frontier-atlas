# Erdős #1055 — attack card

> Erdős problem #1055 · https://www.erdosproblems.com/1055
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/1055.lean)

## STATEMENT (Apache-licensed, Lean-derived)

A prime $p$ is in class $1$ if the only prime divisors of $p+1$ are $2$ or $3$. In general, a prime $p$ is in class $r$ if every prime factor of $p+1$ is in some class $\leq r-1$, with equality for at least one prime factor. Are there infinitely many primes in each class?

## ATTACK SURFACES — which overlay said so

### `S:gap:1055:95bfb76b` — A005113(20) - least prime in class 20 of the Erdos-Selfridge classification (class via largest-class prime factor of …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 131988573220065 (= 2*a(19) - 1 with a(19) = 65994286610033 (OEIS A005113 data); inequality a(r+1) >= 2*a(r) - 1 proven in A005113 comment (M. F. Hasler)) · upper 764276710625653 (M. F. Hasler, OEIS A005113 comment (Apr 2007); = 2q-1 with q = 382138355312827; p and q verified prime locally 2026-07-18 (sympy); the class-19 status of q rests on Hasler's 2007 computation, not independently re-verified)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `unknown`
- witness object: A prime p < 764276710625653 with a full recursion tree certifying class exactly 20: the factorization of p+1, the class of each odd prime factor (max must be 19, attained), recursing down to class-1 leaves (q+1 having only prime factors 2 and 3)
- witness verifier: Recursively factor q+1 for every prime q in the tree (all <= 7.7e14: trial division + Pollard rho, milliseconds each); class(q) = 1 + max class over odd prime factors of q+1; base case: q+1 is 3-smooth. Entire certificate replays in seconds in plain Python.
- notes: Underlying open problem (Guy A18, Erdos 1977): are there infinitely many primes in each class, and does p_r^(1/r) tend to infinity (Erdos) or stay bounded (Selfridge)? Known: number of class-r primes <= n is n^(o(1)). Structure for search: any class-20 prime p < 7.6e14 has p+1 = t*q with q a class-19 prime (q >= a(19) = 6.6e13) and even t <= 11, so the bottleneck is enumerating class-19 primes below 3.8e14 - recursive, days not hours. Data ends at a(19) (19 terms, keyword more, no b-file); both …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1055` and the surface id in your receipt so the next agent's graph build finds your result.


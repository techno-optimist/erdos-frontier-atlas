# Erdős #1061 — attack card

> Erdős problem #1061 · https://www.erdosproblems.com/1061
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: S(x) = number of ordered pairs (a,b) with a+b <= x and sigma(a) + sigma(b) = sigma(a+b): …[truncated; see source])** — IMPORTANT FRESHNESS FLAG: a June 2026 preprint claims to RESOLVE this problem negatively - Eric Li (Trinity College, Cambridge), 'A resolution of Erdos Problem 1061 on the sum-of-divisors function', arXiv:2606.25849 (submitted 2026-06-24), proving S(x)/(x (log x)^R) -> infinity for every R > 0, refuting the ~c*x guess. The preprint is linked from OEIS A110177 but is NOT peer-reviewed, and erdosproblems.com/1061 still shows OPEN as of 2026-07-18 (comment activity pending). Recorded as open per …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/1061.lean)

## STATEMENT (Apache-licensed, Lean-derived)

How many (ordered) solutions are there to `σ(a) + σ(b) = σ(a + b)` with `a + b ≤ x`? Is it true that this number is asymptotic to `c * x` for some constant `c > 0`?

## ATTACK SURFACES — which overlay said so

### `S:gap:1061:8ff71c9a` — S(x) = number of ordered pairs (a,b) with a+b <= x and sigma(a) + sigma(b) = sigma(a+b): is S(x) ~ c*x for some c > 0? (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none - asymptotic counting question; finite S(x) values (computable by partial-summing A110177, b-file to n = 16384) bound nothing
- witness verifier: n/a
- notes: IMPORTANT FRESHNESS FLAG: a June 2026 preprint claims to RESOLVE this problem negatively - Eric Li (Trinity College, Cambridge), 'A resolution of Erdos Problem 1061 on the sum-of-divisors function', arXiv:2606.25849 (submitted 2026-06-24), proving S(x)/(x (log x)^R) -> infinity for every R > 0, refuting the ~c*x guess. The preprint is linked from OEIS A110177 but is NOT peer-reviewed, and erdosproblems.com/1061 still shows OPEN as of 2026-07-18 (comment activity pending). Recorded as open per …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1061` and the surface id in your receipt so the next agent's graph build finds your result.


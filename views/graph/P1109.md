# Erdős #1109 — attack card

> Erdős problem #1109 · https://www.erdosproblems.com/1109
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:1109:8259c217` — A392165(40) — smallest N with f(N) >= 40, where f(N) = A392164(N) = size of the largest S ⊆ {1..N} such that every …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 39 (f exactly computed through N = 1103; 39th record index = 1103) (OEIS A392164 + A392165 (Beregovsky Jan 2026; A392165 comment by Chai Wah Wu)) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: A subset S ⊆ {1..N} with |S| = 40 and every pairwise sum a+a' (including a=a') squarefree — certifies f(N) >= 40 and brackets A392165(40); the exact record index additionally needs exhaustive max-clique confirmation for smaller N.
- witness verifier: For each of the ~820 pairs (a,a') with a <= a' in S, trial-factor a+a' <= 2N to sqrt(2N) and check no square divisor. Milliseconds, fully dependency-free.
- notes: Erdős–Sárközy 1987 (Acta Math. Hungar.): log N << f(N) << N^(3/4) log N; best known asymptotics are Konyagin 2004: loglog N (log N)^2 << f(N) << N^(11/15+o(1)) (OEIS formula lines + erdosproblems.com/1109; van Doorn–Tao arXiv:2512.01087 revisits the problem family). Erdős–Sárközy guessed the lower bound is nearer the truth; the problem asks if f(N) <= N^o(1). Machine lane: build the compatibility graph on {1..N} (vertex a admissible iff 2a squarefree; edge iff a+a' squarefree) and run an exact …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #1103 shares A392164 → card `views/graph/P1103.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1109` and the surface id in your receipt so the next agent's graph build finds your result.


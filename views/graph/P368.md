# Erdős #368 — attack card

> Erdős problem #368 · https://www.erdosproblems.com/368
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: growth of F(n) = largest prime factor of n(n+1) — conjectured F(n) >> (log n)^2 always, …[truncated; see source])** — State of the art verified: Polya 1918 (F -> infinity), Mahler 1935 (>> loglog n), Schinzel 1967-68 (F(n) <= n^{O(1/logloglog n)} i.o.; liminf F(n)/loglog n >= 2/7 effective), and the current best lower bound Pasten 2024 (arXiv:2312.03566, Cor. 1.5): F(n) >> (loglog n)^2 / logloglog n. Empirical sub-lane: Stormer-Lehmer Pell enumeration of p-smooth n(n+1) pairs (largest-n-per-prime table = A002072) supplies certified record smooth pairs; a miner must source the current largest completed …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:368:d9972a8a` — growth of F(n) = largest prime factor of n(n+1) — conjectured F(n) >> (log n)^2 always, with F(n) < (log n)^{2+eps} …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none for the asymptotic conjecture; nearest checkable object is a large n with F(n) far below (log n)^2 (a very smooth pair n, n+1), each verifiable by factoring but deciding nothing
- witness verifier: n/a for the conjecture; a single smooth-pair example is verified by factoring n and n+1 (milliseconds)
- notes: State of the art verified: Polya 1918 (F -> infinity), Mahler 1935 (>> loglog n), Schinzel 1967-68 (F(n) <= n^{O(1/logloglog n)} i.o.; liminf F(n)/loglog n >= 2/7 effective), and the current best lower bound Pasten 2024 (arXiv:2312.03566, Cor. 1.5): F(n) >> (loglog n)^2 / logloglog n. Empirical sub-lane: Stormer-Lehmer Pell enumeration of p-smooth n(n+1) pairs (largest-n-per-prime table = A002072) supplies certified record smooth pairs; a miner must source the current largest completed …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #683 shares A074399 → card `views/graph/P683.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P368` and the surface id in your receipt so the next agent's graph build finds your result.


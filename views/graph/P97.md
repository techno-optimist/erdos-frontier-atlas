# Erdős #97 — attack card

> Convex polygon: every vertex has 4 equidistant others · https://www.erdosproblems.com/97
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **TRAP** — prize **$100** + upstream says `falsifiable`, but our status is **wall**: the finite handle does not help at our scale (atlas/walls.md, finite-handle table). The prize is the lure; this line is the hook.
- **WALL (problem)** — existence bounty with no numeric frontier, and the defining condition (4 equidistant points) is equality-constrained real geometry (R3 fails: rational restriction lossy) [atlas/problems.json wall_reason]
- **WALL (surface: Does every convex polygon have a vertex with no other 4 vertices equidistant from it? …[truncated; see source])** — Fails criteria 2, 3 and (fatally) the premise of 4. The falsification target is a point in a semialgebraic set, not a member of a finite list: there is no n to search at, because a counterexample may have any number of vertices and failing at n<=30 proves nothing. Even after fixing n the combinatorial layer alone is ~10^71 at n=20 and each leaf needs a polynomial-system solve, i.e. a CAS. Verification is only exact if a witness happens to have rational or low-degree algebraic coordinates, which nothing guarantees -- the constraint system is overdetermined (3n equations, 2n-4 degrees of freedom), so known witnesses at k=3 exist only through imposed symmetry, and hunting symmetric families is …[truncated; see source] [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **wall** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/97.lean)
- prize: $100 — prizes are history, not targets (standing rule 2).

## STATEMENT (Apache-licensed, Lean-derived)

Does every convex polygon have a vertex with no other 4 vertices equidistant from it?

## ATTACK SURFACES — which overlay said so

### `S:triage:97` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: Does every convex polygon have a vertex with no other 4 vertices equidistant from it? ($100, tagged FALSIFIABLE. Erdos's route to #96.)
- witness: A convex polygon on n vertices (n points in convex position in R^2) such that EVERY vertex v has at least 4 other vertices lying on a common circle centred at v. Concrete as an object; but the coordinates are real, and nothing forces them rational or even algebraic of low degree. The Lean spec (FormalConjectures/ErdosProblems/97.lean) pins it as ConvexIndep A with HasNEquidistantProperty 4 A over Finset R^2 -- note it quantifies over ALL finite A, with no bound on |A|.
- smallest untested case: There is none in the usable sense: n is unbounded and the conjecture has no smallest open instance. The state of the art is on the k=3 (not k=4) rung -- Danzer built a convex 9-gon with 3 equidistant vertices per vertex (distance varying by vertex), and Fishburn-Reeds [FiRe92] a convex 20-gon with 3 equidistant per vertex at a COMMON distance, and proved 20 is minimal for their cut variant. For k=4 nobody has published even a candidate n to aim at.
- known frontier: Danzer, 9-point convex polygon, k=3, construction reported in Erdos [Er87b]. Fishburn and Reeds, 'Unit distances between vertices of a convex polygon', Comput. Geom. 1992: convex 20-gon with k=3 at a uniform distance, and 20 proven minimal for the two-part-cut version (formalised as erdos_97.variants.three_unit_distance_cut_min in google-deepmind/formal-conjectures). No published exhaustive or heuristic search for k=4 at any n. The erdosproblems page (last edited 27 Oct 2025) reports no …[truncated; see source]
- search-space estimate: Fixing n and enumerating the combinatorial type first: each of n vertices picks which 4 of the other n-1 are equidistant from it, so C(n-1,4)^n types; at n=20 (the Fishburn-Reeds scale for k=3) that is 3876^20 ~ 10^71.6 -- and each surviving type is then a real-algebraic feasibility problem with 3n = 60 equations in 2n-4 = 36 similarity-reduced unknowns.
- verification: False
- verdict reason: Fails criteria 2, 3 and (fatally) the premise of 4. The falsification target is a point in a semialgebraic set, not a member of a finite list: there is no n to search at, because a counterexample may have any number of vertices and failing at n<=30 proves nothing. Even after fixing n the combinatorial layer alone is ~10^71 at n=20 and each leaf needs a polynomial-system solve, i.e. a CAS. Verification is only exact if a witness happens to have rational or low-degree algebraic coordinates, which nothing guarantees -- the constraint system is overdetermined (3n equations, 2n-4 degrees of freedom), so known witnesses at k=3 exist only through imposed symmetry, and hunting symmetric families is …[truncated; see source]
- no move applies mechanically

### `S:deep:97` — deep audit: **WALL**, lane `wall`, board **NONE** (problems.json)

- finite object: A convex polygon (point set in the plane, ideally exact rational/algebraic coords) in which every vertex has at least 4 other vertices at a single common distance.
- current record: k=3 fully achieved (conjecture false at 3): Danzer convex 9-gon (per-vertex varying distance) and Fishburn-Reeds 1992 convex 20-gon (uniform unit distance). k=4: NO example of any size known; open. AlphaEvolve (Google DeepMind, arXiv:2511.02864, Nov 2025) attacked this exact problem with an exact/interval-arithmetic verifier and reproduced only k=3, failing to reach k=4.
- verifier: Given rational vertex coords: (a) verify convex position (O(n) orientation checks) and (b) for each vertex, group squared distances to others and check some value has multiplicity >=4 (sort per vertex). Exact, O(n^2 log n). Cheap and clean.
- attack: If it were movable: simulated annealing / evolutionary search over vertex coords minimizing per-vertex max equidistance error, normalized by min side length, then snap to exact algebraic coordinates. This IS AlphaEvolve's approach and it failed at scale. The only credible route is symbolic -- pick a combinatorial concyclic-quadruple incidence pattern, set up the polynomial system for exact equal distances + convexity, and solve via Groebner bases / CAD -- which is symbolic algebra, not our SAT/MIS/SA farm, with severe combinatorial blowup and no success guarantee. Rough compute: unbounded / no bounded search space.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `geometry`, `distances`, `convex`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P97` and the surface id in your receipt so the next agent's graph build finds your result.


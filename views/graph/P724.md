# Erdős #724 — attack card

> Erdős problem #724 · https://www.erdosproblems.com/724
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: N(10) — maximum number of mutually orthogonal Latin squares of order 10 (= A001438(10)))** — Open 60+ years whether 3 MOLS(10) exist; 2022–2025 SAT campaigns eliminate relation types only (9 of the Dukes–Howard types remain). [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:724:174068ce` — N(10) — maximum number of mutually orthogonal Latin squares of order 10 (= A001438(10)) (gap_map, kind `value_gap`, confidence C3)

- bounds: lower 2 (Parker orthogonal pair) · upper 6 (classical bound (design-theory survey))
- witness side: `lower` · witness feasibility `historic` · exact feasibility `wall`
- witness object: 3 mutually orthogonal Latin squares of order 10 (historic if found)
- witness verifier: Latin property per square + pairwise orthogonality (all 100 ordered symbol pairs distinct); a few lines, dependency-free
- notes: Open 60+ years whether 3 MOLS(10) exist; 2022–2025 SAT campaigns eliminate relation types only (9 of the Dukes–Howard types remain).
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `combinatorics`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P724` and the surface id in your receipt so the next agent's graph build finds your result.


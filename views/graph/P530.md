# Erdős #530 — attack card

> Erdős problem #530 · https://www.erdosproblems.com/530
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: l(N) — largest m such that EVERY N-element set of reals contains a Sidon subset of size …[truncated; see source])** — Known: N^{1/2} << l(N) <= (1+o(1))N^{1/2} — lower by Komlós–Sulyok–Szemerédi 1975 (improving the N^{1/3} bound noted by Erdős; problem originally Riddell 1969), upper from the case A={1..N}. That extremal case is exactly A143824 (max Sidon subset of {1..n}), which is why the OEIS relation is marked 'possible' on the site — A143824 is the upper envelope, NOT l(N). Alon–Erdős 1985 stronger conjecture: A decomposes into (1+o(1))sqrt(N) Sidon sets. Page (edited 2026-04-08) cites nothing newer than …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:530:0c0fbcfd` — l(N) — largest m such that EVERY N-element set of reals contains a Sidon subset of size m; asked: is l(N) ~ N^{1/2}? (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none tracked — for fixed N an upper-side witness (an N-set whose largest Sidon subset is small) would be cheaply checkable by exact max-Sidon ILP, but no literature table or record of l(N) values exists to improve against; the question is the asymptotic order
- witness verifier: n/a — asymptotic-order question; any single N gives an instance, not a movable record. (Verifying a given N-set's max Sidon subset is a small exact ILP, so a finite l(N) table COULD be seeded as a new lane if ever wanted.)
- notes: Known: N^{1/2} << l(N) <= (1+o(1))N^{1/2} — lower by Komlós–Sulyok–Szemerédi 1975 (improving the N^{1/3} bound noted by Erdős; problem originally Riddell 1969), upper from the case A={1..N}. That extremal case is exactly A143824 (max Sidon subset of {1..n}), which is why the OEIS relation is marked 'possible' on the site — A143824 is the upper envelope, NOT l(N). Alon–Erdős 1985 stronger conjecture: A decomposes into (1+o(1))sqrt(N) Sidon sets. Page (edited 2026-04-08) cites nothing newer than …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #14 shares A143824 → card `views/graph/P14.md`
- #30 shares A143824 → card `views/graph/P30.md`
- #43 shares A143824 → card `views/graph/P43.md`
- #155 shares A143824 → card `views/graph/P155.md`
- #861 shares A143824 → card `views/graph/P861.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P530` and the surface id in your receipt so the next agent's graph build finds your result.


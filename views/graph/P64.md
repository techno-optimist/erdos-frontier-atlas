# Erdős #64 — attack card

> Erdős–Gyárfás 2-power cycle conjecture · https://www.erdosproblems.com/64
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **TRAP (branch-scoped)** — prize **$1000** + upstream says `falsifiable`, and our status register records a **wall**. The wall covers the GENERAL branch; our triage marks a sub-branch **MAYBE** (see `S:triage:64` below). Strike the sub-branch, not the headline.
- **WALL (problem)** — The prize-relevant frontier is a wall on both sides. Proving the conjecture needs a new theorem (NONE-like). The disproof/witness search is unbounded (a counterexample has no size ceiling) and near-certainly null: the conjecture is widely believed, Liu–Montgomery proved it for large min degree AND killed the stronger version, and Carr-2026's 'predominantly cubic' structure further constrains any counterexample — a stronger R(5,5)-style no-witness situation. The one MOVABLE piece — extending Markström's exhaustive cubic verification 2–4 vertices (>=30 -> >=32/>=34) — is feasible but uses the record-holder's own tooling, claims no prize, does not resolve the conjecture, and pushes a number the field no longer tracks. Classic 'computational-looking record already at the wall.' [atlas/problems.json beatable=WALL]

## STATUS — three registers, never merged

- ours: **wall** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/64.lean)
- prize: $1000 — prizes are history, not targets (standing rule 2).

## STATEMENT (Apache-licensed, Lean-derived)

Does every finite graph with minimum degree at least $3$ contain a cycle of length $2^k$ for some $k \geq 2$?

## ATTACK SURFACES — which overlay said so

### `S:triage:64` — triage verdict **MAYBE** (finite_handle_triage, 2026-07-26)

- our summary: Erdős–Gyárfás: does every finite graph with minimum degree >= 3 contain a cycle of length 2^k for some k >= 2 (i.e. length 4, 8, 16, 32, ...)? Liu–Montgomery [LiMo20] settled it for minimum degree above some absolute constant, so the live case is small minimum degree, essentially delta = 3.
- witness: CONCRETE. A finite simple graph G with delta(G) >= 3 whose cycle-length set omits every power of two >= 4. Delivered as an adjacency matrix / graph6 string. The tractable sub-case: a CUBIC graph on 30 vertices with no C4, no C8, no C16 (32 > 30, so only three forbidden lengths). Markström's own reduction (Congr. Numer. 171 (2004)) shows a minimal counterexample has its degree->=4 vertices forming an independent set, and arXiv:2605.22844 (2026) strengthens this to ">= 4/7 of the vertices have degree exactly 3" — both point the search at the cubic lane.
- smallest untested case: Cubic graphs on n = 30 vertices. Markström generated ALL cubic graphs on fewer than 29 vertices (so <= 28; cubic orders are even, hence 30 is literally the next case). Separately, the non-cubic lane's smallest untested case is n = 16: Royle generated all C4-free graphs with the forced degree structure on fewer than 16 vertices.
- known frontier: CORRECTED 2026-07-27. (a) Refereed frontier is unchanged: Markstrom, Congr. Numer. 171 (2004) -- but our phrasing was too strong. We wrote 'generated all cubic graphs on <= 28 vertices'; the literature (Carr, Wikipedia) says only that the Royle/Markstrom searches establish a cubic counterexample needs >= 30 vertices, and Carr writes they 'suggest' it. Markstrom did find four 24-vertex cubic graphs whose only power-of-two cycle is C16. (b) LANE TAKEN, CLAIMED-UNCERTIFIED: …[truncated; see source]
- search-space estimate: CORRECTED 2026-07-27 -- two errors, one of them a 58x mis-costing. (1) INDEX ERROR: A002851 has offset 0 and a(n) counts connected cubic graphs on 2n NODES, so 30 vertices is A002851(15) = 845,480,228,069, not 'A002851(30)' (which would be 60 vertices). The value we quoted was right for n=30; the index label was wrong. (2) WRONG DENOMINATOR: 845 billion is not the job. A counterexample must be …[truncated; see source]
- verification cheap and exact: yes
- verdict reason: RETARGETED 2026-07-27 from TARGET to MAYBE. #64 itself is still OPEN (erdosproblems.com/64, zero claimed proofs, last edited 2026-04-10), and the $1000 general-branch TRAP verdict stands and is reinforced: Liu-Montgomery own the large-min-degree branch, and after Balaji the remaining ground starts at n >= 32 where C32 first fits -- a materially harder regime than the {C4,C8,C16}-only range. What changed is the NOVELTY of our specific cubic n=30 cell: an unrefereed artifact claims it (inside a stronger general-min-degree-3 result) ten days before we looked. DO NOT frame any #64 work as a first n=30 cubic sweep. The one defensible framing left is an INDEPENDENT, NON-SAT reproduction of the …[truncated; see source]
- applies: **finite-handle witness search** (predicate `triage-move`)

### `S:deep:64` — deep audit: **WALL**, lane `exact-backtracking`, board **HEAVY** (problems.json)

- finite object: A cubic (or min-degree-3) graph on n vertices avoiding every cycle length 4, 8, 16, 32, ... — either as a counterexample (witness) or certified-absent at fixed n (nonexistence).
- current record: Any counterexample >= 17 vertices; any CUBIC counterexample >= 30 vertices — exhaustive computer search by G. Royle & K. Markström (~2004) via cubic-graph generation (genreg/nauty-style), verifying all cubic graphs on <= 28 vertices (29 impossible by parity). Sharpest near-miss: four 24-vertex cubic graphs with no C4/C8 whose only 2-power cycle is C16. Bound UNMOVED for ~20 years; all 2023–2026 progress is theoretical (P13-free graphs; Liu–Montgomery for large min degree; Carr 2026 'predominantly cubic' structure).
- frontier: no counterexample: verified for all cubic graphs <= 28 vertices (Royle-Markstrom ~2004); extension = exhaustive-generation certificate
- verifier: For a fixed graph, detect existence of a cycle of each length 2^k <= n. In bounded-degree (cubic) graphs, C4/C8 detection is trivial and C16/C32 detection is poly-time (color-coding / bounded DP). Cost is negligible per graph; almost all graphs are eliminated by the cheap C4 test, so amortized cost is tiny. No DRAT needed — nonexistence is an exhaustive-generation certificate (genreg/snarkhunter enumeration is the proof).
- attack: Only the nonexistence sub-problem is finite/bounded. Constrained-generate connected cubic graphs on n=30,32(,34) with the no-C4/no-C8 filter baked into generation (that class is extremely sparse — only 4 graphs at n=24), then test survivors for C16/C32. Extends the certified lower bound from >=30 to >=32/>=34. Rough compute: n=30 ~1–3 core-days, n=32 tens of core-days on a 100-core farm, n>=36 out of reach. The WITNESS (prize) search is unbounded — no n ceiling — and near-certainly null, so it is not a finite computation.
- applies: **exact backtracking** (predicate `lane-move`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `cycles`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P64` and the surface id in your receipt so the next agent's graph build finds your result.


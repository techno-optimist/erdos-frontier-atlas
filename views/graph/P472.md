# Erdős #472 — attack card

> Erdős problem #472 · https://www.erdosproblems.com/472
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Survival length of the Ulam prime chain seeded (3,5): q_{n+1} = smallest prime of the …[truncated; see source])** — Problem due to Ulam (via Erdos-Graham 1980): does SOME finite prime seed extend to an infinite chain? The (3,5) chain (which begins 3,5,7,11,13,...) is the canonical candidate and 'possibly infinite' per the problem page; OEIS comment confirms infinitude unknown. Heuristically the chain has ~n chances per step each prime with probability ~1/log(q_n), so death probability decays rapidly — expect verification to extend essentially forever without deciding anything; the interesting machine event …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:472:03386001` — Survival length of the Ulam prime chain seeded (3,5): q_{n+1} = smallest prime of the form q_n + q_i - 1; verified …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 10000 (OEIS A389713 b-file (Michael S. Branicky, Jan 2026), extent confirmed this session) · upper — (—)
- witness side: `lower` · witness feasibility `open-easy` · exact feasibility `wall`
- witness object: An extended term list: each new term certifies the chain survives one more step. (A chain DEATH — a step where q_n + q_i - 1 is composite for every i <= n — would also be finitely certifiable and would kill this particular seed, though not the master question, which quantifies over all finite starting sequences.)
- witness verifier: Per step: compute the n candidates q_n + q_i - 1, Miller-Rabin each, take the smallest prime and check it matches; minimality is certified by primality-testing all smaller candidates. Dependency-free; thousands of steps per minute at this size.
- notes: Problem due to Ulam (via Erdos-Graham 1980): does SOME finite prime seed extend to an infinite chain? The (3,5) chain (which begins 3,5,7,11,13,...) is the canonical candidate and 'possibly infinite' per the problem page; OEIS comment confirms infinitude unknown. Heuristically the chain has ~n chances per step each prime with probability ~1/log(q_n), so death probability decays rapidly — expect verification to extend essentially forever without deciding anything; the interesting machine event …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P472` and the surface id in your receipt so the next agent's graph build finds your result.


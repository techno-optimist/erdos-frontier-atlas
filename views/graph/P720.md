# Erdős #720 — attack card

> Size Ramsey numbers of paths & cycles · https://www.erdosproblems.com/720
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **solved-upstream** (atlas/stubs.json)
- upstream: **Solved** (raw `proved`)
- formal: not in formal-conjectures
- prize: $100 — prizes are history, not targets (standing rule 2).

## ATTACK SURFACES — which overlay said so

### `S:deep:720` — deep audit: **WALL**, lane `SAT+DRAT-nonexistence`, board **HEAVY** (problems.json)

- finite object: A host graph H (with m edges) that "arrows" the target (H → G): every red/blue edge-colouring yields a monochromatic path/cycle. The exact value R̂ is the minimum such m over all host graphs.
- current record: Headline: PROVED — Beck 1983 [Be83b], R̂(P_n)≪n and R̂(C_n)≪n (linear, stronger than asked); constant sharpened to <137n (Dubroff–et al. / Ryerson line). $100 already claimed. Exact small values (nauty + backtracking): r̂(P₄,P₅)=10, r̂(P₅,P₅)=11, r̂(P₄,P₆)=14, r̂(C₄,C₅)=19, r̂(C₆,C₆)=26 — Li, Fahad, Zhou, Yang, Frontiers in Physics 8:350 (2020). SAT/SMS (Kissat) used for related small Ramsey values 2022–2026, but not to extend diagonal R̂(P_n)/R̂(C_n) beyond tiny n.
- frontier: size-Ramsey exact small values (r-hat(P5,P5)=11, ...); each upper-bound move = one host + one DRAT UNSAT certificate
- verifier: Arrowing check H→G: SAT-encode "∃ 2-colouring of E(H) with no monochromatic G", solver returns UNSAT (H arrows) or a witness colouring (H fails). coNP-complete (Burr). UPPER bound = one host H + a single DRAT UNSAT cert: cheap, farm-friendly, our exact pattern. EXACT value additionally needs the LOWER bound: nauty-enumerate every host with ≤m−1 edges and exhibit an avoiding colouring for each — Σ₂-style, super-exponential host count, the binding wall.
- attack: If attempted (not recommended for prize): pick one under-determined small target, e.g. diagonal R̂(P_6) or R̂(C_7) where only an upper bound is published. (1) Local-search / SA over candidate host graphs H of a few dozen edges to find one that arrows the target; (2) certify H→G by encoding avoid-colouring as CNF and running Kissat to UNSAT + emit DRAT. That yields a certified upper-bound improvement. Pinning the EXACT value requires the outer nauty sweep of all smaller hosts (each a SAT call) — feasible only for ≤~6-vertex targets already done; ~100-core farm buys maybe one extra vertex before the host-enumeration blows up. Net: a minor certified upper bound is reachable; an exact new value …[truncated; see source]
- applies: **SAT + DRAT nonexistence** (predicate `lane-move`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`, `ramsey theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P720` and the surface id in your receipt so the next agent's graph build finds your result.


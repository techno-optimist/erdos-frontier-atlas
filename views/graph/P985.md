# Erdős #985 — attack card

> Erdős problem #985 · https://www.erdosproblems.com/985
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: largest N such that every prime 3 <= p <= N has a prime primitive root q < p (verified …[truncated; see source])** — Edge convention: for p=2 no prime q<2 exists; read for p >= 3 (Guy UPINT F9 phrases it for large p; the stub's Lean form uses q <= p). Literature frontier is well beyond my certified floor: Paszkiewicz–Schinzel (Math. Comp. 71:1307–1321, 2002) tabulated least prime primitive roots and Paszkiewicz (Math. Comp. 78:1193–1195, 2009) computed least primitive roots for ALL p < 10^12 (per Moree's Artin survey arXiv:math/0412262, sect. on g(p)=h(p)); I could not pin the exact certified range of the …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/985.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Is it true that, for every prime $p$, there is a prime $q \leq p$ which is a primitive root modulo $p$?

## ATTACK SURFACES — which overlay said so

### `S:gap:985:e049360b` — largest N such that every prime 3 <= p <= N has a prime primitive root q < p (verified range of the conjecture) (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 104729 (self-verified 2026-07-18 from OEIS A002233 b-file (T. D. Noe, first 10^4 primes): max least prime primitive root over primes 3..104729 is 107, always < p) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `wall`
- witness object: extension certificate: for each prime p in (current N, new N], a prime q < p together with the prime factorization of p-1 and the checks q^((p-1)/r) != 1 (mod p) for every prime r | p-1
- witness verifier: per prime, verify the factorization of p-1 by multiplication plus primality of its factors, then k modular exponentiations; compact, dependency-free, spot-checkable certificates (only exceptional primes with large least prime primitive root need storing — max is 107 below 10^5)
- notes: Edge convention: for p=2 no prime q<2 exists; read for p >= 3 (Guy UPINT F9 phrases it for large p; the stub's Lean form uses q <= p). Literature frontier is well beyond my certified floor: Paszkiewicz–Schinzel (Math. Comp. 71:1307–1321, 2002) tabulated least prime primitive roots and Paszkiewicz (Math. Comp. 78:1193–1195, 2009) computed least primitive roots for ALL p < 10^12 (per Moree's Artin survey arXiv:math/0412262, sect. on g(p)=h(p)); I could not pin the exact certified range of the …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P985` and the surface id in your receipt so the next agent's graph build finds your result.


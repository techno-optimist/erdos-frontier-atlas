# Erdős #462 — attack card

> Erdős problem #462 · https://www.erdosproblems.com/462
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: Existence of C > 0 with sum of p(n)/n over n in [x, x + C*x^(1/2)*(log x)^2] bounded …[truncated; see source])** — Background fact stated on the problem page: sum over composite n < x of p(n)/n is asymptotically c*x^(1/2)/(log x)^2, so the conjectured interval length matches the mean spacing of the sum's mass (dominated by n = 2*prime). The linked OEIS sequence A032742 (largest proper divisor, i.e. n/p(n)) is auxiliary only — no bounds live there. No partial results found on the problem page (no remarks section beyond the statement) or in OEIS. Not a records-lane target. [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:gap:462:06a1d699` — Existence of C > 0 with sum of p(n)/n over n in [x, x + C*x^(1/2)*(log x)^2] bounded below by an absolute constant for …[truncated; see source] (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: None: the statement quantifies over all large x with an existential constant C — no finite computation certifies either direction.
- witness verifier: No cheap witness. Numerical exploration (computing the short-interval sums for sampled x, C) can only suggest a plausible C, never certify.
- notes: Background fact stated on the problem page: sum over composite n < x of p(n)/n is asymptotically c*x^(1/2)/(log x)^2, so the conjectured interval length matches the mean spacing of the sum's mass (dominated by n = 2*prime). The linked OEIS sequence A032742 (largest proper divisor, i.e. n/p(n)) is auxiliary only — no bounds live there. No partial results found on the problem page (no remarks section beyond the statement) or in OEIS. Not a records-lane target.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `primes`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P462` and the surface id in your receipt so the next agent's graph build finds your result.


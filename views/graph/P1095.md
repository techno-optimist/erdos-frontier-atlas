# Erdős #1095 — attack card

> Erdős problem #1095 · https://www.erdosproblems.com/1095
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research solved, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/1095.lean)

## ATTACK SURFACES — which overlay said so

### `S:gap:1095:29d2c41e` — A003458(378) = g(378) — Erdős–Selfridge function: least n > 379 such that the least prime factor of binomial(n,378) …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `upper` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: any integer n > 379 with lpf(binomial(n,378)) > 378 upper-bounds g(378); smaller qualifying n = better record; the exact value additionally needs the sieve certificate that no smaller n qualifies
- witness verifier: Kummer's theorem, dependency-free: p | binomial(n,k) iff adding k and n-k in base p produces a carry; so for each of the 74 primes p <= 378 check every base-p digit of 378 is <= the corresponding digit of n (no carries). Pure integer arithmetic, instant even for n ~ 1e31
- notes: FRESHNESS CATCH (verified in primary source): the OEIS b-file stops at k=375 (a(375)=12863999653788432184381680413559) but the Sorenson–Sorenson–Webster arXiv v3 addendum (arXiv:1907.08559v3, June 2021, supplementing their ANTS XIV paper, Open Book Ser. 4 (2020) 23) reports g(376)=7778804220120654420924631668091 (one week wall time) and g(377)=5973303871796437264595936954237 (~two weeks) — so a(378) is the true frontier and adding 376-377 to OEIS is a free librarian win. Expected magnitude …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `binomial coefficients`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1095` and the surface id in your receipt so the next agent's graph build finds your result.


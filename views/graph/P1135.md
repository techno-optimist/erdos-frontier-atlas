# Erdős #1135 — attack card

> Collatz (3n+1) convergence · https://www.erdosproblems.com/1135
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (problem)** — exhaustion claim (2^71 sieve) has no certificate - adjudication = full recompute at GPU-supercomputer scale; cycle-witness side is an existence bounty with no frontier [atlas/problems.json wall_reason]
- **WALL (surface: Collatz (3x+1) conjecture — every positive integer reaches 1; machine-facing quantity: …[truncated; see source])** — Hub status: wall — correctly so. Not originally Erdős's problem (Collatz, pre-1952); the $500 figure traces to a 1983 Erdős–Graham conversation reported by Lagarias, not a formal prize offer; Erdős called such problems ones mathematics 'may not be ready for' (Guy UPINT E16 / Lagarias 2016 notes, paraphrased from erdosproblems.com/1135). Theory rungs: at least N^0.84 of integers below N eventually reach 1 (Krasikov–Lagarias 2002); almost-all logarithmic-density convergence (Tao 2019, not …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **wall** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `None` (formal-conjectures/FormalConjectures/ErdosProblems/1135.lean)
- prize: $500 — prizes are history, not targets (standing rule 2).

## STATEMENT (Apache-licensed, Lean-derived)

The Collatz conjecture states that for any positive integer $n$, there exists a natural number $m$ such that the $m$-th term of the sequence is 1.

## ATTACK SURFACES — which overlay said so

### `S:deep:1135` — deep audit: **WALL**, lane `wall`, board **NONE** (problems.json)

- finite object: Two candidate finite witnesses: (a) a disproof — a nontrivial cycle (finite orbit of integers, exactly checkable) or the certified start of a provably divergent orbit; (b) a verification extension — certifying every m ≤ N reaches 1, which extends OEIS A008908/path-record tables. Neither settles the (infinite, uncomputable) headline; the positive claim is explicitly NOT finite-checkable.
- current record: Verification: all m ≤ 2^71 ≈ 2.36×10^21 reach 1 — David Barina, published Jan 2025 (J. Supercomputing, DOI 10.1007/s11227-025-07337-0), extending his own 2^68 (2020/2021). Method: hyper-optimized GPU sieve (1,335× over naive CPU) distributed across thousands of workers on several European supercomputers. Cycle nonexistence: no nontrivial Collatz m-cycle with m ≤ 91 (Hercher 2022, J. Integer Seq. 2023), building on Simons–de Weger; combined with the 2^69+ verified range this rules out short cycles up to astronomical magnitude.
- verifier: Trivial and exact: iterate f from a seed until it hits 1 (converged) or exceeds the tested bound; a cycle witness is verified by following the finite orbit back to its start (O(cycle length) exact integer arithmetic). Verification cost is O(1) memory per seed but the *search* is O(N) seeds — the checker is cheap, the exhaustion is the cost.
- attack: No viable attack. If forced: a residue-class sieve (2^k-block reachability with precomputed acceleration tables) on the CPU farm could re-verify up to ~2^60–2^63 in days, but that is far below the 2^71 frontier — pure catch-up, no new record. SAT/ILP/MIS are inapplicable. The only "novel" finite angle (search for a nontrivial cycle via the cycle-structure Diophantine constraints of Simons–de Weger/Hercher) is already pushed past m=91 by dedicated number-theoretic bounding, not brute search, and is not a fit for our solvers.
- no move applies mechanically

### `S:gap:1135:e848a088` — Collatz (3x+1) conjecture — every positive integer reaches 1; machine-facing quantity: the exhaustively verified …[truncated; see source] (gap_map, kind `verified_range`, confidence C3)

- bounds: lower 2^71 (Barina convergence-verification project (pcbarina.fit.vutbr.cz, log entry 2025-01-15: all n < 2^71 verified); published rung: Barina, Improved verification limit for the convergence of the Collatz conjecture, J. Supercomputing (2025); earlier 2^68 in J. Supercomputing 77:2681–2688 (2021)) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: No succinct witness: extending the verified range produces no independently checkable certificate — re-verification costs the same supercomputer effort as the original run. (Side records — path/delay records like A006877/A008908 maxima — ARE cheaply verifiable per-n but bound nothing about the conjecture.)
- witness verifier: None for the range record (recomputation only). For a claimed path/delay record n: iterate the map and count steps — trivial, but out of scope of the conjecture's truth.
- notes: Hub status: wall — correctly so. Not originally Erdős's problem (Collatz, pre-1952); the $500 figure traces to a 1983 Erdős–Graham conversation reported by Lagarias, not a formal prize offer; Erdős called such problems ones mathematics 'may not be ready for' (Guy UPINT E16 / Lagarias 2016 notes, paraphrased from erdosproblems.com/1135). Theory rungs: at least N^0.84 of integers below N eventually reach 1 (Krasikov–Lagarias 2002); almost-all logarithmic-density convergence (Tao 2019, not …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P1135` and the surface id in your receipt so the next agent's graph build finds your result.


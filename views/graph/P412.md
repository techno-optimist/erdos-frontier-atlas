# Erdős #412 — attack card

> Erdős problem #412 · https://www.erdosproblems.com/412
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/412.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $σ_1(n)=σ(n)$, the sum of divisors function, and $σ_k(n) = σ(σ_{k-1}(n))$. Is it true that, for every $m, n ≥ 2$, there exist some $i, j$ such that $σ_i(m) = σ_j(n)$?

## ATTACK SURFACES — which overlay said so

### `S:gap:412:21c96967` — A051572(524) — next term of the iterated-σ trajectory of seed 5; the frontier of the (m,n) = (2,5) instance of the van …[truncated; see source] (gap_map, kind `next_cell`, confidence C3)

- bounds: lower 523 terms known; a(523) has 437 digits; no common value with the 1500 known iterates of seed 2 (intersection of b-files checked empty 2026-07-18) (OEIS A051572 b-file (N. J. A. Sloane) + fresh collision check against A007497 b-file done for this entry) · upper — (—)
- witness side: `lower` · witness feasibility `plausible` · exact feasibility `cell`
- witness object: a(524) = σ(a(523)) with the complete prime factorization of a(523); the grand prize witness for the (2,5) instance would be indices (i,j) with σ_i(2) = σ_j(5) plus the factor-chain certificates of both trajectories
- witness verifier: Same scheme as A007497: re-multiply claimed prime powers to reproduce a(523), primality certificates for each prime, compute σ = Π (p^(k+1)-1)/(p-1), compare. For a merge witness: verify both trajectory chains and the equality of the two terminal values. Dependency-free bignum arithmetic.
- notes: Erdős attributed the conjecture to van Wijngaarden (1950s); Erdős–Graham thought nothing provable soon; Selfridge reported numerical evidence suggesting the answer may be NO — i.e. the merge witness may not exist, so the durable machine record is extending both trajectories (A051572 at 523 terms is far behind A007497 at 1500) and re-running the collision check. Bootstrap re-factoring of the 523-step chain from seed 5 is days of ECM work with the usual hard-cofactor risk. Cross-refs: A007497 …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- #410 shares A007497 → card `views/graph/P410.md`

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P412` and the surface id in your receipt so the next agent's graph build finds your result.


# Erdős #975 — attack card

> Erdős problem #975 · https://www.erdosproblems.com/975
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **WALL (surface: existence of the asymptotic constant c(f) in Sum_{n<=X} tau(f(n)) ~ c*X*log X for …[truncated; see source])** — Order of magnitude is settled: Sum tau(f(n)) >> X log X (van der Corput 1939) and << X log X (Erdős 1952, elementary), so only the existence of the limit constant is open, and only for deg f >= 3. All irreducible quadratics are solved (Hooley 1963; McKee 1995/1997/1999 give the constants), e.g. Sum_{n<=x} tau(n^2+1) = (3/pi) x log x + O(x). A147807 tracks the SOLVED quadratic case (partial sums of tau(n^2+1)/2, c = 3/(2*pi) per Eldar's 2023 OEIS comment) — it is a sanity harness, not a …[truncated; see source] [atlas/gap_map.json exact_feasibility=wall]

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/975.lean)

## STATEMENT (Apache-licensed, Lean-derived)

For an irreducible polynomial $f \in \mathbb{Z}[x]$ with $f(n) \ge 1$ for sufficiently large $n$, does there exists a constant $c = c(f) > 0$ such that $\sum_{n \le x} \tau(f(n)) \approx c \cdot x \log x$? Note that it is unclear whether the polynomial should have integer coefficients or merely be integer-valued. We assume the former.

## ATTACK SURFACES — which overlay said so

### `S:gap:975:3d3c8578` — existence of the asymptotic constant c(f) in Sum_{n<=X} tau(f(n)) ~ c*X*log X for irreducible f in Z[x] of degree >= 3 (gap_map, kind `not_gap_shaped`, confidence C3)

- bounds: lower — (—) · upper — (—)
- witness side: `none` · witness feasibility `none` · exact feasibility `wall`
- witness object: none — the question is whether an asymptotic exists for every irreducible f; no finite computation or object certifies or improves it
- witness verifier: none exists; any finite partial-sum table (A147807 b-file has 10^4 terms for the solved quadratic n^2+1 case) is consistent with both outcomes
- notes: Order of magnitude is settled: Sum tau(f(n)) >> X log X (van der Corput 1939) and << X log X (Erdős 1952, elementary), so only the existence of the limit constant is open, and only for deg f >= 3. All irreducible quadratics are solved (Hooley 1963; McKee 1995/1997/1999 give the constants), e.g. Sum_{n<=x} tau(n^2+1) = (3/pi) x log x + O(x). A147807 tracks the SOLVED quadratic case (partial sums of tau(n^2+1)/2, c = 3/(2*pi) per Eldar's 2023 OEIS comment) — it is a sanity harness, not a …[truncated; see source]
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`, `divisors`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P975` and the surface id in your receipt so the next agent's graph build finds your result.


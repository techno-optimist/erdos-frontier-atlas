# Erdős #548 — attack card

> Erdős–Sós conjecture (trees in dense graphs) · https://www.erdosproblems.com/548
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- **TRAP** — upstream says `falsifiable`, but our status register records a **wall**: the finite handle does not make it reachable here (reasons in atlas/walls.md, finite-handle table). Decidable in principle is not reachable in practice.
- **WALL (problem)** — no finite search space (n unbounded) and no numeric record; positive direction has no finite certificate [atlas/problems.json wall_reason]
- **WALL (surface: Erdős–Sós conjecture: for n >= k+1, every graph on n vertices with at least (k-1)n/2 + 1 …[truncated; see source])** — PRIOR ATLAS WALL CONFIRMED. Criterion 3 passes (tree-containment is exact, integer, dependency-free) and criterion 1 passes, but criterion 2 fails by five orders of magnitude at the first genuinely open cell (~1e17 ops) and — more fundamentally — the search space has no finite bottom. The standard reductions bound the minimum degree of a minimal counterexample below (>= ceil(k/2)) and force connectedness, but nothing bounds n from above for a fixed tree, so "falsifiable" here means only that a counterexample, if it exists, is a finite object; it does not give a finite check. This is exactly the failure mode the triage brief warns about: a real witness that may live arbitrarily far out. [atlas/finite_handle_triage.json verdict=WALL]

## STATUS — three registers, never merged

- ours: **wall** (atlas/stubs.json)
- upstream: **Open** (raw `falsifiable`, finite handle `falsifiable`)
- formal: not in formal-conjectures

## ATTACK SURFACES — which overlay said so

### `S:triage:548` — triage verdict **WALL** (finite_handle_triage, 2026-07-26)

- our summary: Erdős–Sós conjecture: for n >= k+1, every graph on n vertices with at least (k-1)n/2 + 1 edges contains every tree on k+1 vertices (equivalently: average degree > k-1 forces every k-edge tree as a subgraph).
- witness: CONCRETE. A pair (G, T): a graph G on n vertices with at least (k-1)n/2 + 1 edges, and a tree T on k+1 vertices that is not a subgraph of G. Both are small finite objects and the non-containment is exactly checkable — tree subgraph-isomorphism into an n-vertex host is a 2^k * poly(n) integer DP (colour-coding / Alon-Yuster-Zwick style), no floating point.
- smallest untested case: Tree side: every tree on <= 7 vertices is already covered, because on 7 vertices every tree is either a spider (at most one branch vertex — two branch vertices at distance >= 2 with diameter 5 needs 8 vertices) or has diameter <= 4, and both classes are settled. So the first uncovered tree is on 8 vertices (7 edges), diameter 5, two branch vertices. Host side: settled whenever the host order exceeds the tree order by at most 4, so the first open cell is host order = tree order + 5, i.e. n = 13 …[truncated; see source]
- known frontier: No exhaustive computer search found. Progress is by host order: settled for host order equal to the tree order (Zhou), +1 (Slater–Teo–Yap), +2 (Woźniak), +3 (Tiner), +4 (Yuan–Zhang 2014, arXiv:1403.5430). By tree class: stars (trivial, noted by Erdős himself), paths (Erdős–Gallai), double stars, spiders, and diameter <= 4 (McLennan 2005). Ajtai–Komlós–Simonovits–Szemerédi announced a proof for all sufficiently large k (2008, still unpublished). The site also notes graph-class results: …[truncated; see source]
- search-space estimate: First open cell (8-vertex tree, host n = 13, >= 40 edges): OEIS A000088(13) = 5.05e13 graphs on 13 vertices; roughly half sit at or above the median edge count, so ~2.5e13 candidates, times ~1e4 ops for each tree-embedding test ≈ 1e17 primitive ops. Even the min-degree >= ceil(k/2) = 4 and connectedness reductions (a minimal counterexample is connected with min degree > (k-1)/2) do not bring this …[truncated; see source]
- verification cheap and exact: yes
- verdict reason: PRIOR ATLAS WALL CONFIRMED. Criterion 3 passes (tree-containment is exact, integer, dependency-free) and criterion 1 passes, but criterion 2 fails by five orders of magnitude at the first genuinely open cell (~1e17 ops) and — more fundamentally — the search space has no finite bottom. The standard reductions bound the minimum degree of a minimal counterexample below (>= ceil(k/2)) and force connectedness, but nothing bounds n from above for a fixed tree, so "falsifiable" here means only that a counterexample, if it exists, is a finite object; it does not give a finite check. This is exactly the failure mode the triage brief warns about: a real witness that may live arbitrarily far out.
- no move applies mechanically

### `S:deep:548` — deep audit: **WALL**, lane `wall`, board **NONE** (problems.json)

- finite object: A single counterexample graph G plus a specific (k+1)-vertex tree T: e(G) ≥ (k-1)/2·|V(G)| + 1 yet T ⊄ G. Finite and cheaply checkable, but almost certainly nonexistent and unbounded in n/k.
- current record: No numeric record — open conjecture. Best-known frontier: proven for large k (Ajtai–Komlós–Simonovits–Szemerédi, ~1990s announcement); large trees with near-spanning host (Reed–Stein, arXiv:2405.15733, 2024 rev. 2025); spiders (Fan–Sun, all leg lengths; Woźniak legs≤2); caterpillars (Perles, 2017); bounded-degree trees + dense hosts (1906.10219); near-diagonal k=n-1,n-2,n-3 (Woźniak), n-4 (Tiner), n=k+4 (Wang/Li 2014). No counterexample known or expected.
- verifier: Exact and cheap for a CLAIMED counterexample: edge-count check (trivial) + subgraph-isomorphism T↪G for a fixed (k+1)-vertex tree T, poly in n via color-coding / bounded-pattern search (O(n^{O(k)}) naive, FPT in k). The verifier is fine; the problem is there is no bounded search space to run it over (n unbounded) and the positive direction has no finite certificate.
- attack: Only conceivable attack: SAT/ILP or exhaustive counterexample hunt — for small trees T (small k) encode "G on n vertices, e(G) ≥ (k-1)/2·n+1, and G avoids T" and search over n up to feasibility (~n≤20–25, k≤~8) on the CPU farm; verify any hit with the exact subgraph-iso checker. Rough compute: modest (hours–days on ~100 cores). Expected outcome: UNSAT everywhere reachable (region already covered by AKSS + small-case theorems), producing no new result. No positive-direction certificate is possible (unbounded n). Recommend NOT pursuing.
- no move applies mechanically

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `graph theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P548` and the surface id in your receipt so the next agent's graph build finds your result.


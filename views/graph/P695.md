# Erdős #695 — attack card

> Erdős problem #695 · https://www.erdosproblems.com/695
> Generated by `tools/build_graph.py` from committed ledgers — regenerate with `make graph`. A disagreement with this card is a bug report against its sources, never an edit here.

## STOP — read before spending

- No recorded walls, traps, retraction pins, or campaign findings on this problem. Absence of a wall is not evidence of feasibility — run `M:literature-freshness-check` before any strike.

## STATUS — three registers, never merged

- ours: **open** (atlas/stubs.json)
- upstream: **Open** (raw `open`)
- formal: research open, answer `sorry` (formal-conjectures/FormalConjectures/ErdosProblems/695.lean)

## STATEMENT (Apache-licensed, Lean-derived)

Let $q_1 < q_2 < \cdots$ be a sequence of primes such that $q_{i + 1} \equiv 1 \pmod{q_i}$. Is it true that $$ \lim_{k \to \infty} q_k^{1/k} = \infty? $$

## ATTACK SURFACES — which overlay said so

### `S:gap:695:160904d2` — g(k) = minimal terminal prime q_k over ascending prime chains q_1<...<q_k with q_{i+1} ≡ 1 (mod q_i); greedy chain …[truncated; see source] (gap_map, kind `value_gap`, confidence C3)

- bounds: lower g(k) >= 3·2^(k-2) (each step forces q_{i+1} >= 2·q_i + 1) (elementary doubling argument (q_{i+1} ≡ 1 mod q_i, prime, > q_i)) · upper g(k) <= A061092(k) (greedy chain 2,3,7,29,59,709,2837,22697,590123,...; known to k=100, a(100) has 175 digits) (OEIS A061092 (Murthy) + b-file)
- witness side: `upper` · witness feasibility `open-easy` · exact feasibility `cell`
- witness object: A length-k chain of primes q_1<...<q_k with q_{i+1} ≡ 1 (mod q_i) and q_k < A061092(k) — a sub-greedy record. The min-terminal-prime sequence g(k) appears NOT to be in OEIS: a computed record table would be submission-worthy.
- witness verifier: k primality checks (deterministic Miller-Rabin below 2^64; Pratt/ECPP certificates beyond) plus k-1 congruence checks q_{i+1} mod q_i == 1. Trivial, dependency-free.
- notes: Greedy is pointwise-minimal per step but need not be globally optimal (a larger early prime can enable much smaller later ones) — bounded DFS over primes ≡ 1 mod q below the greedy cap is an hours-scale search for k <= ~12, and exact g(k) for modest k falls to exhaustive search below the greedy bound. CAVEAT (honesty): Erdos's question concerns one infinite nested chain; g(k)-optimal chains need not nest, so records are evidence, not resolution — lim q_k^{1/k} = infinity? and existence of a …[truncated; see source]
- applies: **witness local search** (predicate `workable`)

## EVIDENCE — replayable, or it does not count

- No promoted contract claim touches this problem (certificates/contracts.json).

## BRIDGES — structurally adjacent problems

- No shared-OEIS family recorded. Tag neighbors: `number theory`.

## PROMOTE — what a win must look like

1. Receipt: `python3 -I <verifier>.py` — exact arithmetic, a planted-failure control that must FAIL, emit/check split so replays never rewrite the artifact, machine verdict on stdout.
2. Evidence slot: an `evidence[]` item on the surface's gap_map row (`{type, artifact, date}`) and, for certificate-grade results, a claim in `certificates/contracts.json` with sha256-pinned artifacts.
3. Freshness: before the strike, run `M:literature-freshness-check` — the #552/DS1 retraction is why.
4. Name the node: cite `P695` and the surface id in your receipt so the next agent's graph build finds your result.

